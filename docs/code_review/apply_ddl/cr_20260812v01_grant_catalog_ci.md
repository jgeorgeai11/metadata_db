---
name: cr_20260812v01_grant_catalog_ci
goal: Address code quality issues identified in code/apply_ddl/grant_catalog_ci.sql to align with sql-development best-practices (re-review since cr_20260729v01, reviewed alongside grant_ref_ro.sql for cross-script consistency after the GitLab→GitHub migration).
created: 2026-08-12 14:41:37
updated: 2026-08-12 14:53:18
---

## Implementation Plan

1. [completed] Make a partially-failed run visible - `code/apply_ddl/grant_catalog_ci.sql`
   - 1.1. [minor] Line 33: Add `\set ON_ERROR_STOP on` before the first statement. Without it, `psql -f` prints a failed statement's error on stderr, keeps executing the remaining statements, and still exits 0 — so a run that granted schema USAGE and main-table DML but failed on the `_hstry` block reports success, and the failure only resurfaces post-merge as a `permission denied` inside `load_catalog_data`'s single load transaction (`.github/workflows/post_merge.yml`), which then trips `revert_failed_load`. The script's own preconditions (lines 25-31) name the realistic causes — run before `apply_ddl.py` created the targets, or against a database whose objects differ — and each fails one statement while the rest proceed. Cross-file: the same gap exists in `grant_ref_ro.sql` and in `grant_catalog_ci_ro.sql` (flagged there as finding 1.1 of `cr_20260812v01_grant_catalog_ci_ro.md`, still pending), so the line belongs in all three for a consistent invocation contract.
        - Current: (no `ON_ERROR_STOP`; the file goes straight from the header comment to the `\if :{?schema}` default block on line 37)
        - Expected: `-- Abort on the first failed statement: a not-yet-created target or a`<br>`-- missing role must not leave a half-applied privilege model behind a`<br>`-- zero exit code.`<br>`\set ON_ERROR_STOP on`
        - Resolution: Implemented as specified — placed immediately after the header block, before the `\if :{?schema}` defaults. The cross-file gap is closed in the same changeset: `grant_catalog_ci_ro.sql` (its finding 1.1) and `grant_ref_ro.sql` (its finding 3.1) each got the same `\set ON_ERROR_STOP on` in the same position, so all three scripts now abort on the first failed statement.

2. [completed] State the role-existence precondition - `code/apply_ddl/grant_catalog_ci.sql`
   - 2.1. [minor] Lines 25-31: The Preconditions block omits that `metadata_db_ci` must already exist. The header frames the script purely as a post-rebuild restore ("the role itself is cluster-level and survives", lines 4-5), which is true for a rebuild but not for the other documented invocation: the activation checklist runs this script on a cluster where the role has just been created (`readme/metadata-db-maintenance.md` line 532, "both CI roles created — `metadata_db_ci` (write) — run `grant_catalog_ci.sql`"). Cross-file: both siblings state this precondition explicitly — `grant_catalog_ci_ro.sql` lines 34-36 ("Create the metadata_db_ci_ro role first ... Table grants to a nonexistent role fail") and `grant_ref_ro.sql` lines 37-38 ("The three roles above already exist") — so this file is the only one of the three that leaves a first-time maintainer to discover the ordering from an error message.
        - Current: `-- Preconditions:`<br>`--   * Run this AFTER apply_ddl.py has created the schema — every grant`
        - Expected: `-- Preconditions:`<br>`--   * The metadata_db_ci role already exists (a cluster-level LOGIN role;`<br>`--     it survives a database rebuild, but on first activation it must be`<br>`--     created first — see the maintenance doc's activation checklist).`<br>`--     Grants to a nonexistent role fail.`<br>`--   * Run this AFTER apply_ddl.py has created the schema — every grant`
        - Resolution: Implemented as specified — the new bullet leads the Preconditions block, ahead of the existing apply_ddl.py ordering bullet, so the three grant scripts now all state the role-existence precondition first.

3. [completed] State the maintenance obligation the enumerated grant lists create - `code/apply_ddl/grant_catalog_ci.sql`
   - 3.1. [minor] Lines 8-9: The header names the canonical table set's source file but not the obligation it creates: a migration that adds a main table must extend *two* lists here (the DML list, lines 61-71, and its `_hstry` mirror, lines 73-83), and nothing enforces it. The failure mode is worse than for the read-only sibling — the omission does not surface until the post-merge `load_catalog_data` job runs on `main` as `metadata_db_ci` and aborts the load transaction on `permission denied`, which then triggers `revert_failed_load` to push a revert of an already-merged commit. One clause turns that into a checklist item for the migration author. Cross-file: the same gap is flagged for `grant_catalog_ci_ro.sql` (finding 4.1 of `cr_20260812v01_grant_catalog_ci_ro.md`, pending), so the two files should adopt the same wording.
        - Current: `-- Privilege model (canonical table set defined in`<br>`-- code/apply_ddl/ddl_catalog/0001_initial_schema.sql): CONNECT on the database,`
        - Expected: `-- Privilege model (canonical table set defined in`<br>`-- code/apply_ddl/ddl_catalog/0001_initial_schema.sql — a migration that adds a`<br>`-- main table must add it to BOTH grant lists below): CONNECT on the database,`
        - Resolution: Implemented as specified. The read-only sibling got the matching clause in the same changeset (`grant_catalog_ci_ro.sql` finding 4.1, "must add it to the list below too"), so the two files carry the same sentence shape, differing only where this one must say BOTH (the DML list plus its `_hstry` mirror).

4. [completed] Optional comment enhancements - `code/apply_ddl/grant_catalog_ci.sql`
   - 4.1. [suggestion] Lines 61, 73, 85, 87: The four `grant` statements carry no comment directly above them, while the two that precede them (lines 50-53, 55-59) each explain why they are required — and `grant_catalog_ci_ro.sql` comments its main-table grant (lines 72-74). The `_hstry` grant (line 73) is the one where a comment earns the most: its omission of TRUNCATE is load-bearing beyond append-only-ness — it is what makes `load_catalog_data.py --reset-hstry` unrunnable as the CI role (`readme/metadata-db-maintenance.md` line 478), so a future maintainer debugging that failure has no local signal that the exclusion is deliberate rather than an oversight.
        - Current: `grant insert on`<br>`    systems_hstry,`
        - Expected: `-- INSERT-only, deliberately excluding TRUNCATE: that exclusion is what`<br>`-- keeps --reset-hstry (bootstrap history reset) maintainer-only.`<br>`grant insert on`<br>`    systems_hstry,`
        - Resolution: Deferred — optional; the header (lines 8-16) already states the full privilege model including the "no UPDATE/DELETE/TRUNCATE" intent and the append-only rationale, so the reasoning is present in the file, just not directly above each statement.
   - 4.2. [suggestion] Line 87: `select` on `load_audit` is granted but exercised by no code path — `code/load_catalog_data/db_io.py` only inserts (`_INSERT_LOAD_AUDIT`, lines 542-543), and the documented reads of `load_audit` (joining `loaded_ts` to a row's `update_ts` for provenance, `readme/metadata-db-maintenance.md` line 462) are human/BI queries run under other identities. The grant is harmless and matches the documented model, but a one-line note would say whether it is intentional headroom or an oversight.
        - Current: `grant select, insert on load_audit to metadata_db_ci;`
        - Expected: `-- INSERT for the per-load audit row; SELECT is headroom for the loader to`<br>`-- read its own prior runs (no current code path reads it).`<br>`grant select, insert on load_audit to metadata_db_ci;`
        - Resolution: Deferred — optional; the grant matches the privilege model published in `readme/metadata-db-maintenance.md` line 303 ("SELECT+INSERT on `load_audit`") and narrowing it to INSERT-only would be a behavior change beyond a comment fix, better decided with the maintenance doc in the same edit.
   - 4.3. [suggestion] Lines 1-6: The header never names the consuming CI job, unlike `grant_catalog_ci_ro.sql`, whose "Purpose" paragraph (lines 8-17) names the jobs, the workflow file, and the credential-split rationale. This is the write half of that same split — the role is confined to `load_catalog_data` in `.github/workflows/post_merge.yml` — and stating it here would make the pair readable from either side.
        - Current: `-- Restores the metadata_db_ci loader-role privilege model after a`<br>`-- database rebuild ...`
        - Expected: add a "Purpose" paragraph naming `load_catalog_data` in `.github/workflows/post_merge.yml` as the sole consumer and pointing at the read-only counterpart script, mirroring `grant_catalog_ci_ro.sql` lines 8-17
        - Resolution: Deferred — optional documentation symmetry; the credential split is fully described in `grant_catalog_ci_ro.sql` and in `readme/metadata-db-maintenance.md` (lines 303-304), and nothing in this file is inaccurate without it.

## Skills with No Issues

1. sql-development / best-practices (privilege-model completeness): No issues found. The 9 tables granted DML (lines 62-70) and the 9 `_hstry` tables granted INSERT (lines 74-82) match the 9 main and 9 `_hstry` `create table` statements in `code/apply_ddl/ddl_catalog/0001_initial_schema.sql` (lines 83, 105, 119, 140, 159, 227, 270, 311, 345 and 390, 401, 413, 426, 439, 460, 475, 496, 514), and `load_audit` (line 534) and `ddl_versions` are each granted separately.
2. sql-development / best-practices (privilege sufficiency for the consuming job): No issues found. `load_catalog_data.py` needs exactly SELECT (`read_db_state`), INSERT/UPDATE/DELETE on the main tables, INSERT on the `_hstry` mirrors, and INSERT on `load_audit` (`code/load_catalog_data/db_io.py`); `set constraints ... deferred` needs no privilege, and no sequence grant is required — the only identity column in the schema is `load_audit.load_id` (`generated always as identity`, line 535), whose implicit sequence is covered by the table's INSERT privilege.
3. sql-development / best-practices (least privilege): No issues found beyond finding 4.2. No DDL, no `all privileges`, no TRUNCATE anywhere, no UPDATE/DELETE on the `_hstry` mirrors — and the TRUNCATE exclusion is the documented mechanism keeping `--reset-hstry` maintainer-only (`readme/metadata-db-maintenance.md` line 478).
4. sql-development / best-practices (default schema/database consistency): No issues found. `schema` defaults to `catalog` (line 39) and `database` to `metadata_db` (line 43), matching `code/apply_ddl/config/apply_ddl_catalog.toml`, and the header's invocation example (lines 21-23) matches the rebuild runbook verbatim (`readme/metadata-db-maintenance.md` lines 358-360).
5. sql-development / best-practices (parameterization): No issues found. `\if :{?schema}` / `\if :{?database}` supply defaults without overriding `-v` values, and `set search_path = :"schema"` (line 48) is load-bearing here because every table target is unqualified — unlike `grant_ref_ro.sql`, which correctly dropped the same statement once all of its targets became schema-qualified.
6. sql-development / best-practices (terminology currency after the GitHub migration): No issues found. The file contains no CI or forge references at all, so nothing went stale with the GitLab→GitHub move (contrast `grant_ref_ro.sql`, whose "MR pipelines" wording is flagged in `cr_20260812v01_grant_ref_ro.md`), and "venue-residency table" (lines 10-11) still matches the current `deployment_tables` vocabulary in `readme/metadata-db-overview.md` (lines 30, 140).
7. sql-development / best-practices (formatting — lowercase, 4-space indent, ≤100 chars): No issues found. All SQL is lowercase (uppercase only in prose comments); the `\set` bodies and the one-table-per-line grant lists use 4-space indent; an `awk` length check reports no line over 100 characters.
8. sql-development / best-practices (comments — explain the why, above the line): No issues found beyond findings 3.1, 4.1, and 4.3. Every comment sits above the code it describes, none are inline, and the header's claims (privilege model, idempotency caveat, `ddl_versions` created by `apply_ddl.py` rather than by a migration) all hold against the current DDL and loader.
9. sql-development / best-practices (explicit column references, joins, group/order by): N/A - this is a DCL (GRANT) script with no SELECT, joins, or grouping.
10. sql-development / best-practices (prefer union all): N/A - no set operations.
11. sql-development / best-practices (NULL handling / explicit CAST): N/A - no data-manipulation expressions.
12. sql-development / best-practices (CTEs over nested subqueries): N/A - no queries or CTEs.
13. sql-development / best-practices (query/CTE block annotation): N/A - no queries or CTEs to annotate.
14. sql-development / dbt: N/A - not a dbt project artifact.
15. python-development (all core skills): N/A - target is a `.sql` file, not Python.
