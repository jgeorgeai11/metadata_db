---
name: cr_20260730v01_0001_ref_initial
goal: Address code quality issues identified in code/apply_ddl/ddl_ref/0001_ref_initial.sql to align with sql-development best-practices; re-review since cr_20260729v01, after the audit_id identity PK was added to ref_load_audit.
created: 2026-07-30 14:18:31
updated: 2026-07-30 14:35:00
---

## Implementation Plan

1. [completed] Formatting — lines exceed the 100-char limit - `code/apply_ddl/ddl_ref/0001_ref_initial.sql`
   - 1.1. [minor] Line 20: Plain `--` header comment line is 116 chars (and wraps awkwardly, leaving "there is" dangling); unlike the comment-on string literals, this is an ordinary comment that can be rewrapped to ≤100 chars with no readability cost
        - Current: `--     code/load_ref_data/load_ref_data.py via truncate-and-reload. Git/MR review is the change discipline; there is`
        - Expected: rewrap the bullet across lines ≤100 chars, e.g. `--     code/load_ref_data/load_ref_data.py via truncate-and-reload. Git/MR review is the` / `--     change discipline; there is no per-row history — git history replaces _hstry.`
        - Resolution: Implemented as specified — rewrapped the bullet into two lines exactly as in the Expected example (89 and 85 chars); verified the only remaining >100-char lines in the file are the comment-on literals deferred in 1.2 (lines 55, 62, 64, 91).
   - 1.2. [suggestion] Lines 55, 62, 64, 91: The `comment on table`/`comment on column` string literals run to 246/121/125/227 chars, over the 100-char formatting limit
        - Current: `    'Curated code set: claim type codes (CLM_TYPE_CD) from the source data dictionary — ...';`
        - Expected: split each literal across adjacent string literals (Postgres concatenates literals separated by whitespace/newline), e.g. `'first clause '` / `    'second clause'`
        - Resolution: Deferred — these are human-readable documentation strings; breaking them mid-sentence with implicit concatenation harms readability, and the sibling `ddl_catalog/0001_initial_schema.sql` follows the same one-line-per-comment convention, so the current form is the established project pattern (same deferral as cr_20260729v01 task 1).

2. [completed] Documentation consistency — `ref_load_audit` data columns lack the per-column docs `clm_type_cd` has - `code/apply_ddl/ddl_ref/0001_ref_initial.sql`
   - 2.1. [suggestion] Lines 84-87: `clm_type_cd` documents every column with both an inline comment and a `comment on column`, and `ref_load_audit.audit_id` now carries a detailed inline comment, but the remaining columns (table_name, csv_sha256, row_count, loaded_ts) have neither — only the block comment above the table
        - Current: `    table_name text not null,\n    csv_sha256 text not null,\n    row_count integer not null,\n    loaded_ts timestamptz not null default now()`
        - Expected: add inline `--` comments per column and/or `comment on column ref_load_audit.<col>` to match the documentation depth of `clm_type_cd`
        - Resolution: Deferred — the four columns are self-descriptive and the table-level comment (lines 90-91) already explains the ledger's purpose, so the extra per-column docs add little; revisit if any column gains non-obvious semantics (same deferral as cr_20260729v01 task 2).

3. [completed] Schema design — no index supporting the `--check` latest-row-per-table lookup - `code/apply_ddl/ddl_ref/0001_ref_initial.sql`
   - 3.1. [suggestion] Lines 74-88: The `audit_id` identity PK added since cr_20260729v01 gives the freshness query its deterministic tiebreaker (`order by loaded_ts desc, audit_id desc`), but the latest-row-per-`table_name` lookup still has no index on `(table_name, loaded_ts)`, so each `--check` scans the heap
        - Current: `create table ref_load_audit (\n    audit_id bigint generated always as identity primary key,\n    table_name text not null,\n    ...\n);`
        - Expected: optionally add `create index on ref_load_audit (table_name, loaded_ts desc, audit_id desc);` (or a comment noting the intentional heap-scan choice)
        - Resolution: Deferred — the ledger holds one append-only row per table per load, so it stays tiny and a sequential scan is negligible; an index is not warranted at this scale (same deferral as cr_20260729v01 task 3).

## Skills with No Issues

1. sql-development / best-practices — Comments: No issues found; comments sit on the line above (not inline), explain design intent, nullability rationale, and the audit_id PK choice, and stay in sync with the DDL.
2. sql-development / best-practices — Formatting (lowercase, 4-space indent): No issues found beyond the over-length lines in Task 1; all keywords and identifiers are lowercase with consistent 4-space indentation.
3. sql-development / best-practices — Explicit columns / joins / group-order-by / union / NULL & CAST handling: N/A — this is DDL (create table + comments), with no queries, joins, aggregations, or set operations.
4. sql-development / best-practices — CTEs vs subqueries and query block annotation: N/A — no queries in this file.
5. sql-development / dbt: N/A — not a dbt model; this is a raw migration DDL file applied by apply_ddl.py.
