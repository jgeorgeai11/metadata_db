---
name: cr_20260729v01_0001_ref_initial
goal: Address code quality issues identified in code/apply_ddl/ddl_ref/0001_ref_initial.sql to align with sql-development best-practices.
created: 2026-07-29 14:36:22
updated: 2026-07-29 14:36:22
---

## Implementation Plan

1. [completed] Formatting — comment string literals exceed the 100-char line limit - `code/apply_ddl/ddl_ref/0001_ref_initial.sql`
   - 1.1. [suggestion] Lines 53, 60, 62, 80: The `comment on table`/`comment on column` string literals run to 246/121/125/227 chars, over the 100-char formatting limit
        - Current: `    'Curated code set: claim type codes (CLM_TYPE_CD) from the source data dictionary — ...';`
        - Expected: split each literal across adjacent string literals (Postgres concatenates literals separated by whitespace/newline), e.g. `'first clause '\n    'second clause'`
        - Resolution: Deferred — these are human-readable documentation strings; breaking them mid-sentence with implicit concatenation harms readability, and the sibling `ddl_catalog/0001_initial_schema.sql` follows the same one-line-per-comment convention, so the current form is the established project pattern.

2. [completed] Documentation consistency — `ref_load_audit` columns lack the per-column docs `clm_type_cd` has - `code/apply_ddl/ddl_ref/0001_ref_initial.sql`
   - 2.1. [suggestion] Lines 72-80: `clm_type_cd` documents every column with both an inline comment and a `comment on column`, but `ref_load_audit` (table_name, csv_sha256, row_count, loaded_ts) has neither — only the block comment above the table
        - Current: `create table ref_load_audit (\n    table_name text not null,\n    csv_sha256 text not null,\n    row_count integer not null,\n    loaded_ts timestamptz not null default now()\n);`
        - Expected: add inline `--` comments per column and/or `comment on column ref_load_audit.<col>` to match the documentation depth of `clm_type_cd`
        - Resolution: Deferred — the four columns are self-descriptive and the table-level comment (lines 79-80) already explains the ledger's purpose, so the extra per-column docs add little; revisit if any column gains non-obvious semantics.

3. [completed] Schema design — `ref_load_audit` has no key or index for the `--check` latest-row lookup - `code/apply_ddl/ddl_ref/0001_ref_initial.sql`
   - 3.1. [suggestion] Lines 72-77: `--check` reads the latest row per `table_name`, but the table has no primary key and no index on `(table_name, loaded_ts)`, so each lookup scans the table
        - Current: `create table ref_load_audit (\n    table_name text not null,\n    ...\n    loaded_ts timestamptz not null default now()\n);`
        - Expected: optionally add `create index on ref_load_audit (table_name, loaded_ts desc);` (or a comment noting the intentional heap-scan choice)
        - Resolution: Deferred — the ledger holds one append-only row per table per load, so it stays tiny and a sequential scan is negligible; an index is not warranted at this scale.

## Skills with No Issues

1. sql-development / best-practices — Comments: No issues found; comments sit on the line above (not inline), explain the design intent and nullability rationale, and stay in sync with the DDL.
2. sql-development / best-practices — Formatting (lowercase, 4-space indent): No issues found; only the comment string literals exceed 100 chars (see Task 1).
3. sql-development / best-practices — Explicit columns / joins / group-order-by / union / NULL & CAST handling: N/A — this is DDL (create table + comments), with no queries, joins, aggregations, or set operations.
4. sql-development / best-practices — CTEs vs subqueries: N/A — no queries in this file.
5. sql-development / dbt: N/A — not a dbt model; this is a raw migration DDL file applied by apply_ddl.py.
