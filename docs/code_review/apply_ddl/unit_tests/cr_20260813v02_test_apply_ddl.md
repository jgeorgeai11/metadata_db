---
name: "cr_20260813v02_test_apply_ddl"
goal: Re-review of code/apply_ddl/unit_tests/test_apply_ddl.py since cr_20260813v01, reviewed as a group with apply_ddl.py, against the python-development unit-tests, comments, docstrings, type-hints, and exception-handling skills; four new minors (two uncovered lines, an untested config forward, one grant-script test that skips comment-stripping) plus a stale cross-reference and the long-deferred suggestions.
created: 2026-08-13 11:47:00
updated: 2026-08-13 12:20:38
---

## Implementation Plan

1. [completed] Grant-script agreement test reads raw text while its sibling comment-strips - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 1.1. [minor] Lines 1211-1237: `test_grant_script_table_lists_agree_with_ddl` reads `_GRANT_SCRIPT` raw (`grant_text = _GRANT_SCRIPT.read_text(...)`, line 1223), so its grant-list regexes and the two substring assertions at lines 1236-1237 can be satisfied by `--` prose rather than by an executable statement. The adjacent `test_grant_script_ro_table_list_agrees_with_ddl` answers the same question the other way — it routes the read through `_sql_text_without_comments` (line 1254) with an explicit comment saying why ("so `--` prose naming a table cannot satisfy either the grant-list match or the exclusion assertions"). Two tests pinning the same class of invariant should not disagree on whether comments count. `metadata_db_ci.sql` currently contains no prose that would trip either form, so this is fragility, not an active false pass; the header does discuss "INSERT-only ... _hstry tables" and the canonical table set, which is exactly the kind of text that could drift into a matching shape.
        - Current: `grant_text = _GRANT_SCRIPT.read_text(encoding="utf-8")`
        - Expected: `# Comment-stripped for the same reason as the read-only sibling below:\n    # `--` prose must not be able to satisfy a grant-list match.\n    grant_text = _sql_text_without_comments(_GRANT_SCRIPT)`
        - Resolution: Implemented as specified — `test_grant_script_table_lists_agree_with_ddl` now routes its read through `_sql_text_without_comments(_GRANT_SCRIPT)` with the cross-reference comment to the read-only sibling. The two grant-list regexes and both substring assertions (`grant select on ddl_versions`, `grant select, insert on load_audit`) still pass against the comment-stripped text, so the tightening is behavior-preserving on the shipped `metadata_db_ci.sql`.

2. [completed] Uncovered lines in apply_ddl.py - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 2.1. [minor] Lines 852-925 (`run` / `--allow-pending` section): no test exercises the unused-exemption WARNING. `uv run pytest code/apply_ddl/unit_tests/test_apply_ddl.py --cov=apply_ddl --cov-report=term-missing` reports `Missing 250, 587`; line 587 is `logger.warning(f"--allow-pending entries match no pending migration: ...")` (apply_ddl.py lines 586-590). That warning exists precisely because a typo in the CI-computed exemption list is easy to miss and does not fail the check on its own — the one guardrail against it is therefore itself unguarded. Unit-tests guideline 7.2 requires investigating and addressing uncovered lines, and the file already has the `caplog` pattern for exactly this (lines 1070-1097).
        - Current: `test_run_check_allow_pending_exempts_named_file` and `test_run_check_allow_pending_still_fails_other_pending` only (lines 858-894); nothing asserts on the warning
        - Expected: add `def test_run_check_allow_pending_unused_entry_warns(patched_run, caplog)` — set `caplog.set_level(logging.WARNING)`, return `_migrations("0001")` with `applied_migrations` `{"0001": "c1"}`, call `run(..., check_only=True, allow_pending=["0099_typo.sql"])`, then `assert "match no pending migration" in caplog.text` and `assert "0099_typo.sql" in caplog.text`
        - Resolution: Implemented as specified — added `test_run_check_allow_pending_unused_entry_warns(patched_run, caplog)` in the `--allow-pending` section: `caplog.set_level(logging.WARNING)`, one repo migration `0001` already applied, `allow_pending=["0099_typo.sql"]`, then asserts both `"match no pending migration"` and `"0099_typo.sql"` appear in `caplog.text`. apply_ddl.py line 587 is now covered.
   - 2.2. [minor] Lines 842-850: `test_list_repo_migrations_lowercase_sql_unaffected` covers the "unrelated extension is ignored" path with `notes.txt`, but nothing puts a directory in `ddl_dir`, leaving apply_ddl.py line 250 (`continue` under `if not path.is_file()`) uncovered — the second of the two lines reported by `--cov-report=term-missing`. The guard matters because `list_repo_migrations` iterates `ddl_dir.iterdir()` rather than globbing (see its own comment, apply_ddl.py lines 244-247), so directory entries reach the suffix and numeric-prefix checks; a subdirectory named `0002_wip.sql` would otherwise raise or be listed as a migration.
        - Current: `(tmp_path / "0001_first.sql").write_text("select 1;")\n    (tmp_path / "notes.txt").write_text("ignore me")`
        - Expected: `(tmp_path / "0001_first.sql").write_text("select 1;")\n    (tmp_path / "notes.txt").write_text("ignore me")\n    # A directory entry is skipped before the extension/prefix checks —\n    # iterdir() surfaces directories, glob("*.sql") would not.\n    (tmp_path / "0002_wip.sql").mkdir()`
        - Resolution: Implemented as specified — `test_list_repo_migrations_lowercase_sql_unaffected` now also creates a directory named `0002_wip.sql` (with the iterdir-vs-glob comment); the existing assertion that only `0001_first.sql` is returned proves the entry was skipped by the `if not path.is_file()` guard rather than parsed as a migration. apply_ddl.py line 250 is now covered, bringing the module to 100% statement coverage (214 statements, 0 missed).

3. [completed] The `schema_comment` config forward is never asserted - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 3.1. [minor] Lines 565-585 and 494-496: `ensure_schema`'s comment behavior is tested directly (lines 149-172), but nothing asserts that `run` reads `config.get("schema_comment")` (apply_ddl.py line 510) and forwards it as the third argument to `ensure_schema` (line 556). `test_run_apply_mode_creates_schema_before_ddl_versions` asserts only `call_args[0][1] == "catalog"`, and `_config()` never sets the knob — so deleting the forward, or passing `config["schema"]` twice, would leave all 125 tests green while the documented `schema_comment` config knob silently stopped working. Unit-tests guideline 7.1 (cover all paths) applies to the config-to-behavior plumbing, which is the only part of this feature with no test behind it.
        - Current: `assert patched_run["ensure_schema"].call_args[0][1] == "catalog"`
        - Expected: add a dedicated test — `def test_run_apply_mode_forwards_schema_comment(patched_run)` building `{**_config(), "schema_comment": "The metadata_db catalog"}`, calling `run(..., check_only=False, create_db=False)`, then `assert patched_run["ensure_schema"].call_args[0][2] == "The metadata_db catalog"`; pair it with an assertion that the plain `_config()` case forwards `None`
        - Resolution: Implemented with a small deviation in shape — rather than extending `test_run_apply_mode_creates_schema_before_ddl_versions` (which pins call ordering), the two assertions were added as focused sibling tests so each covers one behavior: `test_run_apply_mode_forwards_schema_comment` builds `{**_config(), "schema_comment": "The metadata_db catalog"}` and asserts `ensure_schema.call_args[0][2] == "The metadata_db catalog"`, and `test_run_apply_mode_omitted_schema_comment_forwards_none` asserts the plain `_config()` case forwards `None` (which also rules out the schema name being passed twice). Both fail if the `config.get("schema_comment")` read or the third argument is dropped.

4. [completed] Stale cross-reference to the relocated connection helper - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 4.1. [minor] Line 1184: `test_ddl_0001_is_schema_agnostic`'s intent comment points a reader at "apply_ddl.connection_kwargs" for the search_path mechanism that makes schema-agnostic DDL work, but that function was extracted to `code/lib/pgconn/pgconn.py` and now only exists in `apply_ddl`'s namespace as an import (apply_ddl.py line 35). This file's own comment at lines 97-100 already records the relocation ("connection_kwargs itself is the shared pgconn helper"), so the two comments disagree about where the behavior lives. Comments guideline 3 (keep comments current).
        - Current: `# apply_ddl.connection_kwargs). Checked on comment-stripped text so`
        - Expected: `# pgconn.connection_kwargs, imported by apply_ddl). Checked on`
        - Resolution: Implemented as specified — the comment now reads "pgconn.connection_kwargs, imported by apply_ddl", agreeing with the relocation note at lines 97-100; the following two comment lines were rewrapped to absorb the length change, with no wording change.

5. [completed] Optional test-quality enhancements (carried forward from cr_20260713v01 through cr_20260813v01) - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 5.1. [suggestion] Lines 499-721: the `run()` happy-path tests assert branching (which migrations apply, when create/verify/schema fire) but none asserts that the target connection is closed. `run()` closes it in a `finally` block (apply_ddl.py lines 618-619); if that cleanup were dropped, every `run` test would still pass. The same guarantee is already locked in for `create_database_if_absent` (close asserted at lines 401, 420, 440). Adding one close assertion to a happy-path test (e.g. `test_run_applies_only_pending`, line 613) would restore that symmetry.
        - Current: `apply_ddl.run(_config(), check_only=False, create_db=False)` (no cleanup assertion)
        - Expected: add `patched_run["connect"].return_value.close.assert_called_once()` after the call
        - Resolution: Deferred — optional cleanup-assertion symmetry, not a defect; deliberated and deferred across cr_20260713v01 through cr_20260813v01, and untouched by the changes since. Left for a future dedicated test-cleanup pass, alongside 5.2 and 5.3.
   - 5.2. [suggestion] Lines 126-132: `test_ensure_ddl_versions_pins_applied_ts_column` pins the `applied_ts` column shape by asserting on `inspect.getsource(apply_ddl.ensure_ddl_versions)` — an assertion on source text (internal state; unit-tests guideline 6) rather than on observable behavior, plus a function-level `import inspect` (line 128). The sibling test `test_ensure_ddl_versions_creates_and_commits` (lines 109-123) already asserts on the SQL actually passed to `fake_cursor.execute`; the same pin could live there as one more substring assertion on `create_sql`, retiring the `inspect` import.
        - Current: `source = inspect.getsource(apply_ddl.ensure_ddl_versions)` / `assert "applied_ts timestamptz not null default now()" in source`
        - Expected: in `test_ensure_ddl_versions_creates_and_commits`, add `assert "applied_ts timestamptz not null default now()" in create_sql` and delete the source-inspection test
        - Resolution: Deferred — optional; the test's intent (pinning the docs' `ddl_versions` snippet) is explicitly commented, the pin is equally effective either way, and the pattern has been accepted through several prior review cycles. Fold into a future test-cleanup pass alongside 5.1.
   - 5.3. [suggestion] Lines 818-957: tests remain split across non-contiguous sections — `list_repo_migrations` at lines 34-94 and again at 818-850 ("case-variant .sql extension"), `run` at 443-721 and again at 853-925 ("--allow-pending exemption and permissions-vs-absent split"); additionally `test_main_allow_pending_plumbs_to_run` (lines 928-957) sits under that `run` divider though it exercises `main()`. Consolidating each function's tests under one divider (and moving the `main` plumbing test to the `main` section) would make the file's structural map easier to scan.
        - Current: two `list_repo_migrations` sections and two `run` sections (the latter also hosting one `main` test), appended in the order features landed
        - Expected: move the later blocks up under their original dividers (or merge the divider titles), and relocate `test_main_allow_pending_plumbs_to_run` under the `main` divider
        - Resolution: Deferred — purely organizational; every divider is accurate for the function-under-test beneath it except the one `main` test noted, and append-style growth keeps feature diffs small and reviewable. Not worth a large reshuffle outside a dedicated cleanup pass.
   - 5.4. [suggestion] Lines 220, 341, 358, 383, 390, 405, 417, 424, 431, 469, 474, 499, 614, 619, 652, 739, 863, 1234, 1334: nineteen lines exceed 79 characters (longest 89, line 417), while the module under review holds every line to 79. Grouped-review consistency point: the two files in this group answer the line-width question differently, and the overruns cluster in the parameter lists of the `create_database_if_absent` tests (lines 383-440) and the `patched_run` fixture wiring (lines 469-474), where the dict literals could wrap the way their neighbours already do.
        - Current: e.g. line 341 — `apply_ddl.verify_checksums({"0001": m}, {"0001": apply_ddl.compute_checksum(m)})`
        - Expected: e.g. `apply_ddl.verify_checksums(\n        {"0001": m}, {"0001": apply_ddl.compute_checksum(m)}\n    )`
        - Resolution: Deferred — optional. No python-development core skill sets a line-width limit, the overruns are all in test scaffolding rather than assertions, and a whitespace-only reflow of nineteen scattered lines would churn the file without changing behavior. Fold into the cleanup pass that takes 5.1-5.3.

## Skills with No Issues

1. unit-tests skill: Four minors — see Implementation Plan items 1-3 (the grant-script comment-stripping inconsistency, the two uncovered lines, and the untested `schema_comment` forward) — plus the deferred suggestions in item 5. Otherwise the suite is in good shape: all 125 tests pass in 0.52s at 99% statement coverage of apply_ddl.py (213 statements, 2 missed). Conventions remain strong: pytest (not unittest), `test_<function>_<scenario>_<expected>` naming, shared `fake_conn`/`fake_cursor` fixtures from conftest.py (the cursor is `spec=`'d to the real psycopg2 cursor class), idiomatic `tmp_path`/`monkeypatch`/`caplog`, and `@pytest.mark.parametrize` with ids. All referenced fixture files were verified present on disk (`ddl_catalog/0001_initial_schema.sql`, `ddl_ref/0001_ref_initial.sql`, `grants/metadata_db_ci.sql`, `grants/metadata_db_ci_ro.sql`), as was the `code/lib/pgconn/unit_tests/test_pgconn.py` the lines 97-100 comment defers to. The stdlib `unittest.mock` + `monkeypatch` combination (instead of `mocker.patch()`) remains an accepted project deviation, and mocking `run()`'s own leaf helpers is documented in the module docstring. Grouped-review consistency check against apply_ddl.py: the behaviors asserted (check-mode no-writes, allow-pending exemption, permissions-vs-absent split, checksum immutability, transaction-control guard ordering) all match the current source — no divergence beyond the coverage gaps above.
2. type-hints skill: No issues found. All test functions, fixtures, and helpers carry parameter and return annotations using modern syntax (`list[tuple[str, Path]]`, `dict[str, MagicMock]`, `set[str]`, `-> None`); the nested `_grant_list` helper is annotated (`(grant_clause: str) -> set[str]`, line 1225), and the `*a: object, **k: object` stubs remain appropriately typed for throwaway callables.
3. docstrings skill: No issues found. cr_20260813v01's sole minor is resolved — the module docstring now names both the catalog DDL and the ddl_ref migrations alongside the per-role grant scripts (lines 5-8), matching `_DDL_0001`, `_DDL_REF_0001`, and the two `_GRANT_SCRIPT*` constants it actually reads. The `_migrations`/`patched_run`/`_config`/`_sql_text_without_comments`/`_ddl_text_without_comments`/`_created_tables`/`_table_body` helper docstrings are present and explain intent (`_table_body` includes Args/Returns); individual tests are conventionally self-documenting via their names plus intent comments.
4. comments skill: One minor — see Implementation Plan item 4 (stale `apply_ddl.connection_kwargs` cross-reference). Other intent comments were spot-verified as current: the concept_id shape comment (lines 1371-1376) matches the 3-6-label CHECK it pins and correctly points to the env-gated integration suite; the ref PK-guard comment (lines 1447-1452) accurately describes the two-layer defense; the `fake_conn` `__exit__` note in conftest.py matches the behavior `test_apply_one_rolls_back_on_error` depends on.
5. exception-handling skill: No issues found. Expected errors are asserted with `pytest.raises(..., match=...)`; error-injection stubs raise specific types (`RuntimeError`, `KeyError`, `SystemExit`, `psycopg2.Error`); no bare excepts in test code.
6. logging skill: N/A - test module; `setup_logging` is stubbed and `caplog` is used correctly to assert emitted ERROR/INFO text (including the closing-separator symmetry test at lines 1070-1097). The one logging path not asserted is covered by Implementation Plan item 2.1.
7. executable-scripts skill: N/A - this is a test module, not a CLI entry point (though it fully exercises `main()`'s argparse/config error paths).
8. data-validation skill: N/A - no data-output validation logic in the tests.
9. sql-development skill: N/A - SQL appears only as literal assertion substrings and regex pins against shipped DDL and grant files; no queries are authored in this file.
