---
name: "cr_20260813v01_test_apply_ddl"
goal: Re-review of code/apply_ddl/unit_tests/test_apply_ddl.py since cr_20260803v01, reviewed as a group with apply_ddl.py, against the python-development unit-tests, docstrings, type-hints, comments, and exception-handling skills; one minor docstring-currency issue plus three long-deferred suggestions.
created: 2026-08-13 11:05:07
updated: 2026-08-13 11:05:07
---

## Implementation Plan

1. [completed] Module docstring omits the ref-DDL static tests - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 1.1. [minor] Lines 5-7: the module docstring says the static-DDL-invariant tests "read the shipped 0001_initial_schema.sql and the per-role grant scripts as text", but the suite also reads the ref DDL — `_DDL_REF_0001` (line 22) feeds `test_ddl_ref_0001_ref_load_audit_has_identity_pk` (lines 1466-1475), and `test_ddl_ref_every_created_table_declares_a_primary_key` globs every migration under `ddl_ref/` (lines 1445-1463). Docstrings guideline 4 requires keeping docstrings current; a reader scanning the docstring would not discover that ref-side DDL invariants are pinned here too.
        - Current: `The\nstatic-DDL-invariant tests at the bottom read the shipped\n0001_initial_schema.sql and the per-role grant scripts as text.`
        - Expected: `The\nstatic-DDL-invariant tests at the bottom read the shipped catalog and\nref DDL (0001_initial_schema.sql, the ddl_ref migrations) and the\nper-role grant scripts as text.`
        - Resolution: Implemented as specified — module docstring now names both the catalog DDL (0001_initial_schema.sql) and the ddl_ref migrations alongside the per-role grant scripts. All 125 tests pass (0.44s).

2. [completed] Optional test-quality enhancements (carried forward from cr_20260713v01 through cr_20260803v01) - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 2.1. [suggestion] Lines 498-720: the `run()` happy-path tests assert branching (which migrations apply, when create/verify/schema fire) but none asserts that the target connection is closed. `run()` closes it in a `finally` block (apply_ddl.py lines 618-619); if that cleanup were dropped, every `run` test would still pass. The same guarantee is already locked in for `create_database_if_absent` (close asserted at lines 400, 419, 439). Adding one close assertion to a happy-path test (e.g. `test_run_applies_only_pending`, line 612) would restore that symmetry.
        - Current: `apply_ddl.run(_config(), check_only=False, create_db=False)` (no cleanup assertion)
        - Expected: add `patched_run["connect"].return_value.close.assert_called_once()` after the call
        - Resolution: Deferred — optional cleanup-assertion symmetry, not a defect; deliberated and deferred across cr_20260713v01 through cr_20260803v01, and untouched by the changes since. Left for a future dedicated test-cleanup pass.
   - 2.2. [suggestion] Lines 125-131: `test_ensure_ddl_versions_pins_applied_ts_column` pins the `applied_ts` column shape by asserting on `inspect.getsource(apply_ddl.ensure_ddl_versions)` — an assertion on source text (internal state; unit-tests guideline 6) rather than on observable behavior, plus a function-level `import inspect` (line 128). The sibling test `test_ensure_ddl_versions_creates_and_commits` (lines 108-122) already asserts on the SQL actually passed to `fake_cursor.execute`; the same pin could live there as one more substring assertion on `create_sql`, retiring the `inspect` import.
        - Current: `source = inspect.getsource(apply_ddl.ensure_ddl_versions)` / `assert "applied_ts timestamptz not null default now()" in source`
        - Expected: in `test_ensure_ddl_versions_creates_and_commits`, add `assert "applied_ts timestamptz not null default now()" in create_sql` and delete the source-inspection test
        - Resolution: Deferred — optional; the test's intent (pinning the docs' `ddl_versions` snippet) is explicitly commented, the pin is equally effective either way, and the pattern has been accepted through several prior review cycles. Fold into a future test-cleanup pass alongside 2.1.
   - 2.3. [suggestion] Lines 817-956: tests remain split across non-contiguous sections — `list_repo_migrations` at lines 34-93 and again at 817-849 ("case-variant .sql extension"), `run` at 442-720 and again at 852-924 ("--allow-pending exemption and permissions-vs-absent split"); additionally `test_main_allow_pending_plumbs_to_run` (lines 927-956) sits under that `run` divider though it exercises `main()`. Consolidating each function's tests under one divider (and moving the `main` plumbing test to the `main` section) would make the file's structural map easier to scan.
        - Current: two `list_repo_migrations` sections and two `run` sections (the latter also hosting one `main` test), appended in the order features landed
        - Expected: move the later blocks up under their original dividers (or merge the divider titles), and relocate `test_main_allow_pending_plumbs_to_run` under the `main` divider
        - Resolution: Deferred — purely organizational; every divider is accurate for the function-under-test beneath it except the one `main` test noted, and append-style growth keeps feature diffs small and reviewable. Not worth a large reshuffle outside a dedicated cleanup pass.

## Skills with No Issues

1. unit-tests skill: No new issues; only the deferred suggestions 2.1-2.3 remain. Since cr_20260803v01 the suite slimmed from 138 to 125 tests (retired static pins for since-removed DDL details) and gained the read-only grant-script agreement test `test_grant_script_ro_table_list_agrees_with_ddl` (lines 1239-1264), which comment-strips the script so prose cannot satisfy the exclusion assertions. All referenced fixture files were verified present on disk (`ddl_catalog/0001_initial_schema.sql`, `ddl_ref/0001_ref_initial.sql`, `grants/metadata_db_ci.sql`, `grants/metadata_db_ci_ro.sql`). Conventions remain strong: pytest (not unittest), `test_<function>_<scenario>_<expected>` naming, shared `fake_conn`/`fake_cursor` fixtures from conftest.py, idiomatic `tmp_path`/`monkeypatch`/`caplog`, and `@pytest.mark.parametrize` with ids. The stdlib `unittest.mock` + `monkeypatch` combination (instead of `mocker.patch()`) remains an accepted project deviation, and mocking `run()`'s own leaf helpers is documented in the module docstring (lines 4-5). Grouped-review consistency check against apply_ddl.py: every public function has tests, and the behaviors asserted (check-mode no-writes, allow-pending exemption, permissions-vs-absent split, checksum immutability, transaction-control guard ordering) match the current source — no divergence found. All 125 tests pass (0.45s).
2. type-hints skill: No issues found. All test functions, fixtures, and helpers carry parameter and return annotations using modern syntax (`list[tuple[str, Path]]`, `dict[str, MagicMock]`, `set[str]`, `-> None`); the nested `_grant_list` helper is annotated (`(grant_clause: str) -> set[str]`, line 1224), and the `*a: object, **k: object` stubs remain appropriately typed for throwaway callables.
3. docstrings skill: One minor issue — see Implementation Plan item 1 (module docstring omits the ref-DDL static tests). Otherwise no issues: the `_migrations`/`patched_run`/`_config`/`_sql_text_without_comments`/`_ddl_text_without_comments`/`_created_tables`/`_table_body` helper docstrings are present and explain intent (`_table_body` includes Args/Returns); individual test functions are conventionally self-documenting via their names plus intent comments.
4. comments skill: No issues found. Intent comments were spot-verified as current: the concept_id shape comment (lines 1370-1375) matches the 3-6-label CHECK it pins and correctly points to the env-gated integration suite; the ref PK-guard comment (lines 1446-1451) accurately describes the two-layer defense; the conftest-vs-module `fake_conn` behavior notes match the fixtures actually defined in conftest.py.
5. exception-handling skill: No issues found. Expected errors are asserted with `pytest.raises(..., match=...)`; error-injection stubs raise specific types (`RuntimeError`, `KeyError`, `SystemExit`, `psycopg2.Error`); no bare excepts in test code.
6. logging skill: N/A - test module; `setup_logging` is stubbed and `caplog` is used correctly to assert emitted ERROR/INFO text (including the closing-separator symmetry test at lines 1069-1096).
7. executable-scripts skill: N/A - this is a test module, not a CLI entry point (though it fully exercises `main()`'s argparse/config error paths).
8. data-validation skill: N/A - no data-output validation logic in the tests.
9. sql-development skill: N/A - SQL appears only as literal assertion substrings and regex pins against shipped DDL files; no queries are authored in this file.
