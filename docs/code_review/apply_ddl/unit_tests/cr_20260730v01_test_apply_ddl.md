---
name: cr_20260730v01_test_apply_ddl
goal: Re-review of code/apply_ddl/unit_tests/test_apply_ddl.py since cr_20260729v01, covering the ref-pipeline additions (schema-probe tests, ensure_schema comment tests, ddl_ref static invariants), against the python-development unit-tests, comments, docstrings, type-hints, and exception-handling skills.
created: 2026-07-30 14:20:23
updated: 2026-07-30 14:38:14
---

## Implementation Plan

1. [completed] Keep the section divider an accurate map of the tests beneath it - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 1.1. [minor] Line 817: The divider reads `check_no_transaction_control / strip_sql_comments`, but no test in that block (lines 821-891) — or anywhere in the file — targets `apply_ddl.strip_sql_comments` (a public function, `apply_ddl.py:112`) directly; it is exercised only transitively through the comment-ignoring `check_no_transaction_control` tests (lines 838-853). This violates unit-tests guideline 7 ("Every public function should have tests") and the comments skill's keep-comments-current rule, and is a carry-forward of cr_20260729v01 finding 2.1, which the maintainer deferred to "a future test pass" — the divider and coverage are unchanged since. Either add a direct test (line-comment and block-comment removal, e.g. `test_strip_sql_comments_removes_line_and_block_comments`) so the header is truthful, or narrow the header to `# check_no_transaction_control`.
        - Current: `# check_no_transaction_control / strip_sql_comments`
        - Expected: add `test_strip_sql_comments_removes_line_and_block_comments` under this divider, or narrow the header to `# check_no_transaction_control`
        - Resolution: Implemented via the first option — added `test_strip_sql_comments_removes_line_and_block_comments` under the divider (after the `check_no_transaction_control` tests, matching the header's order, before the `apply_one` guard test). It calls `apply_ddl.strip_sql_comments` directly and asserts exact output equality, pinning that a `--` comment is removed to end of line with the newline kept, a `/* */` comment is removed across lines, and surrounding SQL is untouched. The divider is now truthful and the public function has direct coverage (unit-tests guideline 7). All 138 tests pass (0.44s).

2. [completed] Optional test-quality enhancements - `code/apply_ddl/unit_tests/test_apply_ddl.py`
   - 2.1. [suggestion] Lines 591-813: The `run()` happy-path tests assert branching (which migrations apply, when create/verify/schema fire) but none asserts that the target connection is closed. `run()` closes it in a `finally` block; if that cleanup were dropped, every `run` test would still pass. The same guarantee is already locked in for `create_database_if_absent` (close asserted at lines 493, 512, 532). Adding one close assertion to a happy-path test (e.g. `test_run_applies_only_pending`, line 705) would restore that symmetry.
        - Current: `apply_ddl.run(_config(), check_only=False, create_db=False)` (no cleanup assertion)
        - Expected: add `patched_run["connect"].return_value.close.assert_called_once()` after the call
        - Resolution: Deferred — optional cleanup-assertion symmetry, not a defect; deliberated and deferred across cr_20260713v01, cr_20260717v01, and cr_20260729v01, and untouched by the ref-pipeline work. Left for a future dedicated test-cleanup pass.
   - 2.2. [suggestion] Lines 218-225: `test_ensure_ddl_versions_pins_applied_ts_column` pins the `applied_ts` column shape by asserting on `inspect.getsource(apply_ddl.ensure_ddl_versions)` — an assertion on source text (internal state; unit-tests guideline 6) rather than on observable behavior, plus a function-level `import inspect`. The sibling test `test_ensure_ddl_versions_creates_and_commits` (lines 201-215) already asserts on the SQL actually passed to `fake_cursor.execute`; the same pin could live there as one more substring assertion on `create_sql`, retiring the `inspect` import.
        - Current: `source = inspect.getsource(apply_ddl.ensure_ddl_versions)` / `assert "applied_ts timestamptz not null default now()" in source`
        - Expected: in `test_ensure_ddl_versions_creates_and_commits`, add `assert "applied_ts timestamptz not null default now()" in create_sql` and delete the source-inspection test
        - Resolution: Deferred — optional; the test's intent (pinning the docs' `ddl_versions` snippet) is explicitly commented, the pin is equally effective either way, and the pattern has been accepted through several prior review cycles. Fold into a future test-cleanup pass alongside 2.1.
   - 2.3. [suggestion] Lines 894-931: Tests for `list_repo_migrations` and `run()` are split across non-contiguous sections — `list_repo_migrations` at lines 31-91 and again at 894-926 ("case-variant .sql extension"), `run` at 536-813 and again at 929-1001 ("--allow-pending exemption and permissions-vs-absent split"). Consolidating each function's tests under one divider would make the file's structural map easier to scan as it grows (now 1530 lines).
        - Current: two `list_repo_migrations` sections and two `run` sections, appended in the order features landed
        - Expected: move the later blocks up under their original dividers (or merge the divider titles)
        - Resolution: Deferred — purely organizational; every divider is accurate for the tests beneath it, and append-style growth keeps feature diffs small and reviewable. Not worth a 100+-line reshuffle outside a dedicated cleanup pass.

## Skills with No Issues

1. unit-tests skill: One minor (1.1, carried forward: divider names `strip_sql_comments`, which still has no direct test) plus the deferred suggestions 2.1-2.3. Otherwise strong: pytest (not unittest), correct `test_<function>_<scenario>_<expected>` naming, shared `fake_conn`/`fake_cursor` fixtures from conftest.py, and good use of `tmp_path`/`monkeypatch`/`caplog` and `@pytest.mark.parametrize` with ids (e.g. lines 139-152, 155-180, 821-835, 899-915, 1176-1208). The additions since cr_20260729v01 resolve its major finding 1.1 exactly as specified: direct `schema_present`/`has_schema_usage` tests (lines 287-329) mirror the `ddl_versions_exists` pattern, assert the bound parameter, and assert no commit on read-only probes; the divider at 196-197 was extended to stay accurate. New `ensure_schema` comment-knob tests (241-264) verify the Identifier-injected name and bound comment text against `apply_ddl.py:241-280`; the new static ddl_ref invariants (1490-1519) and the named-deferrable FK pin (1472-1487) assert real shipped-file properties, and the `_sql_text_without_comments` refactor (1241-1249) cleanly generalizes the existing helper. The stdlib `unittest.mock` + `monkeypatch` combination (instead of `mocker.patch()`) remains an accepted project deviation; mocking `run()`'s own leaf helpers is documented in the module docstring (lines 3-5). All 137 tests pass (0.39s).
2. type-hints skill: No issues found. All test functions, fixtures, and helpers carry parameter and return annotations using modern syntax (`list[tuple[str, Path]]`, `dict[str, MagicMock]`, `set[str]`, `-> None`); the new `_sql_text_without_comments(path: Path) -> str` is fully annotated, and the `*a: object, **k: object` stubs remain appropriately typed for throwaway callables.
3. docstrings skill: No issues found. The module docstring and the `_migrations`/`patched_run`/`_config`/`_sql_text_without_comments`/`_ddl_text_without_comments`/`_created_tables`/`_table_body` helper docstrings are present and explain intent (`_table_body` includes Args/Returns); individual test functions are conventionally self-documenting via their names.
4. comments skill: No issues found beyond the divider staleness in 1.1. Inline comments explain the "why" and are accurate against the current source — including the new ones on the schema-probe tests (290, 302, 312, 324-325), the ensure_schema comment-knob tests (244-245, 259-260), the deferrable-FK rationale (1473-1478), and the two-layer ref PK guard rationale (1491-1496).
5. exception-handling skill: No issues found. Expected errors are asserted with `pytest.raises(..., match=...)`; error-injection stubs raise specific types (`RuntimeError`, `KeyError`, `SystemExit`, `psycopg2.Error`); no bare excepts in test code.
6. logging skill: N/A - test module; `setup_logging` is stubbed and `caplog` is used correctly to assert emitted ERROR/INFO text in the `main()` tests (e.g. 1041-1054, 1146-1173, 1211-1233).
7. executable-scripts skill: N/A - this is a test module, not a CLI entry point.
8. data-validation skill: N/A - no data-output validation logic in the tests.
9. sql-development skill: N/A - SQL appears only as literal assertion substrings and regex pins against shipped DDL files; no queries are authored in this file.
