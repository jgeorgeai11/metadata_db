---
name: cr_20260727v01_git_ops
goal: Re-review code/revert_merge/git_ops.py against python-development skills since cr_20260725v01 (file unchanged); confirm continued standards compliance.
created: 2026-07-27 13:59:06
updated: 2026-07-27 13:59:06
---

## Implementation Plan

1. [completed] Clarify the internal `subprocess.run(check=False)` choice - `code/revert_merge/git_ops.py`
   - 1.1. [suggestion] Line 76: `subprocess.run` is always invoked with `check=False` so that `run_git` can log the failure (and redact argv) before deciding whether to raise. This deliberate difference from the wrapper's own `check` parameter is not called out inline, so a reader could mistake it for an oversight.
        - Current: `            check=False,`
        - Expected: `            check=False,  # run_git handles the return-code check below so it can log/redact before raising`
        - Resolution: Deferred — optional. The manual `if result.returncode != 0` block immediately below makes the intent clear in context, and the wrapper's `check` argument is already documented in the docstring; the comment only adds local emphasis.

## Skills with No Issues

1. Type Hints: No issues found — every function carries full parameter and return annotations in modern syntax (`list[str]`, `str | None`, `Path`, `subprocess.CompletedProcess[str]`); no `Any`.
2. Docstrings: No issues found — the module docstring explains the single-subprocess-seam and credential-hygiene rationale; each function has a Google-style docstring with Args / Returns / Raises, including the `KeyError` note on `build_authenticated_url` (lines 170-172).
3. Comments: No issues found — comments explain "why" (the stderr-anonymization note at lines 67-69, the `check`-branch log-level rationale at lines 84-97, the "first token is the commit itself" note at lines 137-138); no redundant "what" comments.
4. Logging: No issues found — uses `logconfig.get_logger`, f-strings throughout, no `print()`, no "Entering/Exiting" noise; ERROR only on the `check=True` raising path, WARNING when the caller opts into `check=False`, and the token is never logged (redacted via `log_args`).
5. Exception Handling: No issues found — no bare `except`; `run_git` inspects the return code explicitly and raises a specific `subprocess.CalledProcessError` with redacted argv. The `raise` is driven by a return-code check rather than an active `except`, so `from e` chaining does not apply.
6. Executable Scripts: N/A — this is a library module (no `main()`, no argparse entry point).
7. Data Validation: N/A — not a `data_val_*` output-validation script.
8. Unit Tests: N/A — not a test module.
