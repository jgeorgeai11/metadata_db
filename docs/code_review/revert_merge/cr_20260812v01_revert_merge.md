---
name: cr_20260812v01_revert_merge
goal: Re-review of code/revert_merge/revert_merge.py against python-development skills after the GitLab-to-GitHub migration (supersedes cr_20260727v01), reviewed alongside its unit tests and TOML config.
created: 2026-08-12 13:53:03
updated: 2026-08-12 14:53:20
---

## Implementation Plan

1. [completed] Refresh post-migration placeholder vocabulary - `code/revert_merge/revert_merge.py`
   - 1.1. [minor] Lines 48, 78, 103: The `<group>` example is GitLab-era vocabulary (a GitLab *group*); the GitHub analogue is an *org*, and the sibling tests already use `<org>` (`unit_tests/test_revert_merge.py` lines 279, 722). Three sites name `<group>` as the canonical unfilled placeholder, so the file now teaches the reader a term that no longer appears anywhere in the repo.
        - Current: `# Angle-bracket placeholders (e.g. \`<group>\`) mark template values the` (line 48); `such as \`<group>\` (a never-customized template) or lacks the` (line 78)
        - Expected: `<org>` in place of `<group>` at all three sites, matching `test_revert_merge.py` and `readme/metadata-db-maintenance.md`'s GitHub org (`Warehouse`)
        - Resolution: Implemented with a partial deviation at the third site — the module comment (line 48) and the `run` docstring's `RuntimeError` entry now read `<org>`. The third site (the step-0 comment) is not a `<group>` → `<org>` substitution because item 1.2 rewrote that sentence away entirely; `<group>` no longer appears in the file. Note for a follow-up outside this file's scope: `readme/metadata-db-maintenance.md` line 491 still cites `<group>` in the refusal contract the module docstring references, so the term does survive elsewhere in the repo.
   - 1.2. [minor] Lines 101-105: The step-0 comment states the shipped config "holds `<group>` until an operator customizes it". That is no longer true — the migration filled `remote_url_template` in with the real `https://x-access-token:{token}@github.example.com/Warehouse/metadata_db.git` (`config/revert_merge.toml` line 19), and the only `<...>` placeholders the shipped config still carries are `bot_name` / `bot_email`. The comments skill treats an outdated comment as worse than none: a reader trusting this would expect the URL check, not the identity check, to be the one that fires on a stock checkout.
        - Current: `# Template sanity: refuse BEFORE any git operation if the URL / # still carries an unfilled \`<...>\` placeholder (the shipped config / # holds \`<group>\` until an operator customizes it).`
        - Expected: `# Template sanity: refuse BEFORE any git operation if the URL still / # carries an unfilled \`<...>\` placeholder (the shipped config's URL is / # filled in, but a re-pointed fork may not be; the bot identity below / # ships unfilled).`
        - Resolution: Implemented as specified, with the existing trailing rationale retained — the comment now reads as the Expected text followed by the unchanged "Without this, git would fail later with a confusing DNS/404 error — or worse, push somewhere unintended.", which is the "why" the comments skill asks for and was never stale.

2. [completed] Harden the credential guard - `code/revert_merge/revert_merge.py`
   - 2.1. [minor] Lines 89-96: The token guard rejects only unset/empty, while the bot-identity guard 40 lines below rejects whitespace-only via `.strip()`. A `CLEANUP_BOT_TOKEN` secret stored with stray whitespace (or a lone newline) therefore passes, gets formatted into the remote URL, and fails mid-incident with an opaque git auth error — precisely the "fail later, with a git auth error mid-incident" outcome the surrounding sanity block exists to prevent. Stripping on read also inoculates against a trailing newline in the secret value.
        - Current: `token = os.environ.get(TOKEN_ENV_VAR, "")`
        - Expected: `token = os.environ.get(TOKEN_ENV_VAR, "").strip()`
        - Resolution: Implemented with three accommodations, since the guard's behavior changed: (a) the adjacent comment now says unset/empty/whitespace-only are all rejected and notes that stripping also drops a trailing newline from an otherwise-valid secret; (b) the `run` docstring's `RuntimeError` entry now reads "missing, empty, or whitespace-only"; (c) `test_run_empty_token_refuses_with_no_git_calls` became `test_run_blank_token_refuses_with_no_git_calls`, parametrized over `""` / `"   "` / `"\n"` (ids `empty` / `whitespace` / `newline`), so the new whitespace and newline cases are covered rather than only the pre-existing empty one.

3. [completed] Tighten `main()` logging and exception structure - `code/revert_merge/revert_merge.py`
   - 3.1. [minor] Lines 230-255: The success logging (`logger.info("SUCCESS")`, the closing separator) sits inside the `try` alongside `run(...)`, and each of the six `except` arms repeats `logger.info("=" * 60)` + `sys.exit(1)`. The exception-handling skill asks that try/except/else/finally each carry their own logging; moving the success lines to `else:` and the separator to `finally:` removes six duplicated lines and closes the (narrow but real) hole where an `OSError` raised while logging SUCCESS — after a completed push — is caught by the `OSError` arm and reported as a failure.
        - Current: `try: run(...); logger.info("SUCCESS"); logger.info("=" * 60)` followed by six arms each ending `logger.info("=" * 60); sys.exit(1)`
        - Expected: `try: run(...)` / `except ...: logger.error(...); sys.exit(1)` (per arm) / `else: logger.info("SUCCESS")` / `finally: logger.info("=" * 60)`
        - Resolution: Implemented as specified — the `try` now holds only `run(...)`, the five arms each end at `sys.exit(1)`, SUCCESS moved to `else`, and the closing separator to `finally` (six duplicated lines removed). Added a short comment above the `try` recording why SUCCESS is in `else` rather than the `try`. The `finally` still runs on the `sys.exit(1)` paths, since `SystemExit` propagates through it — verified by the five `test_main_*` arm tests, which continue to pass. The two pre-`try` exits (config not found, bad TOML) log no closing separator, unchanged from before and not in this finding's scope.
   - 3.2. [minor] Lines 215-221: After the opening separator, `main()` logs nothing about what it is acting on. Every refusal that fires before `run()` reaches git (missing token, unfilled `<...>`, missing `{token}`, blank/unfilled identity) therefore produces a failed-pipeline log with no record of the target SHA or config path — the logging skill's "include context" guidance matters most on an incident-response script whose whole output is a post-mortem artifact. `run()`'s only INFO lands on the success path (line 188), and the `PreconditionError` messages are the only other place a SHA appears.
        - Current: `setup_logging(log_dir="logs/revert_merge")` / `logger.info("=" * 60)`
        - Expected: `logger.info("=" * 60)` / `logger.info(f"Reverting commit {args.commit_sha} using config {args.config}")`
        - Resolution: Implemented as specified, immediately after the opening separator and before the config-existence check, so even the earliest refusal (config not found) records the target. Added a comment noting why the line is there. Neither value can carry the token, so the end-to-end redaction sweep (`test_main_token_never_in_logs_across_happy_path`) still holds.

4. [completed] Deferred enhancements carried forward from cr_20260727v01 - `code/revert_merge/revert_merge.py`
   - 4.1. [suggestion] Lines 205-210: `main()` takes a second CLI argument, `--commit-sha`, alongside `--config`, deviating from the executable-scripts convention of a single `--config` argument. The value is genuine runtime CI data and could instead be read from the `GITHUB_SHA` environment variable (mirroring the `CLEANUP_BOT_TOKEN` pattern already used) to restore the single-argument convention.
        - Current: `parser.add_argument("--commit-sha", type=str, required=True, help=...)`
        - Expected: read `GITHUB_SHA` from the environment, or keep the flag (the module-docstring Usage block and `config/revert_merge.toml` line 4 both document it) as an intentional deviation for testability.
        - Resolution: Deferred — optional; the explicit `--commit-sha` flag keeps `run` testable with an arbitrary SHA without mutating process env, and the module docstring, the TOML header, and `.github/workflows/post_merge.yml`'s `revert_failed_load` step all show the flag carrying `$GITHUB_SHA`. Carried over from cr_20260727v01 item 1.2 (the env var it would read is now `GITHUB_SHA`, not the GitLab-era `CI_COMMIT_SHA`). Promote to `[minor]` if the single-argument convention should be enforced.
   - 4.2. [suggestion] Lines 130-135: The bot-identity guard `if not isinstance(value, str) or not value.strip():` raises with the message "{key} is blank in the config". The `not isinstance(value, str)` branch fires when the TOML value is a non-string (e.g. `bot_name = 123`), where "is blank" is a slightly imprecise description of the cause even though the remediation ("set the cleanup bot's git identity") remains correct.
        - Current: `f"{key} is blank in the config — set the cleanup bot's git identity before running; refusing to touch git."`
        - Expected: `f"{key} must be a non-empty string in the config — set the cleanup bot's git identity before running; refusing to touch git."`
        - Resolution: Deferred — optional; a non-string identity requires a malformed hand-edited TOML, the `isinstance` guard already handles it gracefully (avoiding an `AttributeError` on `.strip()`) and fails fast before any git operation. Carried over from cr_20260727v01 item 1.3. The wording is only imprecise for that unlikely edge, not wrong.

5. [completed] Widen the `KeyError` arm's message to cover the template it also catches - `code/revert_merge/revert_merge.py`
   - 5.1. [minor] Lines 240-241: The `KeyError` arm always logs "Missing required config field: {e}", but it also catches the `KeyError` raised by `build_authenticated_url` when the template names a placeholder other than `{token}` (e.g. `https://{tokn}:{token}@...`, which slips past both sanity checks). That case surfaces as "Missing required config field: 'tokn'", pointing the operator at a config key that does not exist and away from the URL template that does — on the incident path, where the log is the whole post-mortem and the reader is already under time pressure. One string. Promoted from `[suggestion]`; carried over from cr_20260727v01 item 1.1 (originally cr_20260725v01 item 2.1).
        - Current: `logger.error(f"Missing required config field: {e}")`
        - Expected: `logger.error(f"Missing required config field or malformed URL template placeholder: {e}")`
        - Resolution: Implemented as specified (wrapped across two f-string lines for line length), with a comment added above the arm recording that it also catches `build_authenticated_url`'s `KeyError`. `test_main_missing_config_field_exits_nonzero` asserts on the substring "Missing required config field", which the widened message still contains, so it passes unchanged.

## Skills with No Issues

1. Type Hints: No issues found — `run(config: dict[str, Any], commit_sha: str, cwd: Path) -> None` and `main() -> None` carry full parameter and return annotations in modern syntax; `dict[str, Any]` is the right shape for a parsed-TOML config.
2. Docstrings: Issues found (see items 1.1, 4.1) — otherwise sound: the module docstring states the refusal contract with its source reference and a correct post-migration Usage block (`.github/workflows/post_merge.yml`, `$GITHUB_SHA`), and `run`'s Args/Raises enumerate every config key and every raised type including the bot-identity `RuntimeError` cases.
3. Comments: Issues found (see items 1.1, 1.2) — the remaining comments explain "why" correctly and track the implementation (deferred log setup so `--help` creates no log dir; `checkout -B` for reused runner checkouts; per-command `-c user.name/email` mirroring the token hygiene; token never logged).
4. Logging: Issues found (see items 3.1, 3.2) — otherwise conformant: `logconfig.get_logger`/`setup_logging`, f-strings throughout, `"=" * 60` separators on every exit path, `log_dir="logs/revert_merge"` mirroring the script location, no `print()`, no Entering/Exiting noise, and the token deliberately never logged.
5. Exception Handling: Issues found (see items 3.1, 4.2, 5.1) — otherwise sound: no bare `except`, six specific arms with distinct messages, `PreconditionError` given its own arm first, no generic `Exception` wrapping, and every refusal `raise` placed before any destructive git call.
6. Executable Scripts: Issues found (see item 4.1, deferred) — otherwise conformant: `main()` under an `if __name__ == "__main__"` guard, config at `code/revert_merge/config/revert_merge.toml`, logging deferred until after argparse, config-existence check before `tomllib.load`.
7. Data Validation: N/A - this is a CI-side cleanup script, not a `data_val_*` output-validation script.
8. Unit Tests: N/A for this file - the suite is reviewed separately in `docs/code_review/revert_merge/unit_tests/cr_20260812v01_test_revert_merge.md`; `revert_merge.py` is at 100% statement coverage under `pytest --cov`.
