---
name: cr_20260812v01_test_revert_merge
goal: Re-review of code/revert_merge/unit_tests/test_revert_merge.py against python-development skills after the GitLab-to-GitHub migration and the bot-identity work (supersedes cr_20260713v01), reviewed alongside revert_merge.py and its TOML config.
created: 2026-08-12 13:53:03
updated: 2026-08-12 14:53:20
---

## Implementation Plan

1. [completed] Build refusal-path configs from the shared helpers - `code/revert_merge/unit_tests/test_revert_merge.py`
   - 1.1. [minor] Lines 277-282, 298-303, 720-724: Three tests hand-build a config with only `remote_url_template` and `main_branch`, bypassing the `_config()` / `_write_config()` helpers that every other test uses and omitting the now-required `bot_name` / `bot_email`. They pass only because `run()`'s step-0 block happens to check the URL before reading the identity keys (`revert_merge.py` lines 106-129). Reorder that block — a plausible refactor, since all four checks are peers — and these three tests stop testing what they claim: they would raise `KeyError` on the missing identity keys rather than the `RuntimeError` under test, and `pytest.raises(RuntimeError, ...)` would fail for the wrong reason. Deriving from the helper keeps each test's variable the one thing it overrides.
        - Current: `config = {"remote_url_template": ("https://x-access-token:{token}@github.example.com/<org>/metadata_db.git"), "main_branch": "main"}`
        - Expected: `config = _config()` / `config["remote_url_template"] = ("https://x-access-token:{token}@github.example.com/<org>/metadata_db.git")` — and likewise for line 298's missing-`{token}` case and line 720's `_write_config`-based file
        - Resolution: Implemented as specified for the two `run`-level tests (`test_run_unfilled_placeholder_refuses_without_touching_git`, `test_run_template_missing_token_refuses_without_touching_git`), each now `_config()` plus a single `remote_url_template` override. The third site needed an accommodation: `test_main_unfilled_placeholder_exits_nonzero_without_git` goes through a TOML *file*, so `_write_config` gained an optional `remote_url_template` parameter (defaulting to the value it already wrote) and that test now calls `_write_config(tmp_path, remote_url_template="https://x-access-token:{token}@x/<org>/y.git")` — the file it writes now carries `bot_name` / `bot_email` too. Also added the docstring `_write_config` was missing, stating why it carries every key. All three tests still raise from the check they name; the suite passes at 33 tests.

2. [completed] Deferred test-suite enhancements - `code/revert_merge/unit_tests/test_revert_merge.py`
   - 2.1. [suggestion] Lines 141, 189, 209, 227, 244, 259, 272, 292, 320, 339, 363, 378, 414, 434, 469, 491, 515, 543, 578, 610, 642, 676, 708: The docstrings skill states all public functions need docstrings; only `test_main_token_never_in_logs_across_happy_path` (line 752) has one. The test names are descriptive and several tests carry an explanatory leading comment instead, so this is optional, but a one-line docstring per test would document intent consistently with the fixtures and helpers, which are all documented.
        - Resolution: Deferred — optional; the names follow `test_<function>_<scenario>_<expected>` and read as sentences, and the non-obvious cases already carry a leading "why" comment (e.g. lines 192-195, 273-276, 419-421), so the intent is not actually undocumented. Carried over from cr_20260713v01 item 3.1.
   - 2.2. [suggestion] Lines 543-705: The five `test_main_*` tests exercising `main()`'s exception arms share near-identical scaffolding (stub `setup_logging`, write config, patch `sys.argv`, patch `revert_merge.run` to raise, assert `exc.value.code == 1`, assert a substring in `caplog.text`). Per the unit-tests skill's parametrize guidance they could collapse into one `@pytest.mark.parametrize`-driven test over `(raised_exception, expected_log_substring)`. The config-not-found, bad-TOML, and unfilled-placeholder cases (lines 491, 515, 708) differ enough to keep separate.
        - Resolution: Deferred — optional DRY refactor with no coverage change; the explicit per-arm tests name the exception type in the test name, which keeps a failing arm identifiable from the pytest summary line alone. Carried over from cr_20260713v01 item 4.1.
   - 2.3. [suggestion] Lines 217-223: The comment states "fetch + checkout ran (those happen before the precondition check)" but the assertions only verify that `revert` and push did *not* run, so the claim about what did run is unverified. Asserting the exact `run_git_calls` list would pin both halves of the stated behavior.
        - Current: `assert not any("revert" in args for args in patched_git["run_git_calls"])`
        - Expected: `assert patched_git["run_git_calls"] == [["checkout", "-B", "main", "origin/main"]]`
        - Resolution: Deferred — optional; the happy-path test (lines 165-168) already pins the full `fetch` → `checkout` → `revert` ordering, and this test's job is the refusal half, which it asserts directly. The stricter equality would also couple this refusal test to any future pre-precondition git command.
   - 2.4. [suggestion] Lines 179-186: The happy path proves the token never reaches the logs and that no `set-url`/`config` command ran, but never asserts that the token stays out of the `run_git` argv itself. A direct sweep would state the invariant the design rests on — credentials travel only through `fetch_branch` / `push_branch`, never on a general git command line.
        - Current: `assert not any("set-url" in args or "config" in args for args in patched_git["run_git_calls"])`
        - Expected: additionally `assert not any(SECRET_TOKEN in a for args in patched_git["run_git_calls"] for a in args)`
        - Resolution: Deferred — optional belt-and-braces; the existing assertions already pin the exact `run_git_calls` list (lines 165-168), so a token-bearing argv could not appear without that equality assertion failing first.
   - 2.5. [suggestion] Lines 469-488: `test_main_happy_path_does_not_exit` stubs `run` with `lambda *a, **k: None` and asserts only that no `SystemExit` is raised, so it does not check that `main()` forwards `--commit-sha` and `Path.cwd()` into `run`. Capturing the call would make the CLI-to-`run` wiring explicit.
        - Current: `monkeypatch.setattr(revert_merge, "run", lambda *a, **k: None)`
        - Expected: a recording stub plus `assert recorded["commit_sha"] == "abc"`
        - Resolution: Deferred — optional; the wiring is already covered indirectly by `test_main_token_never_in_logs_across_happy_path` (line 752), which drives `main()` end-to-end with `--commit-sha deadbeef` against a `head` of `deadbeef` — a mis-forwarded SHA would trip `verify_head_is` and fail that test.
   - 2.6. [suggestion] Line 374: The section header `# run — cleanup-bot git identity (Task 19)` carries a bare activity task number. "Task 19" is unresolvable without the originating plan; elsewhere in the repo the traceable form names the activity too (e.g. `code/bootstrap/.../test_generate_corpus_from_infoschema.py` line 347: "activity 20260728v01, note 10").
        - Current: `# run — cleanup-bot git identity (Task 19)`
        - Expected: `# run — cleanup-bot git identity`
        - Resolution: Deferred — optional; bare `Task N` references are an established convention across this repo's test and script comments, so removing this one in isolation would be inconsistent rather than clarifying. Address repo-wide if the traceable form is adopted as the standard.
   - 2.7. [suggestion] Lines 279, 722: These tests use `<org>` as the sample unfilled placeholder, which is the correct post-migration (GitHub) vocabulary, while `revert_merge.py` still says `<group>` (GitLab-era) at lines 48, 78, and 103. The two files disagree on the same concept.
        - Resolution: Deferred — no change belongs in this file; `<org>` here is the correct form, and the fix is tracked against the source at `docs/code_review/revert_merge/cr_20260812v01_revert_merge.md` item 1.1 (`revert_merge.py`). Recorded here only for cross-file traceability.

3. [completed] Guard the shipped config artifact - `code/revert_merge/unit_tests/test_revert_merge.py`
   - 3.1. [minor] Lines 454-462: No test loads the shipped `code/revert_merge/config/revert_merge.toml`; every test synthesizes its own config, so a typo'd key or a `{token}`-less URL committed to the real artifact would go undetected until an incident — when the revert job is the last line of defense and no earlier signal exists. The GitLab→GitHub migration rewrote `remote_url_template` (line 19 of the config) in exactly this blind spot. Add one test that `tomllib.load`s the shipped file and asserts the four keys `run()` reads (`remote_url_template`, `main_branch`, `bot_name`, `bot_email`) are present and that `remote_url_template` contains `{token}`.
        - Current: every test builds its config via `_config()` / `_write_config()`; the shipped TOML is never opened.
        - Expected: a `test_shipped_config_has_required_keys_and_token_placeholder` that resolves the path from `__file__` (`parents[1] / "config" / "revert_merge.toml"`), `tomllib.load`s it, asserts the four keys are present and non-blank, and asserts `"{token}" in config["remote_url_template"]`
        - Resolution: Implemented as specified, added under a new `# The shipped config artifact` section with `import tomllib` at the top of the module. The non-blank assertion also checks `isinstance(..., str)`, mirroring `run`'s own identity guard so a non-string TOML value fails here rather than at `.strip()`. Deliberately does *not* assert the identity keys are filled in — the shipped `bot_name` / `bot_email` are `<...>` placeholders an operator fills per deployment, and the runtime step-0 block already refuses them; the test's docstring records that reasoning. Passes against the current artifact.

## Skills with No Issues

1. Type Hints: No issues found — every fixture, helper, and test is annotated in modern syntax (`dict[str, Any]`, `list[str]`, `subprocess.CompletedProcess[str] | MagicMock`); the `-> Any` stub flagged in cr_20260713v01 item 2.1 is resolved.
2. Docstrings: Issues found (see item 2.1, deferred) — the module docstring, both fixtures, and both private helpers are documented, including the `patched_git` fixture's full key-by-key contract.
3. Comments: Issues found (see items 2.3, 2.6, deferred) — otherwise the comments explain "why" (per-command credential injection, the `checkout -B` stale-local-`main` rationale, why `revert` is matched anywhere in argv rather than at index 0) and track the current implementation; the counting error flagged in cr_20260713v01 item 1.1 is resolved.
4. Logging: No issues found — `caplog` is used correctly to assert on log content, and the token-leak invariant is asserted on both the `run`-level (line 186) and end-to-end `main` (line 777) happy paths.
5. Exception Handling: No issues found — `pytest.raises(..., match=...)` is used throughout with patterns specific enough to distinguish the arms (`match="CLEANUP_BOT_TOKEN"`, `match="unfilled placeholder"`, `match=r"lacks the \`{token}\` placeholder"`, `match="bot_"`).
6. Unit Tests: Issues found (see items 1.1, 2.2, 2.4, 2.5, 3.1) — otherwise strong: pytest (not unittest), `test_<function>_<scenario>_<expected>` naming, shared fixtures with `conftest.py` handling the `sys.path` seam, mocking confined to the external boundaries (`run_git`, `fetch_branch`, `push_branch`, `head_sha`, `parent_shas`) and patched where used rather than where defined, `@pytest.mark.parametrize` applied with `id=`s (lines 226, 401, 433), and every refusal, subprocess-failure, and config-error path covered — `pytest --cov=revert_merge` reports 100% statement coverage with no missing lines.
7. Data Validation: N/A - test module, no data outputs to validate.
8. Executable Scripts: N/A - test module, not a CLI entry point.
