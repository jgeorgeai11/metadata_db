---
name: cr_20260812v01_db_io
goal: Re-review of code/load_catalog_data/db_io.py against python-development and sql-development after commit 706f367 (the GitLab-to-GitHub migration renamed the CI SHA env var to $GITHUB_SHA); one new stale-docstring minor plus the standing style suggestions carried from cr_20260730v01.
created: 2026-08-12 13:51:19
updated: 2026-08-12 14:51:40
---

## Implementation Plan

1. [completed] Stale table count in `_update_params` docstring - `code/load_catalog_data/db_io.py`
   - 1.1. [minor] Lines 789-790: the docstring says `old` "is ignored for the other five tables", but the module now covers 9 main tables (`_FK_ORDER`, lines 957-967) of which exactly two carry `validated`/`validated_ts` (`table_relationships`, `column_mappings`) — leaving seven, not five. The count is left over from the pre-`deployment_tables`/pre-`concepts` model and misstates the helper's contract (docstrings guideline 4: keep docstrings current).
        - Current: `transition for the two validated-bearing tables); it is ignored for` / `the other five tables.`
        - Expected: `transition for the two validated-bearing tables); it is ignored for` / `the other seven tables.`
        - Resolution: Implemented as specified — the `_update_params` docstring now reads "the other seven tables". Re-verified the count before editing: `_FK_ORDER` (lines 957-967) lists 9 tables and only `table_relationships` and `column_mappings` carry `validated`/`validated_ts`, leaving seven for which `old` is ignored.

2. [completed] CI SHA env-var name is an inline literal - `code/load_catalog_data/db_io.py`
   - 2.1. [suggestion] Lines 211, 225, 234, 242: `"GITHUB_SHA"` is read as a bare literal at line 211 and restated as prose in three `RuntimeError` messages, while the sibling `POSTGRES_*` env names are single-sourced in the `ENV_VARS` module constant (line 52). The 706f367 migration had to touch all four sites (plus ~12 in `test_db_io.py`), and the activity file records that a missed rename fails silently — the `git rev-parse HEAD` fallback would just record slightly-wrong provenance. A `_CI_SHA_ENV_VAR = "GITHUB_SHA"` constant interpolated into the lookup and the messages would make the platform seam a one-line change.
        - Current: `ci_sha = os.environ.get("GITHUB_SHA", "").strip()` plus three hardcoded `GITHUB_SHA` mentions in the error strings
        - Expected: `_CI_SHA_ENV_VAR = "GITHUB_SHA"` beside `ENV_VARS`, then `os.environ.get(_CI_SHA_ENV_VAR, "")` and `f"... {_CI_SHA_ENV_VAR} is unset/blank ..."`
        - Resolution: Deferred — optional DRY tidy-up on a seam that has now been migrated and is pinned by `test_resolve_commit_sha_prefers_ci_env`; the four sites sit within one 60-line function where drift is immediately visible, and the error strings read more naturally as literal prose than as interpolations.

3. [completed] Columns bound-parameter order described in two places - `code/load_catalog_data/db_io.py`
   - 3.1. [suggestion] Lines 272-274 and 694-696: this file's descriptions of the `columns` parameter order (the `_SELECT_COLUMNS` "ref_table_id sits LAST" note and the `_insert_params` "Trailing ref_table_id matches _INSERT_COLUMNS' column list" note) are both accurate, but `unit_tests/test_db_io.py` lines 904-911 restates the same INSERT/UPDATE orders with `ref_table_id` omitted — the two files now answer the same question differently. No change is required in this file; the corrective edit belongs to the test module.
        - Current: `# Column order matches ColumnRow field order (rows built positionally): ... ref_table_id sits LAST` (accurate here)
        - Expected: unchanged here; see `cr_20260812v01_test_db_io.md` finding 1.1, which corrects the mirrored comment in `test_db_io.py`
        - Resolution: Deferred — recorded only for cross-file traceability; `db_io.py` is the accurate side of the pair, so editing it would introduce the drift rather than remove it.

4. [completed] SQL keyword casing - `code/load_catalog_data/db_io.py`
   - 4.1. [suggestion] Lines 257-307, 429-481, 483-538, 541-562, 565-637, plus 104-110 and 1045: SQL keywords are uppercase across every `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_DELETE_*`/`_HSTRY_INSERT_*` constant, both `_SET_CONSTRAINTS_DEFER_*` statements, `_INSERT_LOAD_AUDIT`, and the runtime `TRUNCATE TABLE {}` template; sql-development best-practices guideline 7 says "Lowercase everything". Applied consistently across the whole module, so this is a single file-wide style deviation rather than many separate defects. Representative line shown.
        - Current: `"SELECT system, description, notes, update_reason FROM systems"`
        - Expected: `"select system, description, notes, update_reason from systems"`
        - Resolution: Deferred — file-wide, style-only casing deviation applied consistently; these are Python string literals holding SQL, so keyword casing carries no semantic weight and reformatting touches every statement for cosmetic gain only. Matches the standing decision in cr_20260724v03 through cr_20260730v01 (each finding 1.1).

5. [completed] Private-helper docstrings omit `Raises` - `code/load_catalog_data/db_io.py`
   - 5.1. [suggestion] Lines 653, 785, 889: `_insert_params`, `_update_params`, and `_pk_params` each `raise ValueError` on an unknown table (lines 757, 886, 904) but their docstrings carry no `Raises:` section. The docstrings skill requires sections on public functions; these are private (`_`-prefixed), so a `Raises:` note is an optional consistency improvement.
        - Resolution: Deferred — helpers are private (`_`-prefixed) and the `ValueError` branch is an unreachable-by-construction guard (callers draw every table name from `_FK_ORDER`); documenting it is optional, not a required fix. Matches cr_20260730v01 finding 2.1.

6. [completed] Numbered local variables in `read_db_state` - `code/load_catalog_data/db_io.py`
   - 6.1. [suggestion] Lines 323-354: the per-table fetch loops assign the constructed row into distinct numbered names (`r`, `r2`, `r3`, `r4`, `r5`, `rdep`, `r6`), and the `column_mappings`/`concepts` loops continue with `r7`/`r8` (lines 370, 390), even though each name is used only within its own loop. A single reused local per loop would read more clearly and avoid implying the values coexist; the mix of `rdep` and numbered names shows the numbering is not load-bearing.
        - Current: `r = SystemRow(*row)` / `r2 = DataSourceRow(*row)` / ... / `rdep = DeploymentRow(*row)` / `r6 = TableRelationshipRow(*row)`
        - Expected: reuse one local name per loop, e.g. `row_obj = SystemRow(*row)` then `state.systems[pk(row_obj, "systems")] = row_obj`
        - Resolution: Deferred — readability-only naming preference; each name is loop-local, so the numbering causes no shadowing or correctness risk. Matches cr_20260730v01 finding 3.1.

7. [completed] Repo-root path expression duplicated - `code/load_catalog_data/db_io.py`
   - 7.1. [suggestion] Lines 31 and 69: the vendored-logconfig `sys.path.insert` computes `Path(__file__).resolve().parents[2]` inline, and `_REPO_ROOT` (line 69) is defined later with the identical expression. Defining `_REPO_ROOT` once before the import block and reusing it (`sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`) would remove the duplication and guarantee the two "repo root" notions can never drift.
        - Current: `sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "code" / "lib"))` ... `_REPO_ROOT = Path(__file__).resolve().parents[2]`
        - Expected: `_REPO_ROOT = Path(__file__).resolve().parents[2]` defined once (before the vendored import), then `sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`
        - Resolution: Deferred — optional DRY tidy-up of a trivial two-token expression; hoisting `_REPO_ROOT` above the import block would split it from the other module constants (lines 52-110), and each site currently carries its own accurate explanatory comment, so the duplication poses no realistic drift risk. Matches cr_20260730v01 finding 4.1.

## Skills with No Issues

1. Type Hints: No issues. Every function annotates parameters and return types with modern syntax (`dict[str, str]`, `tuple[Any, ...]`, `tuple[bool, Any]`, `tuple[str, ...]`, `dict[str, list[RowChange]]`). The `row: Any` / `old: Any` / `key: Any` on the polymorphic param builders (`_insert_params`, `_update_params`, `_pk_params`) remains the established deliberate choice for functions accepting any of the 9 row dataclasses (accepted in cr_20260729v01 and cr_20260730v01). The 706f367 change touched no signature, so no annotation went stale.
2. Docstrings: Issues found — see task 1.1 (stale "other five tables" count in `_update_params`). Otherwise no issues: `connection_kwargs`, `resolve_commit_sha`, `read_db_state`, and `apply_diff` all carry Google-style docstrings with Args/Returns/Raises as applicable and document the "why". `resolve_commit_sha`'s docstring was correctly updated with the rename — the summary now names `$GITHUB_SHA` (line 191) and adds the accurate provenance rationale that on the post-merge `push` event it is the just-landed merge commit, and the `Raises:` section (line 207) names the same variable, matching the lookup at line 211 and `.github/workflows/post_merge.yml` line 196.
3. Comments: No issues in this file. Re-verified against the current code: the vendored-path comment (lines 28-30) matches `code/lib/logconfig`; the two deferred-constraint blocks (lines 77-97) and their in-transaction counterparts (lines 1025-1038) correctly describe the address-swap and updates-before-inserts rationales; the module docstring's ltree[]/text[] cast rationale (lines 11-14) matches the read casts (lines 291-307) and write casts (lines 467-481); the composite-PK WHERE warning (lines 504-508), the validated_ts CASE semantics (lines 515-518, 719-720, 735, 763-774), the transactional-TRUNCATE note (lines 1040-1042), and the `_FK_ORDER` comment (lines 953-956) are all accurate. The one cross-file drift against `test_db_io.py`'s mirrored comment is task 3.1.
4. Logging: No issues. Uses the vendored `logconfig.get_logger`, never `print()`; all messages use f-strings (guideline 6) and carry context (per-table row counts across all 9 tables, commit SHA, diff summary, reset_hstry marker, git-timeout details including `_REPO_ROOT`). No Entering/Exiting messages (funcName is auto-logged). The dirty-tree WARNING (lines 182-185) sits at the right level — a lineage caveat, not a failure.
5. Exception Handling: No issues. `apply_diff` catches `except Exception as e:`, logs the root cause first, then rolls back and bare-`raise`s (lines 1098-1104) so a dead-connection `rollback()` failure cannot mask the original error. `connection_kwargs` raises a contextful `RuntimeError` listing the missing env vars and a `ValueError` for a bad `schema`. `resolve_commit_sha` catches the specific `FileNotFoundError` and `subprocess.TimeoutExpired`, chains both with `raise ... from e`, and separately handles a nonzero `git` return code with stderr in the message. `_warn_if_dirty_working_tree` deliberately swallows `FileNotFoundError`/`TimeoutExpired` because the check is diagnostic, not a gate (documented in its docstring).
6. Executable Scripts: N/A — imported library module, no `main()` / `__main__` entry point.
7. Data Validation: N/A — DB I/O module, not a `data_val_` script; corpus validation lives elsewhere.
8. Unit Tests: N/A for this file — tests live under `unit_tests/test_db_io.py` (reviewed separately in `cr_20260812v01_test_db_io.md`); 86 tests pass against the current module.
9. SQL (correctness, sql-development): No issues beyond casing (finding 4.1). No SQL changed in 706f367; `%s` count <-> param-tuple length <-> column-list width re-verified for all 9 tables against `data_model.py`:
   - INSERT `%s` <-> tuple: systems 4<->4, data_sources 5<->5, schemas 6<->6, tables 6<->6, columns 10<->10 (trailing `ref_table_id` after the bind-NULL `update_reason`), deployment_tables 6<->6, table_relationships 10<->10 (incl. trailing `validated` CASE guard), column_mappings 9<->9 (incl. `%s::ltree[]` and trailing CASE guard), concepts 6<->6 (incl. `%s::ltree[]`).
   - UPDATE `%s` <-> tuple: systems 4<->4, data_sources 5<->5, schemas 6<->6, tables 6<->6, columns 10<->10 (9 SET incl. ref_table_id + 1 PK), deployment_tables 6<->6 (4 SET + 2 PK), table_relationships 11<->11 and column_mappings 10<->10 (both carry the stamp_now/else_value CASE pair plus full composite-PK WHERE), concepts 6<->6. PK columns are excluded from every SET.
   - DELETE / HSTRY-INSERT: single-PK tables bind a 1-tuple; deployment_tables and column_mappings name both PK parts and bind 2-tuples; table_relationships names all 3 PK parts and binds a 3-tuple. Each `*_hstry` INSERT's column list matches its SELECT list plus `end_ts = now()`.
   - `read_db_state`: each SELECT's column order matches its dataclass field order for positional construction — re-verified against `data_model.py` (`ColumnRow`'s trailing defaulted `ref_table_id` at line 282; the 6-field `DeploymentRow` at lines 315-320); `column_mappings` and `concepts` build via explicit kwargs. All SELECTs list explicit columns (no `SELECT *`); every statement is `%s`-parameterized with no value interpolation; the sole runtime-built statement (`TRUNCATE TABLE {}`) uses `sql.Identifier` over the fixed `_HSTRY_TABLES` tuple; the two `SET CONSTRAINTS ... DEFERRED` statements interpolate only fixed module constants; `_INSERT_LOAD_AUDIT` binds 5 params to 5 columns.
