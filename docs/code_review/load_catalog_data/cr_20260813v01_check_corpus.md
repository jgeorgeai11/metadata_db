---
name: cr_20260813v01_check_corpus
goal: Address code quality issues identified in code/load_catalog_data/check_corpus.py to align with python-development skills; first review of this new offline checker, done as a group with load_catalog_data.py and test_load_catalog_data.py.
created: 2026-08-13 11:09:26
updated: 2026-08-13 12:05:00
---

## Implementation Plan

1. [completed] Add unit-test coverage for the offline checker - `code/load_catalog_data/check_corpus.py`
   - 1.1. [major] Whole file: no `test_check_corpus.py` exists under `code/load_catalog_data/unit_tests/` (or anywhere — the only repo references are CONTRIBUTING.md and an activity plan), yet CONTRIBUTING.md (lines 48-52, 85) makes this script the contributor-facing pre-push gate and, while CI is switched off, the only automated check a change gets. Its exit-code contract and error arms are exactly the kind of `main()`-level behavior its sibling loader already pins in `test_load_catalog_data.py` (unit-tests guideline 7: every public function should have tests; 7.1: happy paths, edge cases, error conditions).
        - Current: no test file
        - Expected: `code/load_catalog_data/unit_tests/test_check_corpus.py` covering at least: clean corpus → exit 0 with the "Corpus OK" summary; assembly failure → exit 1 logging "Corpus assembly failed"; validation failure → exit 1 logging "Corpus validation failed"; missing `--data-root` → exit 1 logging "Corpus root not found"; and the stderr mirroring (`_mirror_log_to_stderr` attaches a root-logger stream handler). The staged-corpus helpers in `test_load_catalog_data.py` are reusable as-is. Also add `# pragma: no cover` to the `if __name__ == "__main__":` guard (line 118) to match the loader once the file enters the coverage run.
        - Resolution: Implemented with adaptations for finding 4.1 — added `test_check_corpus.py` (10 tests, `check_corpus.py` at 100% branch-free line coverage) reusing `_stage_corpus` from `test_load_catalog_data.py`, plus the `# pragma: no cover` guard. Covers: clean corpus returns with the "Corpus OK" summary (counts pinned) and mirrors it to stderr (stdout stays empty); assembly failure → exit 1 with "Corpus assembly failed" and the per-issue record; validation failure → exit 1 with a single-line "Corpus validation failed" summary record plus one record per issue (pinning finding 2.2); `_mirror_log_to_stderr` attaches exactly one INFO-level plain-`%(message)s` stream handler to the root logger. Because 4.1 replaced `--data-root` with `--config`, the "missing `--data-root`" case became four config-sourced cases: missing corpus root → "Corpus root not found", missing config file, unparsable TOML, and missing `data_root` field → exit 1 each; a zero-argument test (cwd'd to an empty tmp dir) proves the default config resolves script-relatively and the corpus root comes from the loader's TOML. An autouse fixture detaches the root-logger handlers each `main()` run adds so tests stay independent.

2. [completed] Align the ValidationError arm with the loader - `code/load_catalog_data/check_corpus.py`
   - 2.1. [minor] Line 99: `except (AssemblyError, ValidationError)` is a redundant tuple — `AssemblyError` subclasses `ValidationError` (`corpus_assembly.py` line 90, which states the subclassing exists precisely so a single `except ValidationError` arm catches both stages), and the sibling loader (`load_catalog_data.py` line 380) catches only `ValidationError` with a comment noting the subclass; two conventions for one concern (grouped-review consistency check; exception-handling best practice).
        - Current: `except (AssemblyError, ValidationError) as e:`
        - Expected: `except ValidationError as e:  # AssemblyError is its assembly-stage subclass` (and drop the now-unused `AssemblyError` import at line 34)
        - Resolution: Implemented as specified — single `except ValidationError as e:` arm with a comment noting AssemblyError is its assembly-stage subclass, and the `AssemblyError` import dropped (the import line now reads `from corpus_assembly import assemble_corpus`).
   - 2.2. [minor] Line 100: the arm logs `logger.error(str(e))` — one ERROR record whose message embeds newlines (`ValidationError.__str__` is the summary plus a newline-joined issue list, `corpus_validation.py` lines 84-88) — while the loader logs `e.summary` then one record per issue (`load_catalog_data.py` lines 386-388). For the terminal reader the text is identical, but for `logs/check_corpus/check_corpus.jsonl` — the machine audience this script's own docstring names (line 20) — a single multi-line record is strictly worse than per-issue records; two conventions for reporting the same exception (grouped-review consistency check, cross-referenced as finding 2.1 of `cr_20260813v01_load_catalog_data.md`).
        - Current: `logger.error(str(e))`
        - Expected: `logger.error(e.summary)` / `for issue in e.issues:` / `    logger.error(f"  - {issue}")` (mirroring the loader)
        - Resolution: Implemented as specified — the arm now logs `e.summary` then one `  - {issue}` record per issue, exactly mirroring the loader; pinned by `test_main_validation_failure_exits_1_and_logs_one_record_per_issue` (the summary record is asserted single-line).

3. [completed] Comment accuracy and log-dir convention - `code/load_catalog_data/check_corpus.py`
   - 3.1. [minor] Lines 63-64: the encoding comment is garbled and self-contradictory — "which cannot encode `→` and degrades it to a `→` escape mid-sentence" shows the arrow character itself where the escape form it degrades to was clearly intended (stderr's default error handler is `backslashreplace`, so an un-encodable `→` renders as the six-character sequence backslash-u-2192) (comments guideline 3: keep comments current/accurate).
        - Current: "...which cannot encode `→` and degrades it to a `→` escape mid-sentence."
        - Expected: "...which cannot encode `→` and degrades it to a `\u2192` escape mid-sentence."
        - Resolution: Implemented as specified — the comment now reads "degrades it to a `\u2192` escape mid-sentence", showing the six-character escape form instead of the arrow character.
   - 3.2. [minor] Line 90: `setup_logging(log_dir="logs/check_corpus")` gives this script its own top-level log folder, but the logging skill's log-directory table maps a script's location to its module folder — this file lives in `code/load_catalog_data/`, so the convention is `logs/load_catalog_data/` (the default `log_name` already keeps the file distinct as `check_corpus.jsonl`, so no clash with the loader's `load_catalog_data.jsonl`). The sibling loader follows the convention (`load_catalog_data.py` line 352).
        - Current: `setup_logging(log_dir="logs/check_corpus")` (and the module docstring's `logs/check_corpus/check_corpus.jsonl`, line 20)
        - Expected: `setup_logging(log_dir="logs/load_catalog_data")` with the docstring updated to `logs/load_catalog_data/check_corpus.jsonl`
        - Resolution: Implemented as specified — `setup_logging(log_dir="logs/load_catalog_data")`, and both mentions of the log path (module docstring and `_mirror_log_to_stderr`'s docstring) updated to `logs/load_catalog_data/check_corpus.jsonl`; verified a real run writes `check_corpus.jsonl` alongside the loader's `load_catalog_data.jsonl` with no clash.

4. [completed] Source the corpus root the way the loader does - `code/load_catalog_data/check_corpus.py`
   - 4.1. [minor] Lines 82-88: the corpus root arrives as a `--data-root` CLI value with a hardcoded `Path("data_catalog")` default, deviating from the executable-scripts pattern (guideline 2: single `--config` argument for the TOML path) and duplicating the loader config's `data_root = "data_catalog"` (`code/load_catalog_data/config/load_catalog_data.toml` line 12) — if the corpus root ever moves in that TOML, this script's default silently diverges from what the loader will actually load (grouped-review consistency check: the same knob is sourced two different ways). The mode-flag deviation accepted for the loaders (cr_20260730v01_load_ref_data, Executable Scripts) covered boolean flags, not config values.
        - Current: `parser.add_argument("--data-root", type=Path, default=Path("data_catalog"), ...)`
        - Expected: accept `--config` defaulting to `code/load_catalog_data/config/load_catalog_data.toml` and read `data_root` from it via `tomllib` (preserving the zero-argument usage CONTRIBUTING.md advertises while keeping one source of truth for the corpus root)
        - Resolution: Implemented as specified, with one deliberate detail: the `--config` default is resolved from the script's own location (`Path(__file__).resolve().parent / "config" / "load_catalog_data.toml"`) rather than a cwd-relative literal, so the zero-argument run finds the loader's TOML from any working directory (the TOML's relative `data_root` still resolves from the cwd, matching the loader). The config-handling arms mirror the loader's `main()` exactly: missing file → "Config file not found", `OSError`/`TOMLDecodeError` → "Failed to read config file", absent `data_root` → `KeyError` arm → "Missing required config field", each exiting 1. Module docstring usage updated; verified `uv run code/load_catalog_data/check_corpus.py` with no arguments still validates the shipped corpus (exit 0).

5. [completed] Import-block conventions diverge from the loader - `code/load_catalog_data/check_corpus.py`
   - 5.1. [suggestion] Lines 28-36: this file marks its post-`sys.path.insert` imports with `# noqa: E402` and additionally inserts its own directory (line 30), while the sibling loader does neither (`load_catalog_data.py` lines 43-60) — two conventions for one concern across the group (grouped-review consistency check, cross-referenced as finding 2.2 of `cr_20260813v01_load_catalog_data.md`).
        - Current: `sys.path.insert(0, str(Path(__file__).resolve().parent))` / `from logconfig import get_logger, setup_logging  # noqa: E402`
        - Expected: one convention across both siblings (markers on both or neither; the self-dir insert kept or dropped consistently)
        - Resolution: Deferred — no linter is configured in `pyproject.toml`, so the `noqa` markers are currently inert either way, and the self-dir insert is harmless when run as a script (the script dir is auto-prepended) while making the module importable from elsewhere — which the tests requested in finding 1.1 can use. Align when a linter is adopted.

## Skills with No Issues

1. Type Hints skill: No issues found. Both functions are annotated (`-> None`), and argparse's `type=Path` matches the `Path` consumption downstream.
2. Docstrings skill: No issues found. The module docstring documents purpose, usage, exit-status contract, and output destinations; `_mirror_log_to_stderr`'s docstring is a model of documenting the "why" (root-logger attachment rationale, stderr-vs-stdout choice); `main` carries a brief behavioral docstring.
3. Comments skill: Issues found — see finding 3.1 (garbled `→`-escape comment); the remaining comments (UTF-8 reconfigure guard, plain-formatter rationale) are accurate and explain the "why".
4. Logging skill: Issues found — see finding 3.2 (log-dir convention); otherwise `logconfig` with no `print()`, f-strings, `setup_logging` after argparse, `"=" * 60` separators on the start, success, and both error arms, and the extra stderr handler is a justified, documented addition for this script's interactive audience.
5. Exception Handling skill: Issues found — see findings 2.1 and 2.2; no bare `except`, failures are loud with contextful messages (`Corpus root not found: {args.data_root}`), and nothing is swallowed.
6. Executable Scripts skill: Issues found — see finding 4.1 (config value via CLI instead of TOML) and the missing `# pragma: no cover` noted in finding 1.1; the `main()` + `__main__` guard structure and post-argparse logging setup are in place.
7. Unit Tests skill: Issues found — see finding 1.1 (no test file exists for this script).
8. Data Validation skill: N/A — corpus validation is this script's business logic, not a `data_val_` output-validation script.
9. SQL skill: N/A — the script is deliberately database-free and contains no SQL.
