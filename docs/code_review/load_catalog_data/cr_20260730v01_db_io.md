---
name: cr_20260730v01_db_io
goal: Re-review of code/load_catalog_data/db_io.py against python-development and sql-development after commit 968ab41 (vendored logconfig path resolved from the module's own location; columns.ref_table_id FK deferral added); the standing style suggestions from cr_20260729v01 persist plus one new DRY suggestion.
created: 2026-07-30 14:20:06
updated: 2026-07-30 14:20:06
---

## Implementation Plan

1. [completed] SQL keyword casing - `code/load_catalog_data/db_io.py`
   - 1.1. [suggestion] Lines 255-305, 427-479, 481-536, 539-560, 563-635, plus 104-110 and 1043: SQL keywords are uppercase across every `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_DELETE_*`/`_HSTRY_INSERT_*` constant, both `_SET_CONSTRAINTS_DEFER_*` statements, `_INSERT_LOAD_AUDIT`, and the runtime `TRUNCATE TABLE {}` template; sql-development best-practices guideline 7 says "Lowercase everything". Applied consistently across the whole module, so this is a single file-wide style deviation rather than many separate defects. Representative line shown.
        - Current: `"SELECT system, description, notes, update_reason FROM systems"`
        - Expected: `"select system, description, notes, update_reason from systems"`
        - Resolution: Deferred — file-wide, style-only casing deviation applied consistently; these are Python string literals holding SQL, so keyword casing carries no semantic weight and reformatting touches every statement for cosmetic gain only. Matches the standing decision in cr_20260724v03 through cr_20260729v01 (each finding 1.1).

2. [completed] Private-helper docstrings omit `Raises` - `code/load_catalog_data/db_io.py`
   - 2.1. [suggestion] Lines 651, 783, 887: `_insert_params`, `_update_params`, and `_pk_params` each `raise ValueError` on an unknown table (lines 755, 884, 902) but their docstrings carry no `Raises:` section. The docstrings skill requires sections on public functions; these are private (`_`-prefixed), so a `Raises:` note is an optional consistency improvement.
        - Resolution: Deferred — helpers are private (`_`-prefixed) and the `ValueError` branch is an unreachable-by-construction guard (callers draw every table name from `_FK_ORDER`); documenting it is optional, not a required fix. Matches cr_20260729v01 finding 2.1.

3. [completed] Numbered local variables in `read_db_state` - `code/load_catalog_data/db_io.py`
   - 3.1. [suggestion] Lines 321-352: the per-table fetch loops assign the constructed row into distinct numbered names (`r`, `r2`, `r3`, `r4`, `r5`, `rdep`, `r6`), and the `column_mappings`/`concepts` loops continue with `r7`/`r8` (lines 368, 388), even though each name is used only within its own loop. A single reused local per loop would read more clearly and avoid implying the values coexist; the mix of `rdep` and numbered names shows the numbering is not load-bearing.
        - Current: `r = SystemRow(*row)` / `r2 = DataSourceRow(*row)` / ... / `rdep = DeploymentRow(*row)` / `r6 = TableRelationshipRow(*row)`
        - Expected: reuse one local name per loop, e.g. `row_obj = SystemRow(*row)` then `state.systems[pk(row_obj, "systems")] = row_obj`
        - Resolution: Deferred — readability-only naming preference; each name is loop-local, so the numbering causes no shadowing or correctness risk. Matches cr_20260729v01 finding 3.1.

4. [completed] Repo-root path expression duplicated - `code/load_catalog_data/db_io.py`
   - 4.1. [suggestion] Lines 31 and 69: the new vendored-logconfig `sys.path.insert` computes `Path(__file__).resolve().parents[2]` inline, and `_REPO_ROOT` (line 69) is defined later with the identical expression. Defining `_REPO_ROOT` once before the import block and reusing it (`sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`) would remove the duplication and guarantee the two "repo root" notions can never drift.
        - Current: `sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "code" / "lib"))` ... `_REPO_ROOT = Path(__file__).resolve().parents[2]`
        - Expected: `_REPO_ROOT = Path(__file__).resolve().parents[2]` defined once (before the vendored import), then `sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`
        - Resolution: Deferred — optional DRY tidy-up of a trivial two-token expression; hoisting `_REPO_ROOT` above the import block would split it from the other module constants (lines 52-110), and each site currently carries its own accurate explanatory comment, so the duplication poses no realistic drift risk.

## Skills with No Issues

1. Type Hints: No issues. Every function annotates parameters and return types with modern syntax (`dict[str, str]`, `tuple[Any, ...]`, `tuple[bool, Any]`, `tuple[str, ...]`). The `row: Any` / `old: Any` / `key: Any` on the polymorphic param builders (`_insert_params`, `_update_params`, `_pk_params`) remains the established deliberate choice for functions accepting any of the 9 row dataclasses (accepted in cr_20260729v01).
2. Docstrings: No issues on public functions — `connection_kwargs`, `resolve_commit_sha`, `read_db_state`, and `apply_diff` all have Google-style docstrings with Args/Returns/Raises as applicable and document the "why" (search_path rationale, SHA fallback and pinned cwd, atomic bootstrap reset, validated_ts transitions, the step-0 dual constraint deferral — updated to cover the new columns.ref_table_id FK). The private `_warn_if_dirty_working_tree` and `_validated_ts_update_args` are also documented. The only gap is the optional private-helper `Raises` note (finding 2.1).
3. Comments: No issues. Comments explain "why" and are current: the new vendored-path comment (lines 28-30) matches the actual `code/lib/logconfig` package on disk; the new `COLUMNS_REF_TABLE_ID_FK_CONSTRAINT` block (lines 89-97) and its in-transaction counterpart (lines 1031-1035) correctly describe why the updates-before-inserts phase order needs the FK deferred; the module docstring's ltree[]/text[] cast rationale (lines 11-14) matches the read casts (lines 289-305) and write casts (lines 465-479); the composite-PK WHERE warning (lines 502-506), the validated_ts CASE semantics (lines 513-522, 716-742, 758-780), the transactional-TRUNCATE note (lines 1038-1040), and the `_FK_ORDER` comment (lines 951-954) are all accurate.
4. Logging: No issues. Uses the vendored `logconfig.get_logger`, never `print()`; all messages use f-strings (guideline 6) and carry context (per-table row counts across all 9 tables, commit SHA, diff summary, reset_hstry marker, git-timeout details). No Entering/Exiting messages (funcName is auto-logged).
5. Exception Handling: No issues. `apply_diff` catches `except Exception as e:`, logs the root cause first, then rolls back and bare-`raise`s (lines 1096-1102) so a dead-connection `rollback()` failure cannot mask the original error. `connection_kwargs` raises a contextful `RuntimeError` listing the missing env vars and a `ValueError` for a bad `schema`. `resolve_commit_sha` catches the specific `FileNotFoundError` and `subprocess.TimeoutExpired`, chains both with `raise ... from e`, and separately handles a nonzero `git` return code with stderr in the message. `_warn_if_dirty_working_tree` deliberately swallows `FileNotFoundError`/`TimeoutExpired` because the check is diagnostic, not a gate (documented in its docstring).
6. Executable Scripts: N/A — imported library module, no `main()` / `__main__` entry point.
7. Data Validation: N/A — DB I/O module, not a `data_val_` script; corpus validation lives elsewhere.
8. Unit Tests: N/A for this file — tests live under `unit_tests/`; not exercised here.
9. SQL (correctness, sql-development): No issues beyond casing (finding 1.1). `%s` count <-> param-tuple length <-> column-list width re-verified for all 9 tables against `data_model.py`:
   - INSERT `%s` <-> tuple: systems 4<->4, data_sources 5<->5, schemas 6<->6, tables 6<->6, columns 10<->10 (trailing `ref_table_id` after the bind-NULL `update_reason`, matching `_INSERT_COLUMNS`' column list), deployment_tables 6<->6, table_relationships 10<->10 (incl. trailing `validated` CASE guard), column_mappings 9<->9 (incl. `%s::ltree[]` for target_tables_referenced and trailing CASE guard), concepts 6<->6 (incl. `%s::ltree[]`); each column list equals `%s` count plus the literal `now()`/CASE positions.
   - UPDATE `%s` <-> tuple: systems 4<->4, data_sources 5<->5, schemas 6<->6, tables 6<->6, columns 10<->10 (9 SET incl. ref_table_id + 1 PK), deployment_tables 6<->6 (4 SET + 2 PK), table_relationships 11<->11 and column_mappings 10<->10 (both carry the stamp_now/else_value CASE pair plus full composite-PK WHERE), concepts 6<->6 (single PK, no CASE guard). PK columns are excluded from every SET.
   - DELETE / HSTRY-INSERT: single-PK tables (systems, data_sources, schemas, tables, columns, concepts) bind a 1-tuple; deployment_tables and column_mappings name both PK parts in WHERE and bind 2-tuples; table_relationships names all 3 PK parts and binds a 3-tuple. Each `*_hstry` INSERT's column list matches its SELECT list plus `end_ts = now()` (`columns_hstry` places `ref_table_id` after `is_primary_key` in both lists, per DDL order).
   - `read_db_state`: each SELECT's column order matches its dataclass field order for positional construction — verified against `data_model.py` for `SystemRow` through `TableRelationshipRow`, including `_SELECT_COLUMNS`' trailing `ref_table_id` (matching `ColumnRow`'s defaulted last field) and the 6-field `DeploymentRow`; `column_mappings` and `concepts` build via explicit kwargs. All SELECTs list explicit columns (no `SELECT *`); every statement is `%s`-parameterized with no value interpolation; the sole runtime-built statement (`TRUNCATE TABLE {}`) uses `sql.Identifier` over the fixed `_HSTRY_TABLES` tuple; the two `SET CONSTRAINTS ... DEFERRED` statements interpolate only fixed module constants. The `%s::ltree[]` write casts and `::text[]` read casts sit at the correct positions for both target_tables_referenced and related_object_ids, and `_INSERT_LOAD_AUDIT` binds 5 params to 5 columns.
