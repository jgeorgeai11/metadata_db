---
name: cr_20260813v01_test_sql_parsing
goal: Address code quality issues identified in code/load_catalog_data/unit_tests/test_sql_parsing.py to align with python-development skills (first review of this file; reviewed as a group with test_yaml_discovery.py, so cross-file convention divergences are noted).
created: 2026-08-13 11:48:17
updated: 2026-08-13 12:27:04
---

## Implementation Plan

1. [completed] Complete the navigation/statement canary corpus - `code/load_catalog_data/unit_tests/test_sql_parsing.py`
   - 1.1. [major] Lines 176-223: `sql_parsing._NAVIGATION_NODES` (sql_parsing.py lines 181-204) lists 20 node classes and its comment states "the canary tests in unit_tests/test_sql_parsing.py exercise each so a version bump that renames a class fails loudly", but five entries have no case in either parametrized canary: `exp.Intersect`, `exp.Except`, `exp.Create`, `exp.Alter`, and `exp.TruncateTable`. A sqlglot rename or reshuffle of any of those classes would pass this suite silently while `contains_statement_or_navigation` stopped rejecting a CREATE/ALTER/TRUNCATE stored as a `target_expression`. Verified against the pinned sqlglot: each form below parses to its class and currently returns a label.
        - Current: `test_contains_statement_or_navigation_flags_navigation` covers subquery/JOIN/CTE/UNION/Block only; `test_contains_statement_or_navigation_flags_dml_ddl` covers UPDATE/DELETE/INSERT/MERGE/DROP/GRANT only
        - Expected: add the missing cases, e.g. `"SELECT d.s.t.a INTERSECT SELECT d.s.u.b"`, `"SELECT d.s.t.a EXCEPT SELECT d.s.u.b"`, `"CREATE TABLE d.s.t (a int)"`, `"ALTER TABLE d.s.t ADD COLUMN a int"`, `"TRUNCATE TABLE d.s.t"`
        - Resolution: Implemented with an added accommodation — added all five cases as specified (INTERSECT/EXCEPT joined the navigation table `_NAVIGATION_CANARY_CASES`; CREATE/ALTER/TRUNCATE joined the DML/DDL table `_STATEMENT_CANARY_CASES`), plus a sixth case the completeness canary of finding 1.3 exposed: `exp.Command` had no case either, so `("VACUUM d.s.t", "SQL command/statement")` was added (sqlglot logs its documented "falling back to parsing as a 'Command'" warning for it). Three further entries — `exp.Join`, `exp.CTE`/`exp.With`, and `exp.From` — cannot be pinned by returned label: their node always sits below a `Select`/`Subquery` that `expr.walk()` (BFS, root-first) reports first, so they are canaried by class instead in `_SHADOWED_NAVIGATION_CASES` (see finding 1.2's Resolution).
   - 1.2. [minor] Lines 186-188, 221-223: both navigation canaries assert only `is not None`, never the label returned. `contains_statement_or_navigation` returns a specific human-readable label ("subquery", "JOIN", "UPDATE statement", …) that reaches the author-facing validation message, so a mis-paired class/label entry in `_NAVIGATION_NODES` — or an ordering change that reports the wrapper instead of the wrapped node — is invisible to these tests.
        - Current: `assert ex.contains_statement_or_navigation(tree) is not None`
        - Expected: parametrize `(sql_text, expected_label)` and assert `contains_statement_or_navigation(tree) == expected_label` (e.g. `("(SELECT max(d.s.t.a) FROM d.s.t)", "subquery")`, `("DROP TABLE d.s.t", "DROP statement")`)
        - Resolution: Implemented with an added accommodation — both canaries now run off `(sql_text, expected_label)` case tables and assert `== expected_label`, so a mis-paired class/label entry fails. The accommodation covers the three labels that are structurally unreachable as a *returned* label (`"JOIN"`, `"CTE"`, `"FROM clause"` — always shadowed by the wrapping `Select`/`Subquery` under BFS walk order, which `_NAVIGATION_NODES`' own comment describes as intended): `test_contains_statement_or_navigation_flags_shadowed_nodes` pins them by looking up the classes the module's table pairs with the label and asserting such a node is present in the tree (and that the shape is still rejected). The two `is not None` assertions therefore survive only for those three, where an equality assertion is not expressible.
   - 1.3. [minor] Line 286: there is no completeness canary for `_NAVIGATION_NODES` analogous to `test_volatile_canary_covers_entire_denylist` — a class added to the navigation table without a case would dodge the canary exactly as finding 1.1 describes. Re-triaged from [suggestion] to [minor] on 2026-08-13: the original deferral argued that `_NAVIGATION_NODES` is private and the unit-tests skill discourages asserting on module internals. That is outweighed here — finding 1.1 exists precisely because five entries drifted out of coverage undetected, so fixing 1.1 without this canary repairs today's gap and leaves the mechanism that produced it intact. The repo already accepts private-surface agreement tests as drift guards for exactly this reason (`test_db_io.py` asserts on `db_io._FK_ORDER` / `_HSTRY_TABLES`, and `docs/code_review/load_catalog_data/unit_tests/cr_20260813v02_test_db_io.md` finding 2.2 calls for another).
        - Current: only the volatile denylist has a coverage-completeness assertion
        - Expected: an equivalent test asserting the labels exercised above cover `{label for _, label in ex._NAVIGATION_NODES}`
        - Resolution: Implemented as specified — `test_contains_statement_or_navigation_canary_covers_node_table` asserts the union of the labels in `_NAVIGATION_CANARY_CASES`, `_STATEMENT_CANARY_CASES`, and `_SHADOWED_NAVIGATION_CASES` equals `{label for _, label in sql_parsing._NAVIGATION_NODES}`. Equality (not `>=`) so a case naming a label the module no longer carries also fails. Writing it is what surfaced the uncovered `exp.Command` entry recorded under finding 1.1.

2. [completed] Cover the untested boolean-predicate root types - `code/load_catalog_data/unit_tests/test_sql_parsing.py`
   - 2.1. [minor] Lines 382-412: `_BOOLEAN_ROOT_TYPES` includes `exp.Or` and `exp.Not`, and the `is_boolean_predicate` docstring lists `AND`, `OR`, `NOT` as accepted logical roots, but the true-case corpus exercises `AND` only. `OR` and `NOT` roots are the two logical families a `join_condition` can carry that nothing pins. Verified both return True today.
        - Current: `"d.s.t.a AND d.s.u.b",` is the only logical-root case
        - Expected: add `"d.s.t.a OR d.s.u.b"` and `"NOT d.s.t.a"` to the parametrized true cases
        - Resolution: Implemented as specified — both cases added to `test_is_boolean_predicate_true` beside the `AND` case, under a comment naming them as the docstring's full logical family (`And` / `Or` / `Not`). Verified both parse to their own root type and return True.

3. [completed] Naming conventions - `code/load_catalog_data/unit_tests/test_sql_parsing.py`
   - 3.1. [minor] Line 9: the module under test is imported as `ex`, a leftover from its former name (`expressions.py` -> `sql_parsing.py`, commit b8c8a9b). The alias no longer maps to the module name and is one letter from `exp`, the alias `sql_parsing.py` itself uses for `sqlglot.expressions` — so `ex.parse_expression` reads as if it came from sqlglot. The sibling `test_yaml_discovery.py` (line 7) imports its module unaliased.
        - Current: `import sql_parsing as ex`
        - Expected: `import sql_parsing` (and rename the ~60 `ex.` call sites), or an alias that tracks the current name
        - Resolution: Implemented as specified — the module is imported unaliased as `import sql_parsing` (matching the sibling `test_yaml_discovery.py`) and every call site was rewritten to `sql_parsing.`. Verified no other `ex.`-prefixed text existed in the file before the rewrite.
   - 3.2. [minor] Lines 286, 311-312, 320-321: four test names do not lead with the function under test, breaking the `test_<function>_<scenario>_<expected>` convention, and two name a moment in the change history ("new denylist") that will read as stale once the entries are no longer new: `test_volatile_canary_covers_entire_denylist`, `test_new_denylist_functions_flagged_in_expression_context`, `test_new_denylist_functions_flagged_in_predicate_context`. The same applies to the `_NEW_DENYLIST_CASES` constant they share (line 302).
        - Current: `def test_new_denylist_functions_flagged_in_expression_context(...)`
        - Expected: `def test_find_volatile_functions_flags_session_state_in_expression_context(...)` (and `..._in_predicate_context`, `test_find_volatile_functions_canary_covers_entire_denylist`), with `_NEW_DENYLIST_CASES` renamed to describe the group (e.g. `_SESSION_STATE_CASES`)
        - Resolution: Implemented as specified — renamed to `test_find_volatile_functions_flags_session_state_in_expression_context`, `..._in_predicate_context`, and `test_find_volatile_functions_canary_covers_entire_denylist`, with `_NEW_DENYLIST_CASES` renamed `_SESSION_STATE_CASES`. Its lead comment was also reworded to drop the "added after the original volatile set" change-history framing that the new name no longer carries. The fourth name, `test_volatile_canary_covers_entire_denylist`, is the canary rename listed above; the new navigation canary added under finding 1.3 follows the same `test_<function>_canary_...` shape.
   - 3.3. [minor] Lines 330, 336, 343: three more tests name the SQL feature rather than the function under test — `test_age_flagged_volatile_in_expression_context`, `test_age_flagged_volatile_in_predicate_context`, `test_current_role_qualified_column_not_flagged` — all of which exercise `find_volatile_functions`.
        - Current: `def test_age_flagged_volatile_in_expression_context() -> None:`
        - Expected: `def test_find_volatile_functions_flags_age_in_expression_context() -> None:` (and the matching predicate/qualified-column names)
        - Resolution: Implemented as specified — `test_find_volatile_functions_flags_age_in_expression_context`, `test_find_volatile_functions_flags_age_in_predicate_context`, and (for the negative case) `test_find_volatile_functions_passes_qualified_current_role_column`, which follows the file's existing `_passes_` convention for not-flagged cases.

4. [completed] Optional coverage and redundancy - `code/load_catalog_data/unit_tests/test_sql_parsing.py`
   - 4.1. [suggestion] Line 33 of `sql_parsing.py`: `parse_expression`'s `dialect` parameter is exercised only at its `postgres` default; no test passes another dialect or asserts the dialect reaches sqlglot (the parameter appears in the debug log message and changes parse results).
        - Current: every call is `ex.parse_expression(sql_text)`
        - Expected: one case such as `ex.parse_expression("ocs.general.bene.bene_id", dialect="snowflake")` asserting the refs still decode
        - Resolution: Deferred — optional; the loader only ever parses under the Postgres host dialect, so a second-dialect test would pin behavior the pipeline never exercises and could become brittle across sqlglot versions.
   - 4.2. [suggestion] Lines 302-340: `_NEW_DENYLIST_CASES` re-lists five entries already present in `_VOLATILE_CANARY_CASES` (lines 237-277, the session-state and timing entries at 263-276), and `test_age_flagged_volatile_in_expression_context` re-tests the `age` canary at line 250; each pair differs only in the surrounding expression shape.
        - Current: five duplicated names plus a duplicated `age` case across two case tables
        - Expected: one parametrized pair of context tests (`COALESCE(...)` and predicate shapes) driven by a shared subset of `_VOLATILE_CANARY_CASES`
        - Resolution: Deferred — the duplication is deliberate: the canary table pins name-to-node coverage while these tests pin that both loader field shapes (`target_expression` and `join_condition`) route through the same check. Cheap to run and each documents a different contract.

## Skills with No Issues

1. Type Hints: No issues found — every test function is annotated `-> None`, all parametrized arguments are typed (`sql_text: str`, `expected: str`, `call: str`), and the module-level case tables carry explicit `list[tuple[str, str]]` annotations in modern syntax.
2. Docstrings: No issues found — the module docstring states the venue-free four-segment invariant that shapes every case; test functions convey intent via names plus rationale comments, per project convention (see cr_20260813v01_test_corpus_validation.md).
3. Comments: No issues found — comments explain why a case exists rather than what it does (the TokenError sibling rationale at lines 61-65, the case-preservation chain to `corpus_validation._case_hint` at lines 118-123, the DML/DDL "cleared every guard" note at lines 208-210), and each matches the current `sql_parsing.py` behavior.
4. Unit Tests: Findings 1.x, 2.1, 3.x, 4.x — pytest is used throughout, the filename matches `test_<module>.py`, `pytest.raises(..., match=...)` is used for every error case, tests share no mutable state, and `pytest --cov=sql_parsing` reports 100% line coverage (192 tests pass); the gaps found are table-entry and label gaps that line coverage cannot see. Multi-argument `@pytest.mark.parametrize` uses the explicit tuple argnames form here, matching `test_corpus_validation.py`; the sibling `test_yaml_discovery.py` diverges to the comma-string form, recorded as finding 1.1 of cr_20260813v01_test_yaml_discovery.md.
5. Logging: N/A - test module performs no logging.
6. Exception Handling: N/A - tests assert on raised exceptions rather than handling them.
7. Executable Scripts: N/A - not an executable script.
8. Data Validation: N/A - exercises the SQL parsing helper rather than validating pipeline I/O.
9. PySpark / Ibis / SAS Conversion: N/A - no Spark, Ibis, or SAS code present.
