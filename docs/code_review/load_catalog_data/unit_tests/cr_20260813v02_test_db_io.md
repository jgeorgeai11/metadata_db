---
name: cr_20260813v02_test_db_io
goal: Re-review of code/load_catalog_data/unit_tests/test_db_io.py against python-development skills alongside its module (db_io.py); two new pending coverage findings — an unasserted hstry-before-DELETE ordering and a missing `_FK_ORDER`/`_HSTRY_TABLES` vs `TABLE_ORDER` agreement test — plus the standing deferred suggestions carried from cr_20260813v01.
created: 2026-08-13 11:47:38
updated: 2026-08-13 12:22:38
---

## Implementation Plan

1. [completed] Coverage gaps on documented `apply_diff` contracts - `code/load_catalog_data/unit_tests/test_db_io.py`
   - 1.1. [minor] Lines 469-488: `test_apply_diff_delete_writes_hstry_then_deletes_in_reverse_fk_order` promises two contracts in its name but asserts only one — the body compares the two `DELETE FROM` indexes (lines 482-488) and never checks that the `INSERT INTO *_hstry` for a deleted row precedes its `DELETE`. That ordering is the load-bearing half: once the DELETE runs the row is gone, so a reordering silently empties the history table. `db_io.apply_diff`'s docstring step 2 (db_io.py lines 935-936) states the contract, and the sibling `test_apply_diff_update_writes_hstry_then_updates` (lines 454-466) pins exactly this shape for the update path, so the delete path is the odd one out (unit-tests guidelines 2.2 and 7.1). `test_apply_diff_runs_all_nine_table_updates_and_deletes` (line 801) proves 18 hstry inserts exist but says nothing about their position; `test_integration.py` line 609 covers the outcome end-to-end but is env-gated behind `METADATA_DB_INTEGRATION=1`.
        - Current: `t_idx = next(...DELETE FROM tables...)` / `s_idx = next(...DELETE FROM systems...)` / `assert t_idx < s_idx`
        - Expected: add the hstry-first assertion the name promises, e.g. `h_idx = next(i for i, s in enumerate(statements) if "tables_hstry" in s)` then `assert h_idx < t_idx`, mirroring lines 464-466
        - Resolution: Implemented with an added accommodation — added the hstry-first assertion for BOTH deleted rows (`t_hstry_idx < t_idx` and `s_hstry_idx < s_idx`) rather than just `tables`, since the test's diff already carries a `systems` delete and pinning both costs two lines; kept the existing reverse-FK-order assertion. A why-comment above the pair records that the ordering is load-bearing (once the DELETE runs there is nothing left to copy into history).
   - 1.2. [minor] Lines 984 and 1118: the only assertions on `db_io._FK_ORDER` are two membership spot-checks (`"deployment_tables" in`, `"concepts" in`), and `db_io._HSTRY_TABLES` is likewise only spot-checked (lines 592-593) plus counted (line 591). Neither is pinned against `data_model.TABLE_ORDER` (data_model.py lines 45-55), which `db_io._FK_ORDER` duplicates verbatim and `corpus_diff.compute_diff` (corpus_diff.py line 135) uses to emit `RowChange.table` values. A table added to `TABLE_ORDER` but missed in `db_io` therefore fails at load time with a contextless `KeyError` misreported as a config error — see finding 1.1 of `cr_20260813v02_db_io.md`. The file already models the fix for exactly this class of drift in `test_deferred_constraint_names_match_ddl` (lines 1233-1255).
        - Current: `assert "deployment_tables" in db_io._FK_ORDER` and `assert "concepts" in db_io._FK_ORDER` (no whole-tuple agreement anywhere)
        - Expected: `from data_model import TABLE_ORDER` plus `def test_fk_order_and_hstry_tables_match_table_order() -> None:` asserting `set(db_io._FK_ORDER) == set(TABLE_ORDER)` and `db_io._HSTRY_TABLES == tuple(f"{t}_hstry" for t in TABLE_ORDER)`
        - Resolution: Implemented as specified, with one added assertion — `TABLE_ORDER` is now imported from `data_model` and `test_fk_order_and_hstry_tables_match_table_order` (new "data_model agreement" section, placed after the DDL-agreement section it mirrors) asserts the `_FK_ORDER`/`TABLE_ORDER` set equality, the `_HSTRY_TABLES` exact tuple, and additionally `len(db_io._FK_ORDER) == len(TABLE_ORDER)` so a duplicated entry cannot hide behind the set comparison. A comment records why membership rather than order is asserted for `_FK_ORDER` (its ordering is FK-driven and free to diverge). Verified it fails as intended by temporarily adding a 10th name to `TABLE_ORDER`. Paired with finding 1.1 of `cr_20260813v02_db_io.md`, which replaced the contextless `KeyError` with a named `ValueError`; a parametrized `test_apply_diff_unknown_table_raises_value_error` was also added beside the three existing param-builder unknown-table tests to cover that new branch.

2. [completed] Carried optional refinements from cr_20260813v01 - `code/load_catalog_data/unit_tests/test_db_io.py`
   - 2.1. [suggestion] Lines 30 and 49: `_SHA = "testsha0000"` and the autouse `_stub_commit_sha` fixture's `monkeypatch.setenv("GITHUB_SHA", "testsha0000")` hardcode the same literal in two places serving different roles (the value handed to `apply_diff` vs. the ambient CI default).
        - Current: `_SHA = "testsha0000"` (line 30) and `monkeypatch.setenv("GITHUB_SHA", "testsha0000")` (line 49)
        - Expected: `monkeypatch.setenv("GITHUB_SHA", _SHA)`, or a distinct fixture literal such as `"ambientsha0000"`
        - Resolution: Deferred — cosmetic; both sites carry comments explaining their separate roles, and `test_apply_diff_uses_passed_sha_and_does_not_resolve_internally` (line 626) and `test_apply_diff_writes_load_audit_row_with_counts` (line 1270) both pass explicit non-`_SHA` values, so the independence of the two paths is pinned by assertion rather than by naming. Matches cr_20260813v01 finding 1.1.
   - 2.2. [suggestion] Line 1144: `test_resolve_commit_sha_prefers_ci_env` pins a platform-specific variable name (`GITHUB_SHA`, line 1147) rather than a generic "CI env"; naming the variable would make the contract visible in failure output (unit-tests guideline 2.2).
        - Current: `def test_resolve_commit_sha_prefers_ci_env(`
        - Expected: `def test_resolve_commit_sha_prefers_github_sha_env(`
        - Resolution: Deferred — the current name is not inaccurate (`GITHUB_SHA` is the CI-provided variable) and the platform-neutral wording survives a future CI migration unchanged; the body names the variable one line down. Matches cr_20260813v01 finding 1.2.
   - 2.3. [suggestion] Line 652: `row_factory: Any` is the one bare `Any` on a hint-able parameter; `Callable[[], Any]` states the zero-argument-callable contract more precisely (type-hints guideline 3).
        - Current: `table: str, row_factory: Any, reason_index: int`
        - Expected: `table: str, row_factory: Callable[[], Any], reason_index: int` (import `Callable`)
        - Resolution: Deferred — carried tradeoff; the return type genuinely varies across the eight row dataclasses and the call site (`row_factory()`, line 657) makes the contract obvious. Matches cr_20260813v01 finding 1.3.
   - 2.4. [suggestion] Lines 947-984 and 1113-1118, plus broad use of `_insert_params`/`_update_params`/`_pk_params` and the `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_HSTRY_INSERT_*`/`_HSTRY_TABLES`/`_FK_ORDER` privates: dispatch-dict identity assertions and private-surface reliance couple the tests to internal wiring (unit-tests guideline 6).
        - Current: `assert db_io._INSERT_SQL["deployment_tables"] is db_io._INSERT_DEPLOYMENT_TABLES` (and the `concepts` equivalents)
        - Expected: rely on the public-path smoke tests (`test_apply_diff_runs_all_nine_table_inserts`, `test_apply_diff_runs_all_nine_table_updates_and_deletes`) that already assert the emitted SQL through `apply_diff`
        - Resolution: Deferred — accepted tradeoff reaffirmed across six prior reviews; the private helpers encode the param-order and validated_ts-stamping contracts most precisely pinned at this level, and the same behaviors are also exercised through `apply_diff`. Re-verified this pass: every asserted index still matches `db_io.py` (INSERT `is_nullable`/`is_primary_key` at 4/5, UPDATE at 3/4, column_mappings and table_relationships UPDATE stamp_now/else_value at 6/7, all eight `reason_index` values, `_SELECT_COLUMNS` trailing `ref_table_id`). Matches cr_20260813v01 finding 1.4.
   - 2.5. [suggestion] Lines 313-323 vs 1298-1311 and 295-306 vs 1314-1328: factory pairs `_cm_row`/`_cm` and `_rel_row`/`_rel` duplicate row construction; the second member exposes `validated`/`validated_ts` for the transition-stamping tests but carries no docstring distinguishing it from its sibling.
        - Current: two near-identical factories per table with no note on why both exist
        - Expected: consolidate the pair, or add a one-line docstring on `_cm`/`_rel` clarifying the validated_ts-scenario purpose
        - Resolution: Deferred — accepted tradeoff from prior reviews; the split keeps the validated_ts tests readable, and the section comment at lines 1293-1295 plus local usage make intent clear. Matches cr_20260813v01 finding 1.5.
   - 2.6. [suggestion] Lines 1331-1380: the validated_ts transition matrix remains separate test functions rather than one parametrized case (unit-tests guideline 5.1).
        - Resolution: Deferred — accepted tradeoff from prior reviews; the explicit per-transition names document the state machine and the count is small. Matches cr_20260813v01 finding 1.6.
   - 2.7. [suggestion] Lines 586-590: `test_apply_diff_reset_hstry_truncates_all_nine_tables` matches `"TRUNCATE TABLE" in repr(c.args[0])` — the `repr()` is load-bearing (db_io builds TRUNCATE as a `psycopg2.sql.Composed`, not a `str`, so a plain `in` test raises `TypeError`) but nothing says so, and every neighboring assertion works on plain strings (comments guideline 1).
        - Current: `if "TRUNCATE TABLE" in repr(c.args[0])`
        - Expected: a one-line comment above the comprehension, e.g. `# TRUNCATE is built as psycopg2 sql.Composed (not str) — match on repr().`
        - Resolution: Deferred — optional clarity polish; the assertion is correct, the technique is confined to this one test, and a reader who tries a plain-string match gets an immediate `TypeError` pointing at the cause. Matches cr_20260813v01 finding 2.1.
   - 2.8. [minor] Lines 661-705: the `_warn_if_dirty_working_tree` coverage exercises the dirty and clean outcomes but not the swallow branch (db_io.py lines 123-124) or the nonzero-returncode branch (db_io.py line 125). Re-triaged from [suggestion] to [minor] on 2026-08-13: the near-unreachability argument is what makes the branch worth pinning rather than not — it is the arm that must never turn a diagnostic failure into a load failure, and nothing currently asserts that `resolve_commit_sha()` still returns its SHA when the `git status` leg dies. The same-day sibling review closed three equivalent error-path gaps in `test_load_ref_data.py` (`cr_20260813v01_test_load_ref_data.md` findings 1.1-1.3, taking that module 95% → 100%), so the swallow-branch class is being pinned across this branch and this is the remaining instance.
        - Current: `_fake_run` variants covering only rev-parse-plus-dirty and rev-parse-plus-clean
        - Expected: a companion test where the `git status` leg raises (or returns nonzero) and `resolve_commit_sha()` still returns the SHA with no WARNING record
        - Resolution: Implemented as specified, covering both branches separately — `test_resolve_commit_sha_returns_sha_when_dirty_check_raises` is parametrized over the two swallowed exception types (`FileNotFoundError` / `subprocess.TimeoutExpired`, ids `git_missing` and `git_status_timeout`) with a `_fake_run` that succeeds on the `rev-parse` leg and raises on the `status` leg, and `test_resolve_commit_sha_silent_when_dirty_check_returns_nonzero` covers the `returncode != 0` arm (db_io.py line 125) with a `git status` that exits 128 while writing to stdout — proving the nonzero guard, not the stdout emptiness, is what suppresses the WARNING. Both assert `resolve_commit_sha() == "deadbeef"` and that no "working tree is dirty" record was emitted. This takes `db_io.py` to 100% statement coverage (235 statements, 0 missed).

3. [completed] New optional refinements - `code/load_catalog_data/unit_tests/test_db_io.py`
   - 3.1. [suggestion] Lines 222, 465, 474, 483, 486, 498, 512, 614, 731, 774, 775, 797, 1102, 1332, 1362, 1376: sixteen lines run past 79 columns (up to 87 at line 465) while the rest of the file — and the module under test, which has only three outliers (see finding 9.1 of `cr_20260813v02_db_io.md`) — wraps at 79. The two files answer the same wrapping question differently, and no repo config settles it (`pyproject.toml` declares no ruff/flake8 line-length).
        - Current: `    u_idx = next(i for i, s in enumerate(statements) if s.startswith("UPDATE systems"))` (line 465, 87 columns)
        - Expected: `    u_idx = next(` / `        i for i, s in enumerate(statements) if s.startswith("UPDATE systems")` / `    )`
        - Resolution: Deferred — no configured line-length standard exists, and the overshoots are confined to `next(...)`/factory one-liners whose meaning is unaffected; worth revisiting only alongside a repo-wide formatter decision that would also cover `db_io.py`.
   - 3.2. [suggestion] Lines 44-48 and 708-710: the `_stub_commit_sha` fixture and `_one_insert_per_table_diff` docstrings wrap their summary across lines and close the quotes on the final text line, rather than opening with a one-line summary (PEP 257); every other docstring in the file and in `db_io.py` follows the one-line-summary form.
        - Current: `"""One insert RowChange per main table, exercising every _insert_params` / `branch."""`
        - Expected: `"""One insert RowChange per main table.` / `` / `Exercises every _insert_params branch.` / `"""`
        - Resolution: Deferred — the docstrings skill requires a brief description plus sections as needed, which both satisfy; the one-line-summary convention is PEP 257 rather than a stated project standard, and both docstrings are accurate and short.

## Skills with No Issues

1. Unit Tests skill — naming: No issues found beyond the optional rename in finding 2.2 and the name/assert mismatch in finding 1.1. Every test follows `test_<function>_<scenario>_<expected>`, and the file correctly mirrors `db_io.py` as `test_db_io.py`.
2. Unit Tests skill — pytest usage: No issues found. `pytest.raises(..., match=...)` throughout; `@pytest.mark.parametrize` for null/empty `related_object_ids` (line 203), the update_reason-NULL bind matrix (line 638), and the DDL constraint-name pair (line 1233); conftest `fake_conn`/`fake_cursor` fixtures (conftest.py lines 17-30); `monkeypatch`/`caplog` built-ins; and the autouse `_stub_commit_sha` guard against the ambient GitHub Actions `GITHUB_SHA`.
3. Unit Tests skill — mock external boundaries only: No issues found. DB conn/cursor mocked via the conftest `MagicMock` fixtures; `db_io.subprocess.run` and `db_io.resolve_commit_sha` are patched on the module that looks them up.
4. Unit Tests skill — coverage: Issues found — see findings 1.1, 1.2, and 2.8. Otherwise no issues; re-verified this pass with the full file green (75 passed): nine-SELECT read path plus the null/empty array variants, both SET CONSTRAINTS deferrals asserted in order before any write, the update-links-to-a-same-transaction-insert phase-ordering scenario, reverse-FK deletes, deployment-before-tables delete ordering, composite-PK coverage for all three multi-part tables, rollback-and-reraise, load_audit counts and empty-diff heartbeat, the SHA-is-passed-not-resolved contract, the `resolve_commit_sha` env/git/dirty/clean/timeout/missing-binary/whitespace matrix, validated_ts transitions for both validated-bearing tables, unknown-table error paths for all three param builders, the DDL constraint-name agreement pair, and the all-nine-table insert/update/delete smoke counts. The comment at lines 52-56 remains accurate: `connection_kwargs` is covered by `code/lib/pgconn/unit_tests/test_pgconn.py`, and `load_catalog_data.py` line 46 plus `test_integration.py` line 35 still import from `db_io`, so collection proves the re-export.
5. Type Hints skill: Issues found — see finding 2.3 (single `row_factory: Any`, deferred). Every other test, fixture, factory, and stub is fully and modernly annotated (`db_value: list[str] | None`, `tuple[str, ...]`, `-> None`, `-> MagicMock`, `dict[str, object]`).
6. Docstrings skill: Issues found — see finding 3.2 (two wrapped summary lines, deferred). Content-wise no issues: the module docstring is present and current (venue-free, 9 tables), the autouse `_stub_commit_sha` docstring accurately describes both its role and the apply_diff-takes-the-SHA-as-an-argument arrangement, `_one_insert_per_table_diff` is documented, and per-test intent is conveyed by descriptive names plus inline comments.
7. Comments skill: Issues found — see finding 2.7 (missing why-comment on the `repr()` match, deferred). All other comments re-verified against the current `db_io.py`: the `_SHA` note (27-29), the `_DDL_PATH` note (33-34), the fetchall-stub shape comments including the empty-list run counts (line 182 covers systems..table_relationships = 7; line 208 covers through column_mappings = 8), the SET CONSTRAINTS rationale comments (367-370, 385-389, 411-417), the columns bound-parameter index block (832-848) — both lists still name the trailing `ref_table_id`, agreeing with db_io.py lines 216-218 and 640-642 — and the GITHUB_SHA local-fallback and stamp_now index notes.
8. Logging skill: N/A - test module; log output is asserted via `caplog`, no logging responsibilities of its own.
9. Exception Handling skill: N/A - exceptions are asserted via `pytest.raises`, not handled.
10. Executable Scripts skill: N/A - not an executable script.
11. Data Validation skill: N/A - test module, not a data-pipeline output.
