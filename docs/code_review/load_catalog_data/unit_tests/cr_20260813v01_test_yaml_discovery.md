---
name: cr_20260813v01_test_yaml_discovery
goal: Address code quality issues identified in code/load_catalog_data/unit_tests/test_yaml_discovery.py to align with python-development skills (first review of this file; reviewed as a group with test_sql_parsing.py, so cross-file convention divergences are noted).
created: 2026-08-13 11:48:17
updated: 2026-08-13 12:27:04
---

## Implementation Plan

1. [completed] Align the parametrize argnames form with the suite - `code/load_catalog_data/unit_tests/test_yaml_discovery.py`
   - 1.1. [minor] Lines 80, 251, 588: every multi-argument `@pytest.mark.parametrize` in this file uses the comma-separated string form, while the rest of the loader suite uses the explicit tuple form — `("sql_text", "expected")` in `test_sql_parsing.py` (lines 280, 311, 320) and `("concept_id", "fragment")` in `test_corpus_validation.py` (line 1429). This file is the only divergence; the tuple form also avoids the silent-split failure mode when an argname ever contains a space. See cr_20260813v01_test_sql_parsing.md (Skills with No Issues, item 4).
        - Current: `@pytest.mark.parametrize(\n    "filename,expected_type",`
        - Expected: `@pytest.mark.parametrize(\n    ("filename", "expected_type"),`
        - Resolution: Implemented as specified — all three multi-argument parametrizes now use the explicit tuple form: `("filename", "expected_type")` (line 80), `("value", "kind", "expected")` (line 251, which finding 2.1 also widened to three arguments), and `("folder", "expected_type")` (line 588, now driven by the shared `_SHARD_FOLDER_CASES` table from finding 5.3). Single-argument parametrizes keep the plain string form, matching the rest of the suite.

2. [completed] Make the negative identifier cases assert which error fired - `code/load_catalog_data/unit_tests/test_yaml_discovery.py`
   - 2.1. [minor] Lines 250-267: `test_validate_identifier_segment_negative` wraps nine varied inputs in a bare `pytest.raises(ValueError)` with no `match`, so it cannot distinguish the three distinct messages `validate_identifier_segment` produces (empty -> "Empty {kind} segment is not allowed"; over-length -> the ltree limit message; charset -> "offending character(s)"). A regression that made `"has space"` fail with the empty-value message, or made `""` fail on charset, would still pass. The unit-tests skill (5.2) calls for `pytest.raises(..., match=...)`, and the file's other error tests already do this.
        - Current: `def test_validate_identifier_segment_negative(value: str, kind: str) -> None:` / `with pytest.raises(ValueError):`
        - Expected: carry an expected fragment through the parametrize — e.g. `("", "data_source", "Empty data_source segment")`, `("has$dollar", "column_name", "offending character")` — and assert `pytest.raises(ValueError, match=expected)`
        - Resolution: Implemented with an added accommodation — every case now carries its expected message fragment as a third parametrize argument and asserts `pytest.raises(ValueError, match=expected)`. The accommodation is one extra case, `("a" * 256, "column_name", "exceeds the 255-character ltree label limit")`, so all three message families the function produces are distinguished in one table rather than two (the dedicated `test_validate_identifier_segment_over_length_rejected` is left in place — it also pins the boundary against its 255-character sibling).

3. [completed] Cover documented behavior that no case pins - `code/load_catalog_data/unit_tests/test_yaml_discovery.py`
   - 3.1. [minor] Lines 177-186: the `len(rel) < 2` half of the reserved-`concepts.yaml` guard (`yaml_discovery._decode_parts`, lines 513-515), which handles the pathological `sources/concepts.yaml` with no parent segment to inspect, has no case — every concepts-depth test here sits at three or more segments. Line coverage reports 100% because the guard shares its statement with the tested branch, so only a dedicated case pins it. Verified: `sources/concepts.yaml` raises "concepts.yaml at an unsupported depth".
        - Current: `test_decode_path_concepts_under_mappings_raises` and `test_decode_path_concepts_yaml_inside_other_shard_folder_raises` are the only guard cases, both 4-segment paths
        - Expected: add a case decoding `tmp_path / "sources" / "concepts.yaml"` and asserting `pytest.raises(ValueError, match="unsupported depth")`
        - Resolution: Implemented as specified — `test_decode_path_concepts_directly_under_sources_raises` decodes `sources/concepts.yaml` and asserts the "unsupported depth" message, with a comment noting that this half of the guard shares a statement with the deeper-path half and so is invisible to line coverage.
   - 3.2. [minor] Lines 385-402: `discover_yaml_files` documents an ordering guarantee (yaml_discovery.py lines 590-597: classification issues in sorted path order, then wrong-extension issues in sorted path order), but no test asserts it — `test_discover_yaml_files_aggregates_multiple_issues` checks membership through a joined string, and no test puts a classification issue and a wrong-extension issue in the same walk. The guarantee exists so callers get deterministic `AssemblyError` output across machines; today a reordering would go unnoticed.
        - Current: `joined = "\n".join(issues)` followed by three membership assertions
        - Expected: one test seeding both kinds (e.g. a `stray.yaml` plus a `concepts.yml`) that asserts the classification issue precedes the wrong-extension issue and that same-kind issues come out in sorted path order
        - Resolution: Implemented as specified — `test_discover_yaml_files_orders_issues_by_kind_then_path` seeds two classification issues (`general/a_stray.yaml`, `general/b_stray.yaml`) and two wrong-extension issues (`ocs/concepts.yml`, `general/columns.yml`) in one walk, then asserts by index that both classification issues precede both wrong-extension issues and that each pair comes out in sorted path order. The existing membership-style `test_discover_yaml_files_aggregates_multiple_issues` is left as is — it pins the aggregation contract rather than the ordering one.

4. [completed] Tighten test setup - `code/load_catalog_data/unit_tests/test_yaml_discovery.py`
   - 4.1. [minor] Line 18: `_make_tree` opens by creating the parent of a file it is about to write, which resolves to `root` itself — always an existing `tmp_path` — so the statement is a no-op expressed indirectly. Stale/indirect arrange code misstates what the helper depends on (same class of finding as cr_20260813v01_test_corpus_validation.md 1.x).
        - Current: `(root / "systems.yaml").parent.mkdir(parents=True, exist_ok=True)`
        - Expected: `root.mkdir(parents=True, exist_ok=True)` — or delete the line, since `tmp_path` always exists
        - Resolution: Implemented as specified — replaced with `root.mkdir(parents=True, exist_ok=True)`. Kept rather than deleted so `_make_tree` stays callable against a root that does not exist yet (it is called with `tmp_path` today, but nothing in the helper depends on that).
   - 4.2. [minor] Lines 736-750: `test_decode_path_no_folder_form_for_single_purpose_types` branches inside the test (`if folder == "schema": ... else: ...`) to pick the folder's depth, which hides two different scenarios behind one parametrized name; its comment also names `data_source.yaml` among the types with no folder form, but no `data_source/` case is parametrized. Verified: `sources/ocs/data_source/core.yaml` also raises "Unrecognized YAML location".
        - Current: `@pytest.mark.parametrize("folder", ["schema", "deployments"])` plus an `if/else` building `shard_dir`
        - Expected: parametrize the relative folder path directly — e.g. `("general/schema", "deployments", "data_source")` — and build `shard_dir = tmp_path / "sources" / "ocs" / folder` with no branching
        - Resolution: Implemented as specified — the test now parametrizes `["general/schema", "data_source", "deployments"]` and builds `shard_dir = tmp_path / "sources" / "ocs" / folder` with no `if/else`, adding the missing `data_source/` case the comment already claimed (verified: `sources/ocs/data_source/core.yaml` raises "Unrecognized YAML location"). The comment was reworded to name all three folder forms it now covers.

5. [completed] Optional structure and drift-protection improvements - `code/load_catalog_data/unit_tests/test_yaml_discovery.py`
   - 5.1. [suggestion] Lines 10-40: `_make_tree` is a plain module-level builder called by ~20 tests rather than a `@pytest.fixture`, which the unit-tests skill (3.1) prefers for shared test data.
        - Current: `def _make_tree(root: Path) -> None:` called as `_make_tree(tmp_path)` in each test
        - Expected: a function-scoped fixture (e.g. `def corpus_root(tmp_path: Path) -> Path:`) injected as a parameter
        - Resolution: Deferred — the builder form is deliberate and consistent with the suite (`_happy_corpus` in `test_corpus_validation.py`, deferred there as cr_20260813v01_test_corpus_validation.md 2.1): most tests extend the tree with extra files before acting, which reads more directly from an explicit call, and each call already builds a fresh `tmp_path`.
   - 5.2. [suggestion] Line 40: `_BASE_FILE_COUNT = 8` is maintained by hand next to `_make_tree` and asserted in a dozen tests; adding a file to the helper without bumping the constant would break those tests with a confusing off-by-one rather than at the source.
        - Current: `_BASE_FILE_COUNT = 8` with a comment enumerating the eight files
        - Expected: have `_make_tree` return the paths it wrote and derive the count (`len(_make_tree(tmp_path))`)
        - Resolution: Deferred — optional; the constant is documented by an adjacent comment listing exactly the eight files, and any drift fails loudly in the very next test run, so the indirection buys little.
   - 5.3. [minor] Lines 588-594, 677-678, 698: three parametrize lists re-spell the four shard-folder names that `yaml_discovery._SHARD_FOLDER_TYPES` defines; a fifth row-list type added to that table would silently gain no test coverage. `test_sql_parsing.py` guards the analogous risk with `test_volatile_canary_covers_entire_denylist`. Re-triaged from [suggestion] to [minor] on 2026-08-13, together with finding 1.3 of `cr_20260813v01_test_sql_parsing.md`, which the original deferral cross-referenced: the private-surface objection is outweighed by the demonstrated failure mode — the sibling review found five `_NAVIGATION_NODES` entries that had already drifted out of coverage undetected, and this table is one row-list type away from the same silent gap. The repo accepts private-surface agreement tests as drift guards (`test_db_io.py` on `db_io._FK_ORDER` / `_HSTRY_TABLES`).
        - Current: `["tables", "columns", "table_relationships", "concepts"]` repeated as literals
        - Expected: a drift canary asserting the parametrized names cover the module's shard-folder table
        - Resolution: Implemented as specified — the three re-spelled literal lists were promoted to two shared module-level tables, `_SHARD_FOLDER_CASES` (folder -> expected `FileType`, driving the shard-decode and reserved-schema-name parametrizes) and `_SCHEMA_ONLY_SHARD_FOLDERS` (driving the wrong-depth parametrize). `test_decode_path_shard_folder_cases_cover_module_tables` then asserts each equals the module's own table (`yaml_discovery._SHARD_FOLDER_TYPES`, `yaml_discovery._SCHEMA_ONLY_SHARD_FOLDERS`), so a fifth row-list type — or a change to which folders are schema-only — fails here instead of silently gaining no coverage. Set equality (not `>=`) so a stale name left behind in the tests fails too.

## Skills with No Issues

1. Type Hints: No issues found — every test function is annotated `-> None`, fixtures and parametrized arguments are typed (`tmp_path: Path`, `filename: str`, `expected_type: str`), and the `_make_tree` helper carries full parameter and return annotations.
2. Docstrings: No issues found — the module has a docstring naming the venue-free path grammar under test, `_make_tree` documents the layout it builds, and test functions convey intent via names plus rationale comments, per project convention.
3. Comments: No issues found beyond finding 4.2 — comments consistently explain why a case exists (the Windows case-insensitive-collision rationale at lines 411-414, 497-499, 655-657; the "not created on disk" note for the over-length stem at lines 663-665; the delete-by-absence motivation at lines 480-483), and each matches current `yaml_discovery.py` behavior.
4. Unit Tests: Findings 1.1, 2.1, 3.x, 4.x, 5.x — pytest is used throughout, the filename matches `test_<module>.py`, function names follow `test_<function>_<scenario>_<expected>`, `tmp_path` is used instead of mocking the filesystem, tests share no state (each builds its own tree), assertions target the public `PathIdentity` fields and the returned `(identities, issues)` tuple rather than internals, and `pytest --cov=yaml_discovery` reports 100% line coverage (192 tests pass across the pair).
5. Logging: N/A - test module performs no logging; the module under test logs at debug, which these tests do not assert on.
6. Exception Handling: N/A - tests assert on raised exceptions rather than handling them.
7. Executable Scripts: N/A - not an executable script.
8. Data Validation: N/A - exercises the path-classification module rather than validating pipeline I/O.
9. PySpark / Ibis / SAS Conversion: N/A - no Spark, Ibis, or SAS code present.
