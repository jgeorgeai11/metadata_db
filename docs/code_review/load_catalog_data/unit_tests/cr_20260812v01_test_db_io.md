---
name: cr_20260812v01_test_db_io
goal: Re-review of code/load_catalog_data/unit_tests/test_db_io.py against python-development skills after commit 706f367 (the ~12 CI_COMMIT_SHA to GITHUB_SHA renames from the GitHub migration); one stale bound-parameter comment plus the standing suggestions carried from cr_20260730v01.
created: 2026-08-12 13:51:19
updated: 2026-08-12 14:51:40
---

## Implementation Plan

1. [completed] Stale bound-parameter index comment for `columns` - `code/load_catalog_data/unit_tests/test_db_io.py`
   - 1.1. [minor] Lines 904-911: the comment block documenting the `columns` parameter orders omits `ref_table_id` from both statements. `db_io._INSERT_COLUMNS` (db_io.py lines 448-453) ends `..., update_reason, ref_table_id`, and `db_io._UPDATE_COLUMNS` (db_io.py lines 499-503) sets `..., update_reason=%s, ref_table_id=%s` before the WHERE key — yet the comment stops at `update_reason` in the INSERT list and jumps straight from `update_reason` to `<WHERE key>` in the UPDATE list. `db_io.py` describes the same order correctly at its lines 272-274 and 694-696, so the two files now answer the same question differently (see `cr_20260812v01_db_io.md` finding 3.1). The constants the block introduces (indices 3-5) are unaffected and the tests pass, but the comment misdescribes the layout the next index change would be reasoned from (comments guideline 3: keep comments current).
        - Current: `# _INSERT_COLUMNS order: column_id, table_id, column_name, data_type,` / `# is_nullable, is_primary_key, description, notes, update_reason.` and `# _UPDATE_COLUMNS SET order: table_id, column_name, data_type, is_nullable,` / `# is_primary_key, description, notes, update_reason, <WHERE key>.`
        - Expected: `# is_nullable, is_primary_key, description, notes, update_reason, ref_table_id.` and `# is_primary_key, description, notes, update_reason, ref_table_id, <WHERE key>.`
        - Resolution: Implemented as specified — both comment lines now name the trailing `ref_table_id`, matching `db_io._INSERT_COLUMNS` (db_io.py lines 448-453) and `db_io._UPDATE_COLUMNS` (lines 499-503) and agreeing with `db_io.py`'s own descriptions at lines 272-274 and 694-696 (the cross-file drift noted in `cr_20260812v01_db_io.md` finding 3.1 is now resolved on the test side, as that finding directed). The `COLUMNS_INSERT_*`/`COLUMNS_UPDATE_*` index constants below the comment were re-verified unchanged (4/5 and 3/4) and no code was touched; 86 tests pass.

2. [completed] Optional refinements (new and carried from cr_20260730v01) - `code/load_catalog_data/unit_tests/test_db_io.py`
   - 2.1. [suggestion] Lines 30 and 49: `_SHA = "testsha0000"` and the autouse `_stub_commit_sha` fixture's `monkeypatch.setenv("GITHUB_SHA", "testsha0000")` hardcode the same literal in two places. They serve different roles (the value handed to `apply_diff` vs. the ambient CI default), but the shared literal invites a reader to assume one derives from the other; the fixture could reference `_SHA`, or the two could carry distinct values to make the independence explicit.
        - Current: `_SHA = "testsha0000"` (line 30) and `monkeypatch.setenv("GITHUB_SHA", "testsha0000")` (line 49)
        - Expected: `monkeypatch.setenv("GITHUB_SHA", _SHA)`, or a distinct fixture literal such as `"ambientsha0000"`
        - Resolution: Deferred — cosmetic; both sites already carry comments explaining their separate roles, and `test_apply_diff_uses_passed_sha_and_does_not_resolve_internally` (line 685) and `test_apply_diff_writes_load_audit_row_with_counts` (line 1335) both pass explicit non-`_SHA` values, so the independence of the two paths is already pinned by assertion rather than by naming.
   - 2.2. [suggestion] Line 1216: `test_resolve_commit_sha_prefers_ci_env` now pins a platform-specific variable name (`GITHUB_SHA`, line 1219) rather than a generic "CI env" — and per the migration activity's decision 3 it is the single test standing between a missed rename and silently-wrong `load_audit` provenance. A name stating the variable would make that contract visible in the failure output (unit-tests guideline 2.2).
        - Current: `def test_resolve_commit_sha_prefers_ci_env(`
        - Expected: `def test_resolve_commit_sha_prefers_github_sha_env(`
        - Resolution: Deferred — the current name is not inaccurate (`GITHUB_SHA` is the CI-provided variable) and the platform-neutral wording survives a future CI migration unchanged, which is arguably the point of the seam; the body already names the variable one line down.
   - 2.3. [suggestion] Lines 58-61, 76-79, 112-115, 123-126: four `connection_kwargs` tests repeat the same four `monkeypatch.setenv("POSTGRES_*", ...)` lines; a small `_pg_env` fixture (conftest or module-local) would remove the repetition (unit-tests guideline 3.1). Carried from cr_20260730v01 item 2.1.
        - Current: `monkeypatch.setenv("POSTGRES_HOST", "h")` ... repeated in each test
        - Expected: a shared fixture applying the four env vars, requested by the tests that need them
        - Resolution: Deferred — four short, fully explicit setup blocks in adjacent tests; the inline env values ("h", "5432", "u", "p") are asserted verbatim in the first test, so keeping them visible at the call site aids readability more than a fixture would save.
   - 2.4. [suggestion] Line 724: `row_factory: Any` is the one bare `Any` on a hint-able parameter; `Callable[[], Any]` states the zero-argument-callable contract more precisely (type-hints guideline 3). Carried from cr_20260730v01 item 2.2.
        - Current: `table: str, row_factory: Any, reason_index: int`
        - Expected: `table: str, row_factory: Callable[[], Any], reason_index: int` (import `Callable`)
        - Resolution: Deferred — carried tradeoff; the return type genuinely varies across the eight row dataclasses and the call site (`row_factory()` at line 729) makes the contract obvious. Low value.
   - 2.5. [suggestion] Lines 1040-1055, 1185-1190, plus broad use of `_insert_params`/`_update_params`/`_pk_params` and the `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_HSTRY_INSERT_*`/`_HSTRY_TABLES`/`_FK_ORDER` privates: dispatch-dict identity assertions and private-surface reliance couple tests to internal wiring (unit-tests guideline 6). Carried from cr_20260730v01 item 2.3.
        - Current: `assert db_io._INSERT_SQL["deployment_tables"] is db_io._INSERT_DEPLOYMENT_TABLES` (and the `concepts` equivalents)
        - Expected: rely on the public-path smoke tests (`test_apply_diff_runs_all_nine_table_inserts`, `test_apply_diff_runs_all_nine_table_updates_and_deletes`) that already assert the emitted SQL through `apply_diff`
        - Resolution: Deferred — accepted tradeoff reaffirmed across four prior reviews; the private helpers encode param-order and validated_ts-stamping contracts most precisely pinned at this level, and the same behaviors are also exercised through `apply_diff`. Re-verified this pass: every asserted index still matches `db_io.py` (INSERT is_nullable/is_primary_key at 4/5, UPDATE at 3/4, column_mappings and table_relationships UPDATE stamp_now/else_value at 6/7, all eight `reason_index` values, `_SELECT_COLUMNS` trailing `ref_table_id`).
   - 2.6. [suggestion] Lines 385-395 vs 1370-1383 and 367-378 vs 1386-1400: factory pairs `_cm_row`/`_cm` and `_rel_row`/`_rel` duplicate row construction; the second member exposes `validated`/`validated_ts` for the transition-stamping tests but carries no docstring distinguishing it from its sibling. Carried from cr_20260730v01 item 2.4.
        - Current: two near-identical factories per table with no note on why both exist
        - Expected: consolidate the pair, or add a one-line docstring on `_cm`/`_rel` clarifying the validated_ts-scenario purpose
        - Resolution: Deferred — accepted tradeoff from prior reviews; the split keeps the validated_ts tests readable, and section comments plus local usage make intent clear.
   - 2.7. [suggestion] Lines 1403-1452: the validated_ts transition matrix remains separate test functions rather than one parametrized case (unit-tests guideline 5.1). Carried from cr_20260730v01 item 2.5.
        - Resolution: Deferred — accepted tradeoff from prior reviews; the explicit per-transition names document the state machine and the count is small.

## Skills with No Issues

1. Unit Tests skill — naming: No issues found beyond the optional rename in task 2.2. Every test follows `test_<function>_<scenario>_<expected>`, the file correctly mirrors `db_io.py` as `test_db_io.py`, and `test_connection_kwargs_prod_schema_passes` (line 120) now genuinely exercises `"prod"` — the cr_20260730v01 item 1.1 fix held.
2. Unit Tests skill — pytest usage: No issues found. `pytest.raises(..., match=...)` throughout; `@pytest.mark.parametrize` for invalid schemas (line 92), null/empty `related_object_ids` (line 275), the update_reason-NULL bind matrix (line 710), and the DDL constraint-name pair (line 1305); conftest `fake_conn`/`fake_cursor` fixtures; `monkeypatch`/`caplog` built-ins; and the autouse `_stub_commit_sha` guard. The cr_20260730v01 item 1.2 fix held — the DDL-agreement pair is a single parametrized `test_deferred_constraint_names_match_ddl` over a module-level `_DDL_PATH`.
3. Unit Tests skill — mock external boundaries only: No issues found. DB conn/cursor mocked via conftest `MagicMock` fixtures; `db_io.subprocess.run`, `db_io.resolve_commit_sha`, and `db_io.load_dotenv` are patched on the module that looks them up.
4. Unit Tests skill — coverage: No issues found. The 706f367 rename is fully mirrored — all 12 env-var set/unset sites moved to `GITHUB_SHA` (lines 49, 738, 762, 1219, 1226, 1235, 1245, 1262, 1281, 1290), no `CI_COMMIT_SHA` reference survives anywhere in `code/`, and `test_resolve_commit_sha_prefers_ci_env` pins the new name against the module's lookup at `db_io.py` line 211. The autouse `_stub_commit_sha` fixture is now load-bearing in a new way (a GitHub Actions runner sets `GITHUB_SHA` ambiently, unlike the old GitLab name) and correctly forces a deterministic value; the tests that must see it absent all `delenv` explicitly. Pre-existing coverage is intact: nine-SELECT read path, both SET CONSTRAINTS deferrals in order before any write, the update-links-to-same-transaction-insert phase-ordering scenario, reverse-FK deletes, rollback-and-reraise, load_audit counts and heartbeat, the resolve_commit_sha env/git/dirty/clean/timeout/missing-binary/whitespace matrix, validated_ts transitions, and the all-nine-table smoke counts. Full file: 86 passed.
5. Type Hints skill: Issues found — see task 2.4 (single `row_factory: Any` refinement, deferred). Every other test, fixture, factory, and stub is fully and modernly annotated (`db_value: list[str] | None`, `tuple[str, ...]`, `-> None`, `-> MagicMock`).
6. Docstrings skill: No issues found. Module docstring present and current (venue-free, 9 tables); the autouse `_stub_commit_sha` fixture docstring was correctly updated to name `GITHUB_SHA` (line 45) and still accurately describes the apply_diff-takes-the-SHA-as-an-argument arrangement; `_one_insert_per_table_diff` is documented; per-test intent is conveyed by descriptive names plus inline comments, consistent with prior reviews.
7. Comments skill: Issues found — see task 1.1 (`columns` index comment omits `ref_table_id`). All other comments re-verified accurate against the current `db_io.py`: the `_SHA` note (lines 27-29), the `_DDL_PATH` note (lines 33-34), the fetchall-stub shape comments including the empty-list run counts (line 254 covers systems..table_relationships = 7; line 280 covers through column_mappings = 8), the SET CONSTRAINTS rationale comments (lines 442-444, 459-461, 485-489), the GITHUB_SHA local-fallback comments (lines 736, 1279), and the column_mappings/table_relationships stamp_now index notes (lines 912-920). `repr(c.args[0])` in the TRUNCATE assertions remains required because `db_io` builds TRUNCATE as a `psycopg2.sql.Composed`, not a `str`.
8. Logging skill: N/A — test module; log output is asserted via `caplog`, no logging responsibilities of its own.
9. Exception Handling skill: N/A — exceptions are asserted via `pytest.raises`, not handled.
10. Executable Scripts skill: N/A — not an executable script.
11. Data Validation skill: N/A — test module, not a data-pipeline output.
