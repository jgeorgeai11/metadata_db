---
name: cr_20260813v02_corpus_assembly
goal: Address code quality issues identified in code/load_catalog_data/corpus_assembly.py to align with python-development skills; re-review since cr_20260813v01 (same file state — the CONTRIBUTING.md wave references at lines 248-250, 1404-1405, 1451-1453, 1620-1621, and 1666-1668 re-verified against CONTRIBUTING.md waves 1 and 3), adding two new minor findings and carrying its two deferred suggestions forward.
created: 2026-08-13 11:44:21
updated: 2026-08-13 12:22:11
---

## Implementation Plan

1. [completed] Correct the YAML-body mapping annotations - `code/load_catalog_data/corpus_assembly.py`
   - 1.1. [minor] Line 205: `_check_recognized_keys` annotates its parsed-body parameter as `dict[str, Any]`, but the function's own comment (lines 221-223) states that YAML keys need not be strings (`true:`/`1:` parse as `bool`/`int`) and the code sorts by `repr` precisely to survive them. The annotation contradicts the documented input domain, and a reader (or a type checker) sees the `key=repr` guard as unnecessary. The same over-narrow annotation is on `_require_mapping`'s return (line 356), which feeds these bodies, and on `_check_optional_string_fields` (line 233).
        - Current: `raw: dict[str, Any], file_type: str, ident: PathIdentity` (line 205) and `def _require_mapping(doc: Any, ident: PathIdentity) -> dict[str, Any]:` (line 356)
        - Expected: `raw: dict[Any, Any]` on `_check_recognized_keys` (and `_check_optional_string_fields`), with `_require_mapping` returning `dict[Any, Any]`, so the annotation matches the parsed-YAML reality the guards already handle
        - Resolution: Implemented as specified with an added docstring accommodation — `_check_recognized_keys` (line 205) and `_check_optional_string_fields` (line 236) now take `raw: dict[Any, Any]` and `_require_mapping` (line 359) returns `dict[Any, Any]`. Since the annotation alone no longer hints at why the `key=repr` sort exists, the `Args: raw` entry on `_check_recognized_keys` and the `Returns` section on `_require_mapping` each gained a sentence stating that YAML does not require string keys and that non-string keys are what these guards reject. No call site needed changing (every assembler already takes `raw: Any`, and `dict[str, Any]` remains assignable to `dict[Any, Any]`); `_validate_concept_name`'s `raw: dict[str, Any]` was left alone as out of this finding's scope. 218 tests in `test_corpus_assembly.py` and 725 in the loader suite pass.

2. [completed] Document the nested deployment-row helper - `code/load_catalog_data/corpus_assembly.py`
   - 2.1. [minor] Line 1422: the `_row` closure inside `_expand_schema_entry` carries no docstring, unlike every other nested helper in this module (`_record` line 1953, `_rejections_for` line 1976, `_record_file_failure` line 1982), each of which has a one-line summary. Its three positional arguments (`tbl_id`, `phys_schema`, `phys_table`) are the ones most easily transposed at a call site, so the omission is where a one-liner helps most.
        - Current: `def _row(tbl_id: str, phys_schema: str, phys_table: str) -> DeploymentRow:` followed directly by `return _deployment_row(`
        - Expected: add `"""Build one row for this entry, binding the shared venue context."""` under the signature
        - Resolution: Implemented as specified — `_row` (now line 1428) carries the one-line summary verbatim, matching the style of the module's other nested helpers; the line fits the file's 79-column wrap.

3. [completed] Optional enhancements - `code/load_catalog_data/corpus_assembly.py`
   - 3.1. [suggestion] Line 1395: `_expand_schema_entry` takes `ctx: dict[str, Any]` — a heterogeneous string-keyed bag (`system`, `ds_id`, `phys_db`, `path`, `rejections`, `suppressed_ds`) built in `_assemble_deployment_entry` (line 1675) and read via string subscripts. Per the type-hints skill ("be specific", not `Any`), a small frozen dataclass would give each field a precise type and catch key typos statically. Carried forward from cr_20260729v01 task 2.1, cr_20260803v01 finding 2.2, cr_20260803v02 finding 1.2, and cr_20260813v01 finding 1.2.
        - Current: `ctx: dict[str, Any]` with `ctx["system"]`, `ctx["ds_id"]`, `ctx["phys_db"]`, `ctx["path"]`, `ctx["rejections"]`, `ctx["suppressed_ds"]`
        - Expected: a `@dataclass` context object with typed fields, passed in place of the dict
        - Resolution: Deferred — optional structural refactor, already deferred four times for reasons that still hold: the dict is internal to the deployments pass, its keys are set in one place and read in two adjacent functions, and the field roles are spelled out in the `_expand_schema_entry` docstring, so the change carries churn without a correctness gain.
   - 3.2. [suggestion] Line 305: `_UniqueKeyLoader.construct_mapping` raises `yaml.constructor.ConstructorError` on merge and duplicate keys but its one-line docstring has no Google-style `Raises:` section. Carried forward from cr_20260803v01 finding 2.1, cr_20260803v02 finding 1.1, and cr_20260813v01 finding 1.1.
        - Current: `"""Build the mapping, raising on any duplicated or merge key."""`
        - Expected: add a `Raises: yaml.constructor.ConstructorError: On a merge key or a duplicated key.` section
        - Resolution: Deferred — the one-line summary already names the raising behavior, and the class docstring documents both raise conditions and the why in full; a formal section would restate it. The method is also an override of a PyYAML hook, where the local docstring is conventionally brief.
   - 3.3. [suggestion] Lines 1463-1476 and 1521-1534: the "schema has no documented tables, so it expands to zero deployment rows" branch is duplicated verbatim between the string form and the `name`/no-`tables` mapping form of `_expand_schema_entry` — the same suppression check, the same `suppressed_ds` marking, and the same four-line message. A shared local helper would keep the two forms' wording from drifting apart.
        - Current: two identical blocks — `if rejections.tables_reduced(schema_name): suppressed_ds.add(ctx["ds_id"]); return rows` followed by the same `issues.append(f"deployment entry for system {system!r} ... expands to zero deployment rows ...")`
        - Expected: one helper (e.g. `_no_documented_tables(schema_name)` returning `bool` after recording the issue or the suppression) called from both branches
        - Resolution: Deferred — optional de-duplication; the two copies sit 60 lines apart in one function and are byte-identical today, so the drift risk is low, and extracting a helper that both records an issue and returns a control-flow flag trades one small duplication for a less obvious control flow.
   - 3.4. [suggestion] Lines 430-433, 567-570, 655-659, 715-718, 798-803, 915-918, 1055-1061, 1646-1649, 1669-1672: the wrap around `validate_identifier_segment` — `except ValueError as e: raise ValueError(f"{e} (in {ident.path}: {raw!r})") from e` — is repeated nine times with only the field name varying. A single `_validate_segment(value, field, ident, raw)` helper would centralize the message shape so every call site keeps identical context formatting.
        - Current: `try: validate_identifier_segment(table_name, "table_name")` / `except ValueError as e: raise ValueError(f"{e} (in {ident.path}: {raw!r})") from e`
        - Expected: `_validate_segment(table_name, "table_name", ident, raw)` with the try/except and message wrap inside the helper
        - Resolution: Deferred — optional de-duplication; each site is three mechanical lines whose message shape is already uniform, and the deployment-entry sites (lines 1455-1461, 1507-1515, 1573-1579) append an issue instead of raising, so a single helper would not cover them all anyway.
   - 3.5. [suggestion] Whole file (2163 lines): the module holds two cohesive but separable concerns — per-file-type row assembly (lines 1-1281) and the sparse-to-expanded deployments pass plus corpus orchestration (lines 1283-2163). Splitting the deployments machinery (`_WaveOneRejections`, `_index_tables_by_data_source`, `_deployment_row`, `_expand_schema_entry`, `_assemble_deployment_entry`, `_assemble_deployments`) into its own module would shorten both files and give the second-pass logic its own test surface.
        - Resolution: Deferred — optional structural refactor; the deployments pass shares `_check_recognized_keys`, `_RECOGNIZED_KEYS`, and the issue-accumulation contract with the assemblers, so a split adds a cross-module coupling and renames import paths in the tests for a readability gain only. Revisit if a third pass is added.

## Skills with No Issues

1. Type Hints: No issues found beyond finding 1.1 — every function, method, and nested closure (`_row`, `_record`, `_rejections_for`, `_record_file_failure`) carries parameter and return annotations in modern syntax (`str | None`, `list[...]`, `frozenset[str]`, `tuple[str, ...]`, `dict[str | tuple[str, ...], Path]`); no `Optional`/`List` legacy forms; the remaining `Any` uses are genuinely-untyped parsed-YAML documents. (The `ctx: dict[str, Any]` specificity note remains a deferred suggestion, finding 3.1.)
2. Docstrings: No issues found beyond finding 2.1 — module, class, and every module-level function carry Google-style docstrings with Args/Returns/Raises where applicable, and the CONTRIBUTING.md references remain accurate against the current file: the update_reason discipline is wave 3 (rule 20) and the physical-name charset is the deployment file rules in wave 1 (rule 7), exactly as `_check_optional_string_fields`, `_expand_schema_entry`, and `_assemble_deployment_entry` now say. (The `construct_mapping` Raises section remains a deferred suggestion, finding 3.2.)
3. Comments: No issues found — comments explain the "why" throughout (reserved-segment collisions at lines 571-574 and 660-664, wave-1 cascade suppression at 1584-1588, YAML retyping hazards at 684-685, sort-by-repr rationale at 221-223, validate-physical-name-first ordering at 1558-1562, deterministic sorted-path iteration at 1923-1927), and none is stale: the wave-1 deployment-rule citations at lines 1451-1453 and 1666-1668 match CONTRIBUTING.md rule 7.
4. Logging: No issues found — `get_logger(__name__)` from the vendored logconfig, no `print`, no lazy `%s` formatting, and a single condensed INFO summary (lines 2150-2161) with f-string interpolation; issue reporting is intentionally routed through `AssemblyError` to the orchestrator's logger rather than duplicated here.
5. Exception Handling: No issues found — no bare excepts; `load_yaml` catches `(OSError, yaml.YAMLError)` and chains via `from e` (lines 350-353); every `validate_identifier_segment` call site either wraps with `raise ValueError(...) from e` or appends a deployment-entry-contextualized issue; per-row and per-entry handlers catch the specific `ValueError` to aggregate rather than swallow; and the domain type `AssemblyError` subclasses `ValidationError` instead of a generic `Exception`.
6. Executable Scripts: N/A - library module with no `main()` / CLI entry point.
7. Data Validation: N/A - this module is corpus-assembly/authoring-shape validation itself, not a `data_val_` output-quality script.
8. Unit Tests: N/A - this file is the module under review, not its test suite.
</content>
</invoke>
