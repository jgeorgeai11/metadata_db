---
name: cr_20260813v01_db_io
goal: Re-review of code/load_catalog_data/db_io.py against python-development and sql-development after the vendored-lib refactor (connection_kwargs/logconfig moved to code/lib and re-exported here); one stale overview-comment minor plus the standing style suggestions carried from cr_20260812v01.
created: 2026-08-13 11:05:25
updated: 2026-08-13 11:42:10
---

## Implementation Plan

1. [completed] Stale SQL-dispatch overview comment - `code/load_catalog_data/db_io.py`
   - 1.1. [minor] Lines 367-368: the section-header comment describes a single `Map: table -> (insert_sql, update_sql, delete_sql, hstry_insert_sql, content_columns_for_bind, pk_columns)` six-tuple map, but no such structure exists — the implementation is four separate dicts (`_INSERT_SQL`/`_UPDATE_SQL`/`_DELETE_SQL`/`_HSTRY_INSERT_SQL`, lines 851-894, each `table -> sql string`) plus three param-builder functions (`_insert_params`, `_update_params`, `_pk_params`), and the names `content_columns_for_bind`/`pk_columns` appear nowhere in the module. A reader searching for that map finds nothing (comments guideline 3: keep comments current — this reads as a leftover design sketch).
        - Current: `# Map: table -> (insert_sql, update_sql, delete_sql, hstry_insert_sql,` / `#                content_columns_for_bind, pk_columns)`
        - Expected: `# Per table: four SQL dicts (_INSERT_SQL / _UPDATE_SQL / _DELETE_SQL /` / `# _HSTRY_INSERT_SQL, each table -> statement) plus the param builders` / `# (_insert_params / _update_params / _pk_params) that bind each row` / `# dataclass in its statement's column order.`
        - Resolution: Implemented as specified — replaced the stale two-line six-tuple "Map:" comment with the four-line description of the four SQL dicts (`_INSERT_SQL`/`_UPDATE_SQL`/`_DELETE_SQL`/`_HSTRY_INSERT_SQL`) plus the three param builders (`_insert_params`/`_update_params`/`_pk_params`); verified all seven named structures exist in the module and the 75 `test_db_io.py` tests still pass.

2. [completed] Module docstring names only one of the two ltree[] columns - `code/load_catalog_data/db_io.py`
   - 2.1. [suggestion] Lines 11-14: the module docstring explains the ltree[] read/write cast pattern solely for `column_mappings.target_tables_referenced`, but `concepts.related_object_ids` follows the identical pattern (read cast at lines 248-251, write casts at lines 421-425 and 478-482). Naming both columns (or saying "the two ltree[] array columns") would make the docstring complete.
        - Current: `` `column_mappings.target_tables_referenced` is a Postgres `ltree[]`; ``
        - Expected: `` `column_mappings.target_tables_referenced` and `concepts.related_object_ids` are Postgres `ltree[]` columns; ``
        - Resolution: Deferred — optional completeness polish; both concepts sites carry their own comments explicitly stating they mirror the column_mappings pattern (lines 418-420, 691-692, 819-821), so the pattern is discoverable at every point of use.

3. [completed] CI SHA env-var name is an inline literal - `code/load_catalog_data/db_io.py`
   - 3.1. [suggestion] Lines 155, 169-170, 178-180, 186-188: `"GITHUB_SHA"` is read as a bare literal in the lookup and restated as prose in the three `RuntimeError` messages. A `_CI_SHA_ENV_VAR = "GITHUB_SHA"` module constant interpolated into all four sites would make any future platform seam a one-line change. Carried from cr_20260812v01 item 2.1; the original rationale's `ENV_VARS` sibling has since moved to `code/lib/pgconn`, so there is no longer even a local convention this deviates from.
        - Current: `ci_sha = os.environ.get("GITHUB_SHA", "").strip()` plus three hardcoded `GITHUB_SHA` mentions in the error strings
        - Expected: `_CI_SHA_ENV_VAR = "GITHUB_SHA"` beside the other module constants, then `os.environ.get(_CI_SHA_ENV_VAR, "")` and `f"... {_CI_SHA_ENV_VAR} is unset/blank ..."`
        - Resolution: Deferred — optional DRY tidy-up on a migrated seam pinned by `test_resolve_commit_sha_prefers_ci_env`; all four sites sit within one 60-line function where drift is immediately visible, and the error strings read more naturally as literal prose. Matches cr_20260812v01 finding 2.1.

4. [completed] SQL keyword casing - `code/load_catalog_data/db_io.py`
   - 4.1. [suggestion] Lines 94-100, 201-251, 373-506, 509-581, 989, plus `_INSERT_LOAD_AUDIT` (485-488): SQL keywords are uppercase across every `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_DELETE_*`/`_HSTRY_INSERT_*` constant, both `_SET_CONSTRAINTS_DEFER_*` statements, and the runtime `TRUNCATE TABLE {}` template; sql-development best-practices guideline 7 says "Lowercase everything". Applied consistently module-wide, so this is one file-wide style deviation. Representative line shown.
        - Current: `"SELECT system, description, notes, update_reason FROM systems"`
        - Expected: `"select system, description, notes, update_reason from systems"`
        - Resolution: Deferred — file-wide, style-only casing deviation applied consistently; these are Python string literals holding SQL, so keyword casing carries no semantic weight and reformatting touches every statement (and the tests' `startswith("INSERT INTO ...")` assertions) for cosmetic gain only. Matches the standing decision in cr_20260724v03 through cr_20260812v01.

5. [completed] Private-helper docstrings omit `Raises` - `code/load_catalog_data/db_io.py`
   - 5.1. [suggestion] Lines 597, 729, 833: `_insert_params`, `_update_params`, and `_pk_params` each `raise ValueError` on an unknown table (lines 701, 830, 848) but their docstrings carry no `Raises:` section. The docstrings skill requires sections on public functions; these are private (`_`-prefixed), so a `Raises:` note is an optional consistency improvement.
        - Resolution: Deferred — helpers are private (`_`-prefixed) and the `ValueError` branch is an unreachable-by-construction guard (callers draw every table name from `_FK_ORDER`); documenting it is optional, not a required fix. Matches cr_20260812v01 finding 5.1.

6. [completed] Numbered local variables in `read_db_state` - `code/load_catalog_data/db_io.py`
   - 6.1. [suggestion] Lines 267, 272, 277, 282, 287, 292, 297, 314, 334: the per-table fetch loops assign the constructed row into distinct numbered names (`r`, `r2`-`r5`, `rdep`, `r6`-`r8`) even though each name is used only within its own loop. A single reused local per loop would read more clearly; the mix of `rdep` and numbered names shows the numbering is not load-bearing.
        - Current: `r = SystemRow(*row)` / `r2 = DataSourceRow(*row)` / ... / `rdep = DeploymentRow(*row)` / `r6 = TableRelationshipRow(*row)`
        - Expected: reuse one local name per loop, e.g. `row_obj = SystemRow(*row)` then `state.systems[pk(row_obj, "systems")] = row_obj`
        - Resolution: Deferred — readability-only naming preference; each name is loop-local, so the numbering causes no shadowing or correctness risk. Matches cr_20260812v01 finding 6.1.

7. [completed] Repo-root path expression duplicated - `code/load_catalog_data/db_io.py`
   - 7.1. [suggestion] Lines 32 and 59: the vendored-lib `sys.path.insert` computes `Path(__file__).resolve().parents[2]` inline, and `_REPO_ROOT` (line 59) is defined later with the identical expression. Defining `_REPO_ROOT` once before the import block and reusing it (`sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`) would remove the duplication.
        - Current: `sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "code" / "lib"))` ... `_REPO_ROOT = Path(__file__).resolve().parents[2]`
        - Expected: `_REPO_ROOT = Path(__file__).resolve().parents[2]` defined once (before the vendored import), then `sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`
        - Resolution: Deferred — optional DRY tidy-up of a trivial two-token expression; hoisting `_REPO_ROOT` above the import block would split it from the other module constants, and each site carries its own accurate explanatory comment (lines 27-31, 54-58), so the duplication poses no realistic drift risk. Matches cr_20260812v01 finding 7.1.

8. [completed] Unlogged swallow in `_warn_if_dirty_working_tree` - `code/load_catalog_data/db_io.py`
   - 8.1. [suggestion] Lines 123-124: the `except (FileNotFoundError, subprocess.TimeoutExpired): return` swallows the failure with no log at all; exception-handling guideline 5 asks for logging at every stage. A `logger.debug(f"dirty-tree check skipped: {e}")` in the except would leave a trace without changing behavior.
        - Current: `except (FileNotFoundError, subprocess.TimeoutExpired):` / `    return`
        - Expected: `except (FileNotFoundError, subprocess.TimeoutExpired) as e:` / `    logger.debug(f"dirty-tree check skipped: {e}")` / `    return`
        - Resolution: Deferred — the swallow is deliberate and documented in the docstring ("Any failure to run `git status` is swallowed: the check is diagnostic, not a gate"), and the branch is near-unreachable in practice: the helper only runs after `git rev-parse HEAD` just succeeded with the same binary, repo root, and timeout (line 192), so a `git status` failure immediately afterward is an edge of an edge.

## Skills with No Issues

1. Type Hints: No issues found. Every function annotates parameters and return types with modern syntax (`dict[str, str]`, `tuple[Any, ...]`, `tuple[bool, Any]`, `dict[str, list[RowChange]]`). The `row: Any` / `old: Any` / `key: Any` on the polymorphic param builders remains the established deliberate choice for functions accepting any of the 9 row dataclasses (accepted since cr_20260729v01).
2. Docstrings: Issues found — see task 2.1 (optional ltree[] completeness, deferred). Otherwise no issues: `resolve_commit_sha`, `read_db_state`, `apply_diff`, `_warn_if_dirty_working_tree`, `_insert_params`, `_validated_ts_update_args`, and `_update_params` all carry Google-style docstrings with Args/Returns/Raises as applicable and document the "why". The cr_20260812v01 finding 1.1 fix held — `_update_params` now reads "it is ignored for the other seven tables" (lines 733-734), matching the 9-table `_FK_ORDER` minus the two validated-bearing tables.
3. Comments: Issues found — see task 1.1 (stale six-tuple "Map:" overview comment). All other comments re-verified accurate against the current code: the vendored-lib/re-export comment (lines 26-31) matches reality (`load_catalog_data.py` line 47 and `unit_tests/test_integration.py` line 35 both import `connection_kwargs` from db_io); the `_REPO_ROOT` and `_GIT_TIMEOUT_SECONDS` rationales (lines 54-64); the two deferred-constraint blocks (lines 67-100) and their in-transaction counterparts (lines 969-982); the `_SELECT_COLUMNS` trailing-ref_table_id note (lines 216-218) and its `_insert_params` mirror (lines 638-640); the composite-PK WHERE warning (lines 448-452); the validated_ts CASE semantics (lines 459-462, 665-666, 678); the transactional-TRUNCATE note (lines 984-986); and the `_FK_ORDER` comment (lines 897-900).
4. Logging: No issues found. Uses the vendored `logconfig.get_logger` (line 33), never `print()`; interpolating messages use f-strings and carry context (per-table row counts across all 9 tables, commit SHA, diff summary, reset_hstry marker, git-timeout details including `_REPO_ROOT`). No Entering/Exiting messages. The dirty-tree WARNING (lines 126-129) sits at the right level — a lineage caveat, not a failure.
5. Exception Handling: Issues found — see task 8.1 (optional debug log in the deliberate swallow, deferred). Otherwise no issues: `apply_diff` catches `except Exception as e:`, logs the root cause first, then rolls back and bare-`raise`s (lines 1042-1048) so a dead-connection `rollback()` failure cannot mask the original error; `resolve_commit_sha` catches the specific `FileNotFoundError` and `subprocess.TimeoutExpired`, chains both with `raise ... from e`, and separately handles a nonzero return code with stderr in the message (lines 166-189).
6. Executable Scripts: N/A - imported library module, no `main()` / `__main__` entry point.
7. Data Validation: N/A - DB I/O module, not a `data_val_` script; corpus validation lives elsewhere.
8. Unit Tests: N/A for this file - tests live under `unit_tests/test_db_io.py` (reviewed separately in `cr_20260813v01_test_db_io.md`); 75 tests pass against the current module.
9. SQL (correctness, sql-development): Issues found — casing only (task 4.1, deferred). Correctness re-verified for all 9 tables: INSERT `%s` count <-> param-tuple length (systems 4, data_sources 5, schemas 6, tables 6, columns 10 with trailing `ref_table_id` after the bind-NULL `update_reason`, deployment_tables 6, table_relationships 10 incl. the trailing `validated` CASE guard, column_mappings 9 incl. `%s::ltree[]` and CASE guard, concepts 6 incl. `%s::ltree[]`); UPDATE counts (systems 4, data_sources 5, schemas 6, tables 6, columns 10, deployment_tables 6 = 4 SET + 2 PK, table_relationships 11 and column_mappings 10 with the stamp_now/else_value pair plus full composite-PK WHERE, concepts 6) with PK columns excluded from every SET; DELETE/hstry-insert bind 1-, 2-, or 3-tuples matching each table's PK arity, and each `*_hstry` INSERT's column list matches its SELECT list plus `end_ts = now()`. Each `read_db_state` SELECT's column order feeds its dataclass positionally (or via explicit kwargs for column_mappings/concepts); no `SELECT *`; every statement is `%s`-parameterized; the sole runtime-built statement (`TRUNCATE TABLE {}`) uses `sql.Identifier` over the fixed `_HSTRY_TABLES` tuple; the two `SET CONSTRAINTS ... DEFERRED` statements interpolate only fixed module constants whose names the DDL-agreement tests pin.
