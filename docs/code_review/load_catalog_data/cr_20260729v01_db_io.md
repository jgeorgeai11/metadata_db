---
name: cr_20260729v01_db_io
goal: Re-review of code/load_catalog_data/db_io.py against python-development and sql-development at its new module path (renamed from load_metadata_db); SQL/logic is unchanged since cr_20260727v01, leaving only the same standing style/consistency suggestions.
created: 2026-07-29 14:38:31
updated: 2026-07-29 14:38:31
---

## Implementation Plan

1. [completed] SQL keyword casing - `code/load_catalog_data/db_io.py`
   - 1.1. [suggestion] Lines 238-288, 410-462, 464-519, 522-543, 546-618, plus 90-93, 522-525, and 1016: SQL keywords are uppercase across every `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_DELETE_*`/`_HSTRY_INSERT_*` constant, `_SET_CONSTRAINTS_DEFER_DEPLOYMENT_TABLES`, `_INSERT_LOAD_AUDIT`, and the runtime `TRUNCATE TABLE {}` template; sql-development best-practices guideline 7 says "Lowercase everything". Applied consistently across the whole module, so this is a single file-wide style deviation rather than many separate defects. Representative line shown.
        - Current: `"SELECT system, description, notes, update_reason FROM systems"`
        - Expected: `"select system, description, notes, update_reason from systems"`
        - Resolution: Deferred — file-wide, style-only casing deviation applied consistently; these are Python string literals holding SQL, so keyword casing carries no semantic weight and reformatting touches every statement for cosmetic gain only. Matches the standing decision in cr_20260724v03, cr_20260725v01, and cr_20260727v01 (each finding 1.1).

2. [completed] Private-helper docstrings omit `Raises` - `code/load_catalog_data/db_io.py`
   - 2.1. [suggestion] Lines 634, 766, 870: `_insert_params`, `_update_params`, and `_pk_params` each `raise ValueError` on an unknown table (lines 738, 867, 885) but their docstrings carry no `Raises:` section. The docstrings skill requires sections on public functions; these are private (`_`-prefixed), so a `Raises:` note is an optional consistency improvement.
        - Resolution: Deferred — helpers are private (`_`-prefixed) and the `ValueError` branch is an unreachable-by-construction guard (callers draw every table name from `_FK_ORDER`); documenting it is optional, not a required fix. Matches cr_20260727v01 finding 2.1.

3. [completed] Numbered local variables in `read_db_state` - `code/load_catalog_data/db_io.py`
   - 3.1. [suggestion] Lines 304-335: the per-table fetch loops assign the constructed row into distinct numbered names (`r`, `r2`, `r3`, `r4`, `r5`, `rdep`, `r6`), and the `column_mappings`/`concepts` loops continue with `r7`/`r8` (lines 351, 371), even though each name is used only within its own loop. A single reused local per loop would read more clearly and avoid implying the values coexist; the mix of `rdep` and numbered names shows the numbering is not load-bearing.
        - Current: `r = SystemRow(*row)` / `r2 = DataSourceRow(*row)` / ... / `rdep = DeploymentRow(*row)` / `r6 = TableRelationshipRow(*row)`
        - Expected: reuse one local name per loop, e.g. `row_obj = SystemRow(*row)` then `state.systems[pk(row_obj, "systems")] = row_obj`
        - Resolution: Deferred — readability-only naming preference; each name is loop-local, so the numbering causes no shadowing or correctness risk. Matches cr_20260727v01 finding 3.1.

## Skills with No Issues

1. Type Hints: No issues. Every function annotates parameters and return types with modern syntax (`dict[str, str]`, `tuple[Any, ...]`, `tuple[bool, Any]`, `tuple[str, ...]`). The `row: Any` / `old: Any` / `key: Any` on the polymorphic param builders (`_insert_params`, `_update_params`, `_pk_params`) is a deliberate design choice for functions accepting any of the 9 row dataclasses.
2. Docstrings: No issues on public functions — `connection_kwargs`, `resolve_commit_sha`, `read_db_state`, and `apply_diff` all have Google-style docstrings with Args/Returns/Raises as applicable and document the "why" (search_path rationale, SHA fallback and pinned cwd, atomic bootstrap reset, validated_ts transitions, the step-0 constraint deferral). The private `_warn_if_dirty_working_tree` and `_validated_ts_update_args` are also documented. The only gap is the optional private-helper `Raises` note (finding 2.1).
3. Comments: No issues. Comments explain "why" and are current: the module docstring's ltree[]/text[] cast rationale (lines 11-14) matches the read casts (lines 272-288) and write casts (lines 448-462); the deferred-constraint block (lines 74-93, 1002-1008) justifies both the deferral and the plain-string form; the composite-PK WHERE warning (lines 485-489) correctly states deployment_tables' PK is (table_id, system) with data_source_id/physical names as mutable content; the validated_ts CASE semantics (lines 496-505, 700-716, 741-763) and the transactional-TRUNCATE note (lines 1010-1013) are accurate; the `_FK_ORDER` comment (lines 934-937) correctly says deployment_tables references tables, systems, and data_sources.
4. Logging: No issues. Uses `logconfig.get_logger`, never `print()`; all messages use f-strings (guideline 6) and carry context (per-table row counts across all 9 tables, commit SHA, diff summary, reset_hstry marker, git-timeout details). No Entering/Exiting messages (funcName is auto-logged).
5. Exception Handling: No issues. `apply_diff` catches `except Exception as e:`, logs the root cause first, then rolls back and bare-`raise`s (lines 1069-1075) so a dead-connection `rollback()` failure cannot mask the original error. `connection_kwargs` raises a contextful `RuntimeError` listing the missing env vars and a `ValueError` for a bad `schema`. `resolve_commit_sha` catches the specific `FileNotFoundError` and `subprocess.TimeoutExpired`, chains both with `raise ... from e`, and separately handles a nonzero `git` return code with stderr in the message. `_warn_if_dirty_working_tree` deliberately swallows `FileNotFoundError`/`TimeoutExpired` because the check is diagnostic, not a gate (documented).
6. Executable Scripts: N/A — imported library module, no `main()` / `__main__` entry point.
7. Data Validation: N/A — DB I/O module, not a `data_val_` script; corpus validation lives elsewhere.
8. Unit Tests: N/A for this file — tests live under `unit_tests/test_db_io.py`; not exercised here.
9. SQL (correctness, sql-development): No issues beyond casing (finding 1.1). `%s` count <-> param-tuple length <-> column-list width re-verified for all 9 tables against `data_model.py`, including the recently renamed `deployment_tables`:
   - INSERT `%s` <-> tuple: systems 4<->4, data_sources 5<->5, schemas 6<->6, tables 6<->6, columns 9<->9, deployment_tables 6<->6, table_relationships 10<->10 (incl. trailing `validated` CASE guard), column_mappings 9<->9 (incl. `%s::ltree[]` for target_tables_referenced and trailing CASE guard), concepts 6<->6 (incl. `%s::ltree[]`); each column list equals `%s` count plus the literal `now()`/CASE positions.
   - UPDATE `%s` <-> tuple: systems 4<->4, data_sources 5<->5, schemas 6<->6, tables 6<->6, columns 9<->9, deployment_tables 6<->6 (4 SET + 2 PK), table_relationships 11<->11 and column_mappings 10<->10 (both carry the stamp_now/else_value CASE pair plus full composite-PK WHERE), concepts 6<->6 (single PK, no CASE guard). PK columns are excluded from every SET.
   - DELETE / HSTRY-INSERT: single-PK tables (systems, data_sources, schemas, tables, columns, concepts) bind a 1-tuple; deployment_tables and column_mappings name both PK parts in WHERE and bind 2-tuples; table_relationships names all 3 PK parts and binds a 3-tuple. Each `*_hstry` INSERT's column list matches its SELECT list plus `end_ts = now()`.
   - `read_db_state`: each SELECT's column order matches its dataclass field order for positional construction — verified `_SELECT_DEPLOYMENT_TABLES` (lines 262-266: table_id, system, data_source_id, physical_database_name, physical_schema_name, physical_table_name) against the 6-field `DeploymentRow` (data_model.py lines 312-317), and the 10-column `_SELECT_TABLE_RELATIONSHIPS` <-> `TableRelationshipRow`; `column_mappings` and `concepts` build via explicit kwargs. All SELECTs list explicit columns (no `SELECT *`); every statement is `%s`-parameterized with no value interpolation; the sole runtime-built statement (`TRUNCATE TABLE {}`) uses `sql.Identifier` over the fixed `_HSTRY_TABLES` tuple. The `%s::ltree[]` write casts and `::text[]` read casts sit at the correct positions for both target_tables_referenced and related_object_ids, and `_INSERT_LOAD_AUDIT` binds 5 params to 5 columns.
