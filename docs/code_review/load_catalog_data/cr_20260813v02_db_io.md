---
name: cr_20260813v02_db_io
goal: Re-review of code/load_catalog_data/db_io.py against python-development and sql-development after cr_20260813v01's stale-comment fix landed; one new pending finding on the unguarded `_FK_ORDER` bucket lookup (reviewed together with unit_tests/test_db_io.py), plus the standing deferred suggestions.
created: 2026-08-13 11:47:38
updated: 2026-08-13 12:22:38
---

## Implementation Plan

1. [completed] Unknown-table handling in `apply_diff` - `code/load_catalog_data/db_io.py`
   - 1.1. [minor] Line 963 (and 965, 967): the per-phase buckets are pre-seeded from `_FK_ORDER` (lines 959-961) and then indexed directly by `c.table`, so a `RowChange` naming a table absent from `_FK_ORDER` raises a bare `KeyError('<table>')` with no log and no context. `_FK_ORDER` (lines 903-913) is a verbatim second copy of `data_model.TABLE_ORDER` (data_model.py lines 45-55) and nothing pins the two together, so the drift is reachable by adding a 10th table to `TABLE_ORDER` (the corpus/diff side, `corpus_diff.compute_diff` line 135) and missing this tuple. The resulting `KeyError` then escapes into `load_catalog_data.main`'s `except KeyError` (load_catalog_data.py lines 376-379), which reports it as `Missing required config field: 'concepts'` — an actively misleading diagnosis. The three sibling dispatchers already model the right behavior (`raise ValueError(f"Unknown table {table}")`, lines 703, 832, 850). A companion agreement test is recorded as finding 1.2 in `cr_20260813v02_test_db_io.md`.
        - Current: `for c in diff.deletes:` / `    deletes_by_table[c.table].append(c)` (lines 962-963, and the same pattern for `diff.updates` / `diff.inserts`)
        - Expected: route the three loops through a guard that fails in the module's established style, e.g. `def _bucket(buckets: dict[str, list[RowChange]], c: RowChange) -> None:` raising `ValueError(f"Unknown table {c.table}")` when `c.table not in buckets`, called as `_bucket(deletes_by_table, c)`
        - Resolution: Implemented as specified — added the module-private `_bucket(buckets, c)` helper immediately above `apply_diff`, raising `ValueError(f"Unknown table {c.table}")` when `c.table` is not a pre-seeded key, and routed all three loops (`diff.deletes`/`diff.updates`/`diff.inserts`) through it. The helper carries a Google-style docstring (Args/Raises) explaining why the guard exists — the bare `KeyError` was misreported as a config error by `load_catalog_data.main`. Companion coverage added in `test_db_io.py` (finding 1.2 there for the `_FK_ORDER`/`TABLE_ORDER` agreement test, plus a new parametrized `test_apply_diff_unknown_table_raises_value_error` covering the new branch across all three phases). `db_io.py` holds at 100% statement coverage; 725 passed, 19 skipped.
   - 1.2. [minor] Lines 956-957: `apply_diff`'s docstring declares only `psycopg2.Error` under `Raises:`, but the `except Exception ... raise` at lines 1044-1050 re-raises every exception type unchanged — including the `ValueError` the param builders raise for an unknown table (lines 703, 832, 850) — and the bucket `KeyError` of finding 1.1 is raised before the `try` even opens. Callers reading the contract (`load_catalog_data.run`, line 309) see a narrower surface than the function actually has (docstrings guideline 4: keep docstrings current).
        - Current: `Raises:` / `    psycopg2.Error: On any database failure (after rollback).`
        - Expected: `Raises:` / `    psycopg2.Error: On any database failure (after rollback).` / `    ValueError: If a `RowChange` names a table outside `_FK_ORDER`.`
        - Resolution: Implemented as specified — `apply_diff`'s `Raises:` section now lists `ValueError: If a `RowChange` names a table outside `_FK_ORDER`.` alongside the existing `psycopg2.Error` entry, covering both the new `_bucket` guard and the param builders' unknown-table `ValueError` that the `except Exception ... raise` re-raises unchanged.

2. [completed] Module docstring names only one of the two ltree[] columns - `code/load_catalog_data/db_io.py`
   - 2.1. [suggestion] Lines 11-14: the module docstring explains the ltree[] read/write cast pattern solely for `column_mappings.target_tables_referenced`, but `concepts.related_object_ids` follows the identical pattern (read cast lines 248-251, write casts lines 423-427 and 480-484). Naming both columns would make the docstring complete.
        - Current: `` `column_mappings.target_tables_referenced` is a Postgres `ltree[]`; ``
        - Expected: `` `column_mappings.target_tables_referenced` and `concepts.related_object_ids` are Postgres `ltree[]` columns; ``
        - Resolution: Deferred — optional completeness polish; both concepts sites carry their own comments stating they mirror the column_mappings pattern (lines 420-422, 693-694, 820-823), so the pattern is discoverable at every point of use. Matches cr_20260813v01 finding 2.1.

3. [completed] CI SHA env-var name is an inline literal - `code/load_catalog_data/db_io.py`
   - 3.1. [suggestion] Lines 155, 169-170, 178-180, 186-188: `"GITHUB_SHA"` is read as a bare literal in the lookup and restated as prose in the three `RuntimeError` messages. A `_CI_SHA_ENV_VAR = "GITHUB_SHA"` module constant interpolated into all four sites would make a future platform seam a one-line change.
        - Current: `ci_sha = os.environ.get("GITHUB_SHA", "").strip()` plus three hardcoded `GITHUB_SHA` mentions in the error strings
        - Expected: `_CI_SHA_ENV_VAR = "GITHUB_SHA"` beside the other module constants, then `os.environ.get(_CI_SHA_ENV_VAR, "")` and `f"... {_CI_SHA_ENV_VAR} is unset/blank ..."`
        - Resolution: Deferred — optional DRY tidy-up on a seam pinned by `test_resolve_commit_sha_prefers_ci_env`; all four sites sit within one 60-line function where drift is immediately visible, and the error strings read more naturally as literal prose. Matches cr_20260813v01 finding 3.1.

4. [completed] SQL keyword casing - `code/load_catalog_data/db_io.py`
   - 4.1. [suggestion] Lines 94-100, 201-251, 375-508, 511-583, 991, plus `_INSERT_LOAD_AUDIT` (487-490): SQL keywords are uppercase across every `_SELECT_*`/`_INSERT_*`/`_UPDATE_*`/`_DELETE_*`/`_HSTRY_INSERT_*` constant, both `_SET_CONSTRAINTS_DEFER_*` statements, and the runtime `TRUNCATE TABLE {}` template; sql-development best-practices guideline 7 says "Lowercase everything". Applied consistently module-wide, so this is one file-wide style deviation. Representative line shown.
        - Current: `"SELECT system, description, notes, update_reason FROM systems"`
        - Expected: `"select system, description, notes, update_reason from systems"`
        - Resolution: Deferred — file-wide, style-only casing deviation applied consistently; these are Python string literals holding SQL, so keyword casing carries no semantic weight and reformatting touches every statement (and the tests' `startswith("INSERT INTO ...")` assertions) for cosmetic gain only. Matches the standing decision from cr_20260724v03 through cr_20260813v01.

5. [completed] Private-helper docstrings omit `Raises` - `code/load_catalog_data/db_io.py`
   - 5.1. [suggestion] Lines 599, 731, 835: `_insert_params`, `_update_params`, and `_pk_params` each `raise ValueError` on an unknown table (lines 703, 832, 850) but their docstrings carry no `Raises:` section. The docstrings skill requires sections on public functions; these are private (`_`-prefixed), so a `Raises:` note is an optional consistency improvement.
        - Resolution: Deferred — helpers are private (`_`-prefixed) and the `ValueError` branch is an unreachable-by-construction guard for callers that draw every table name from `_FK_ORDER`; documenting it is optional. Note that finding 1.2 does add the same `ValueError` to the *public* `apply_diff` docstring, where the sections are required. Matches cr_20260813v01 finding 5.1.

6. [completed] Numbered local variables in `read_db_state` - `code/load_catalog_data/db_io.py`
   - 6.1. [suggestion] Lines 267, 272, 277, 282, 287, 292, 297, 314, 334: the per-table fetch loops assign the constructed row into distinct numbered names (`r`, `r2`-`r5`, `rdep`, `r6`-`r8`) even though each name is used only within its own loop. A single reused local per loop would read more clearly; the mix of `rdep` and numbered names shows the numbering is not load-bearing.
        - Current: `r = SystemRow(*row)` / `r2 = DataSourceRow(*row)` / ... / `rdep = DeploymentRow(*row)` / `r6 = TableRelationshipRow(*row)`
        - Expected: reuse one local name per loop, e.g. `row_obj = SystemRow(*row)` then `state.systems[pk(row_obj, "systems")] = row_obj`
        - Resolution: Deferred — readability-only naming preference; each name is loop-local, so the numbering causes no shadowing or correctness risk. Matches cr_20260813v01 finding 6.1.

7. [completed] Repo-root path expression duplicated - `code/load_catalog_data/db_io.py`
   - 7.1. [suggestion] Lines 32 and 59: the vendored-lib `sys.path.insert` computes `Path(__file__).resolve().parents[2]` inline, and `_REPO_ROOT` (line 59) is defined later with the identical expression.
        - Current: `sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "code" / "lib"))` ... `_REPO_ROOT = Path(__file__).resolve().parents[2]`
        - Expected: `_REPO_ROOT = Path(__file__).resolve().parents[2]` defined once (before the vendored import), then `sys.path.insert(0, str(_REPO_ROOT / "code" / "lib"))`
        - Resolution: Deferred — optional DRY tidy-up of a trivial two-token expression; hoisting `_REPO_ROOT` above the import block would split it from the other module constants, and each site carries its own accurate explanatory comment (lines 26-31, 54-58). Matches cr_20260813v01 finding 7.1.

8. [completed] Unlogged swallow in `_warn_if_dirty_working_tree` - `code/load_catalog_data/db_io.py`
   - 8.1. [suggestion] Lines 123-124: the `except (FileNotFoundError, subprocess.TimeoutExpired): return` swallows the failure with no log at all; exception-handling guideline 5 asks for logging at every stage.
        - Current: `except (FileNotFoundError, subprocess.TimeoutExpired):` / `    return`
        - Expected: `except (FileNotFoundError, subprocess.TimeoutExpired) as e:` / `    logger.debug(f"dirty-tree check skipped: {e}")` / `    return`
        - Resolution: Deferred — the swallow is deliberate and documented in the docstring ("Any failure to run `git status` is swallowed: the check is diagnostic, not a gate"), and the branch is near-unreachable: the helper runs only after `git rev-parse HEAD` just succeeded with the same binary, repo root, and timeout (line 192). Matches cr_20260813v01 finding 8.1.

9. [completed] Line-width outliers versus the sibling test file - `code/load_catalog_data/db_io.py`
   - 9.1. [suggestion] Lines 69, 313, 731: the module wraps at 79 columns everywhere except these three lines (82, 80, and 80 columns). The sibling `unit_tests/test_db_io.py` answers the same wrapping question far more loosely (16 lines over 79, up to 87 — see finding 3.1 of `cr_20260813v02_test_db_io.md`), and no repo config pins a limit (`pyproject.toml` declares no ruff/flake8 line-length), so the convention lives only in the code itself.
        - Current: `def _update_params(table: str, row: Any, old: Any, key: Any) -> tuple[Any, ...]:` (line 731, 80 columns)
        - Expected: wrap the signature across lines, e.g. `def _update_params(` / `    table: str, row: Any, old: Any, key: Any` / `) -> tuple[Any, ...]:`
        - Resolution: Deferred — no configured line-length standard exists in `pyproject.toml`, and three lines overshooting by 1-3 columns is cosmetic; worth revisiting only alongside a repo-wide formatter decision that would also cover the test file.

## Skills with No Issues

1. Type Hints: No issues found. Every function annotates parameters and return types with modern syntax (`dict[str, str]`, `tuple[Any, ...]`, `tuple[bool, Any]`, `dict[str, list[RowChange]]`). The `row: Any` / `old: Any` / `key: Any` on the polymorphic param builders remains the established deliberate choice for functions accepting any of the 9 row dataclasses (accepted since cr_20260729v01).
2. Docstrings: Issues found — see findings 1.2 (public `apply_diff` `Raises:` understates the surface) and 2.1 (optional ltree[] completeness, deferred). Otherwise no issues: `resolve_commit_sha`, `read_db_state`, `_warn_if_dirty_working_tree`, `_insert_params`, `_validated_ts_update_args`, and `_update_params` all carry Google-style docstrings with Args/Returns/Raises as applicable and document the "why"; `apply_diff`'s five-step sequence narrative still matches the implementation step for step (deferrals 978-984, truncate 985-995, deletes 998-1005, updates 1008-1016, inserts 1019-1023, audit 1028-1037).
3. Comments: No issues found. The cr_20260813v01 finding 1.1 fix held — lines 364-373 now describe the four SQL dicts plus the three param builders, and all seven named structures exist (`_INSERT_SQL` 853, `_UPDATE_SQL` 864, `_DELETE_SQL` 875, `_HSTRY_INSERT_SQL` 886, `_insert_params` 599, `_update_params` 731, `_pk_params` 835). Re-verified the rest against the code: the vendored-lib/re-export note (26-31), `_REPO_ROOT` and `_GIT_TIMEOUT_SECONDS` rationales (54-64), the two deferred-constraint blocks (67-100) and their in-transaction counterparts (971-984), the `_SELECT_COLUMNS` trailing-`ref_table_id` note (216-218) and its `_insert_params` mirror (640-642), the composite-PK WHERE warning (450-454), the validated_ts CASE semantics (461-464, 665-666, 680), the transactional-TRUNCATE note (986-988), and the `_FK_ORDER` comment (899-902).
4. Logging: No issues found. Uses the vendored `logconfig.get_logger` (line 33), never `print()`; interpolating messages use f-strings and carry context (per-table row counts across all 9 tables, commit SHA, diff summary, reset_hstry marker, git-timeout details including `_REPO_ROOT`). No Entering/Exiting messages. The dirty-tree WARNING (126-129) sits at the right level — a lineage caveat, not a failure.
5. Exception Handling: Issues found — see findings 1.1 (unguarded bucket lookup raising a contextless `KeyError`) and 8.1 (optional debug log in the deliberate swallow, deferred). Otherwise no issues: `apply_diff` catches `except Exception as e:`, logs the root cause first, then rolls back and bare-`raise`s (1044-1050) so a dead-connection `rollback()` failure cannot mask the original error; `resolve_commit_sha` catches the specific `FileNotFoundError` and `subprocess.TimeoutExpired`, chains both with `raise ... from e`, and separately handles a nonzero return code with stderr in the message (166-189).
6. Executable Scripts: N/A - imported library module, no `main()` / `__main__` entry point.
7. Data Validation: N/A - DB I/O module, not a `data_val_` script; corpus validation lives elsewhere.
8. Unit Tests: N/A for this file - tests live in `unit_tests/test_db_io.py`, reviewed in `cr_20260813v02_test_db_io.md`; 75 tests pass against the current module. Two gaps found there are cross-referenced here: the missing `_FK_ORDER`/`_HSTRY_TABLES` vs `TABLE_ORDER` agreement test (bearing on finding 1.1) and the unasserted hstry-before-DELETE ordering that this module's `apply_diff` docstring step 2 (lines 935-936) promises.
9. SQL (correctness, sql-development): Issues found — casing only (finding 4.1, deferred). Correctness re-verified statement by statement for all 9 tables: INSERT `%s` count matches param-tuple length (systems 4, data_sources 5, schemas 6, tables 6, columns 10 with trailing `ref_table_id` after the bind-NULL `update_reason`, deployment_tables 6, table_relationships 10 incl. the trailing `validated` CASE guard, column_mappings 9 incl. `%s::ltree[]` and CASE guard, concepts 6 incl. `%s::ltree[]`); UPDATE counts (systems 4, data_sources 5, schemas 6, tables 6, columns 10, deployment_tables 6 = 4 SET + 2 PK, table_relationships 11 and column_mappings 10 with the stamp_now/else_value pair plus the full composite-PK WHERE, concepts 6) with PK columns excluded from every SET; DELETE and `*_hstry` INSERT statements bind 1-, 2-, or 3-tuples matching each table's PK arity, and each `*_hstry` INSERT's column list matches its SELECT list plus `end_ts = now()`. Each `read_db_state` SELECT's column order feeds its dataclass positionally (or via explicit kwargs for column_mappings/concepts); no `SELECT *`; every statement is `%s`-parameterized; the sole runtime-built statement (`TRUNCATE TABLE {}`) uses `sql.Identifier` over the fixed `_HSTRY_TABLES` tuple; the two `SET CONSTRAINTS ... DEFERRED` statements interpolate only fixed module constants whose names the DDL-agreement tests pin.
