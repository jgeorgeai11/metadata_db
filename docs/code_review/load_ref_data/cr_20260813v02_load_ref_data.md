---
name: cr_20260813v02_load_ref_data
goal: Re-review code/load_ref_data/load_ref_data.py against python-development and sql-development skills (same-day follow-up to cr_20260813v01, whose findings were all deferred suggestions; this pass adds docstring/error-mapping findings and re-validates the carryovers).
created: 2026-08-13 11:46:49
updated: 2026-08-13 12:28:09
---

## Implementation Plan

1. [completed] Docstring completeness on the validation orchestrator - `code/load_ref_data/load_ref_data.py`
   - 1.1. [minor] Lines 941-978: `validate_all`'s docstring has `Args`/`Returns` but no `Raises` section, yet the function propagates rather than accumulates two failure kinds — `documented_columns(docs_dir)` (line 981) raises `ValueError` on a missing/unparsable/duplicated/schema-straddling docs tree, and `fetch_ref_tables`/`fetch_table_columns`/`fetch_pk_columns` raise `psycopg2.Error`. Every other raising function in the module documents this (`list_csv_files`, `read_csv`, `documented_columns`, `load_tables`, `check_freshness`, `run`), so the omission reads as "this function only ever returns issues" — the opposite of its behavior for broken docs (docstrings guideline 1: Args/Returns/Raises; guideline 4: keep docstrings current). The untested half of this same contract is finding 2.2 in `cr_20260813v02_test_load_ref_data.md`.
        - Current: `    Returns:\n        (loadable, issues) — ...\n    """\n    allow_missing = allow_missing_tables or set()`
        - Expected: add before the closing quotes — `    Raises:\n        ValueError: If the ref docs are missing or malformed (from\n            `documented_columns`) — broken docs abort the run instead of\n            becoming an accumulated issue.\n        psycopg2.Error: On any live-introspection failure.`
        - Resolution: Implemented as specified — added the `Raises` section to `validate_all` documenting both propagating kinds, with the "because the gate cannot be run at all against docs that will not parse" rationale appended to the `ValueError` entry (docstrings guideline 3: document the "why"). The test-side half (finding 2.2 of the test review) is now pinned by `test_validate_all_malformed_docs_raise_not_accumulated`.

2. [completed] Entry-point error mapping - `code/load_ref_data/load_ref_data.py`
   - 2.1. [minor] Line 1450: `main` catches `(FileNotFoundError, ValueError, RuntimeError)`, but the directory walks reached from `run` can raise other `OSError` subclasses that are neither wrapped nor caught — `csv_dir.iterdir()` (line 252), `path.rglob("*")` (line 269), `docs_dir.iterdir()` (line 466), and `docs_dir.rglob(...)` (lines 512-516) surface `PermissionError`/`NotADirectoryError` straight to the top. The module elsewhere deliberately maps `OSError` into its documented `ValueError` contract (`read_csv` lines 329-331, `check_freshness` lines 1186-1190), so an unreadable `data_ref/` or docs folder is the one path that ends in an unhandled traceback with no `Error:` log line and no closing `"=" * 60` separator (executable-scripts guideline: every failure mode maps to a logged exit 1).
        - Current: `except (FileNotFoundError, ValueError, RuntimeError) as e:`
        - Expected: `except (OSError, ValueError, RuntimeError) as e:` (`FileNotFoundError` is an `OSError`, so the existing arm stays covered) — and update `run`'s `Raises` list accordingly
        - Resolution: Implemented as specified — widened the arm to `OSError` with a comment naming the directory walks it covers, and replaced `run`'s `FileNotFoundError` Raises entry with an `OSError` entry that keeps the missing-`csv_dir` case explicit and names the `PermissionError` class. Verified `psycopg2.Error` is not an `OSError` subclass (its MRO is `Exception` only), so the later `except psycopg2.Error` arm is unshadowed and still logs `Database error:`. Pinned by a new `permission_error` param on `test_main_run_error_maps_to_exit_one`.

3. [completed] Readability and defensive-contract suggestions (new) - `code/load_ref_data/load_ref_data.py`
   - 3.1. [suggestion] Lines 630-631: in `_docs_gate_issues` the local names read backwards against the message labels they feed — `missing` holds the CSV columns that are *not documented* and is printed as `undocumented:`, while `extra` holds the *documented* columns absent from the CSV and is printed as `documented but absent:`. The emitted message is correct; only the reading of the code is inverted.
        - Current: `missing = sorted(set(header) - set(doc_cols))`\n`extra = sorted(set(doc_cols) - set(header))`
        - Expected: `undocumented = sorted(set(header) - set(doc_cols))`\n`documented_but_absent = sorted(set(doc_cols) - set(header))`
        - Resolution: Deferred — cosmetic local-name change with no behavioral effect; the two lines sit immediately above the f-string that labels them, so the intent is unambiguous at the point of reading, and renaming touches a function that three call sites depend on for exact message text asserted in the tests.
   - 3.2. [suggestion] Lines 770-775 vs. 692-698: the ragged-row cell-count check is written twice with an identical message — once in `_row_issues` and once in `validate_csv_docs_shape`'s gate-failed early return. A shared `_cell_count_issues(table, header, rows)` helper would keep the two copies from drifting in wording.
        - Current: `for line_no, row in enumerate(rows, start=2):  # header is line 1\n                if len(row) != len(header):\n                    issues.append(...)` (duplicated)
        - Expected: extract `_cell_count_issues(table, header, rows) -> list[str]` and call it from both sites
        - Resolution: Deferred — optional DRY refactor of six lines; the duplication is deliberate and local (the docs-shape copy exists precisely because the column-aligned checks in `_row_issues` cannot run without a trustworthy shape), and both copies are pinned by tests asserting the exact `has N cells, expected M` wording.
   - 3.3. [suggestion] Line 1057: `validate_all` records `loadable[table] = (header, rows, csv_sha256)` unconditionally after `validate_csv`, so a table with accumulated issues still appears in the write-phase payload; only `run`'s `if issues:` gate (lines 1304-1309) stops it from reaching `load_tables`. The docstring already warns "A load may proceed only when `issues` is empty", but the data structure itself does not enforce it.
        - Current: `issues.extend(validate_csv(...))`\n`loadable[table] = (header, rows, csv_sha256)`
        - Expected: `table_issues = validate_csv(...)`\n`issues.extend(table_issues)`\n`if not table_issues:\n            loadable[table] = (header, rows, csv_sha256)`
        - Resolution: Deferred — defence-in-depth against a caller that does not exist: `run` is the only caller and refuses to write on any issue, and the module has exactly one write path. Narrowing `loadable` would also weaken the current invariant that every live-validated CSV is represented, which the "validated N tables" logging leans on.
   - 3.4. [minor] Lines 999, 1028, and 1062: an exemption naming a table that no CSV or DB table matches is silently inert — `--allow-missing-table typo_tbl` neither errors nor warns, so a misspelled CI-computed flag looks like it worked. Reporting unused exemption names as a WARNING would make a broken CI computation visible in the dry-run log. Re-triaged from [suggestion] to [minor] on 2026-08-13: the fail-closed argument holds, but the sibling tool already treats this exact risk as worth a guardrail — `apply_ddl.run` warns `--allow-pending entries match no pending migration` for the same CI-computed-exemption failure mode (`code/apply_ddl/apply_ddl.py` lines 644-649, added because it "is easy to miss because it does not fail the check on its own"). Two tools computing exemptions from an MR diff should report an inert one the same way; the shell loops that build these flags live in `.github/workflows/pre_merge.yml` and are not exercised until CI is switched on.
        - Current: `allow_missing = allow_missing_tables or set()` (membership-tested only; never reconciled against the discovered tables)
        - Expected: after the per-CSV loop, `for unused in sorted(allow_missing - csv_tables): logger.warning(f"--allow-missing-table {unused} matched no ref CSV")` (and likewise for the reshaped/dropped sets)
        - Resolution: Implemented with two deviations from the sketch. (1) The three call sites share one private helper, `_warn_unused_exemptions(flag, exempted, matchable, subject)`, which emits a single WARNING naming every unused name for that flag rather than one line per name — the same shape as `apply_ddl`'s `--allow-pending` warning the finding cites. (2) The `--allow-dropped-table` set is reconciled against `ref_tables - csv_tables`, not `csv_tables`: that flag downgrades the DB-table-*without*-CSV issue, so a name matching a CSV (or no live table) is exactly the inert case. `--allow-missing-table`/`--allow-reshaped-table` use `csv_tables` as the sketch specifies. `allow_dropped_tables` is now localized as `allow_dropped` alongside its two siblings and passed to `missing_csv_issues` unchanged in meaning. Covered by `test_validate_all_inert_exemptions_warned` (all three flags) and `test_validate_all_matching_exemption_not_warned_as_inert` (no false positive); the escape-hatch paragraph in `MAINTAINING.md` now states the guardrail.

4. [completed] Carryovers re-validated from cr_20260813v01 (source unchanged since that review) - `code/load_ref_data/load_ref_data.py`
   - 4.1. [suggestion] Lines 138-141: the comment claims the parsers give "the pre-write validation parity with what the INSERT would accept", but Python's `int`/`float` accept forms Postgres string casts reject (`int("1_0")` succeeds; `'1_0'::integer` fails).
        - Current: `# pre-write validation parity with what the INSERT would accept.`
        - Expected: soften to "approximate parity", or tighten the int/float parsers to reject underscore/inf forms
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 1.1: a curated code set containing `1_0`-style literals is a vanishingly narrow authoring edge case, and the failure mode is loud — the real load fails inside the single transaction and rolls back completely.
   - 4.2. [minor] Lines 629-636: a CSV with a duplicated header name fails the sorted-list comparison while both set differences come out empty, producing `undocumented: [], documented but absent: []`. Re-triaged from [suggestion] to [minor] on 2026-08-13: the narrowness of the trigger (duplicated header *and* an escape-hatch dry-run) is what the deferral rested on, but the outcome in that window is a validation failure whose message names nothing at all — a dead end for the author, who is told the lists disagree and shown two empty lists. The module's whole error-reporting stance is to name the offending object; every other issue string in the file does.
        - Current: `if sorted(doc_cols) != sorted(header):` (set-difference message only)
        - Expected: also detect `len(header) != len(set(header))` and name the duplicated header cell(s)
        - Resolution: Implemented with one accommodation. Inside the existing mismatch branch, `_docs_gate_issues` now computes `repeated = sorted({c for c in header if header.count(c) > 1})` and, when non-empty, emits `"{table}: CSV header repeats column(s) [...] — each column may appear exactly once in the header"`. The set-difference message is dropped only when the repeat is the whole story (both differences empty) — the exact dead-end window the finding names; when a repeat coexists with real drift, both messages are returned so no detail is lost, and the existing wording (and the `missing`/`extra` local names left alone per the deferred 3.1) is unchanged for every non-repeat header. Reached through `validate_csv_docs_shape`, since `validate_csv` returns at the live-header comparison before the gate; covered by `test_docs_gate_duplicate_header_cell_named` and `test_docs_gate_duplicate_header_with_drift_reports_both`.
   - 4.3. [suggestion] Lines 527-534: `documented_columns` takes each table's schema from the shard path's first part without checking it is a documented schema folder, while `list_csv_files` rejects any `data_ref/` folder that is not one.
        - Current: `schema = relative_parts[0]`
        - Expected: validate `schema` against `documented_schemas(docs_dir)` and raise a `ValueError` naming the misfiled shard
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 1.3: requires a doubly-misfiled docs shard, and the outcome is a loud (if contradictory) validation error rather than silent drift.
   - 4.4. [suggestion] Line 142: `_TYPE_PARSERS: dict[str, Any]` could be `dict[str, Callable[[str], object]]` (type-hints guideline 3: be specific).
        - Current: `_TYPE_PARSERS: dict[str, Any] = {`
        - Expected: `_TYPE_PARSERS: dict[str, Callable[[str], object]] = {` (with `from collections.abc import Callable`)
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 2.1: the values are only consumed via `.get()` and a try/except call, so the narrower type adds an import without changing any checking outcome.
   - 4.5. [suggestion] Lines 1320-1321: `run`'s `try/finally` closes the connection with no log (exception-handling guideline 5 lists `finally` among the stages that should log).
        - Current: `finally:\n        conn.close()`
        - Expected: `finally:\n        conn.close()\n        logger.debug(f"Closed connection to {config['database']}")`
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 3.1: cleanup-only `finally`; every terminal outcome already logs before the block runs.
   - 4.6. [suggestion] Lines 1090-1091 vs. 1128-1139: `load_tables` documents `Raises: psycopg2.Error`, but the handler is `except Exception` plus a bare `raise`, so any exception type propagates.
        - Current: `Raises:\n        psycopg2.Error: On any database failure (after rollback).`
        - Expected: note that any exception raised during the load is re-raised unchanged after rollback (in practice `psycopg2.Error`)
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 3.2: only `cur.execute` can realistically fail inside the block, so the documented type is accurate for real inputs.
   - 4.7. [suggestion] Lines 1310-1312: `Validation passed for {len(loadable)} ref table(s)` undercounts on an escape-hatch dry-run, since docs-shape-validated tables are deliberately never in `loadable`.
        - Current: `logger.info(f"Validation passed for {len(loadable)} ref table(s)")`
        - Expected: log both counts, e.g. `f"Validation passed for {len(csv_files)} ref CSV(s) ({len(loadable)} loadable)"`
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 3.3: each docs-shape-validated table emits its own naming WARNING immediately above, so the run log is unambiguous.
   - 4.8. [suggestion] Lines 1112-1118: `load_tables` inserts one row per `cur.execute` in a Python loop; `cur.executemany` would batch the round trips per table.
        - Current: `for row in rows:\n                    cur.execute(\n                        insert,\n                        [value if value != "" else None for value in row],\n                    )`
        - Expected: `cur.executemany(insert, [[value if value != "" else None for value in row] for row in rows])`
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 4.1: the `max_rows_per_table` guardrail caps every table at curated-set size, the whole reload is one transaction, and the per-row form keeps the empty-cell-to-NULL mapping visible at the execute site.

## Skills with No Issues

1. Type Hints: Issues limited to the deferred carryover 4.4 — every function and both NamedTuples are fully annotated with modern, specific syntax (`read_csv(path: Path) -> tuple[list[str], list[list[str]], str]`, `validate_all(...) -> tuple[dict[str, tuple[list[str], list[list[str]], str]], list[str]]`, `set[str] | None` for the three escape-hatch parameters, `frozenset` constants).
2. Docstrings: Issues limited to finding 1.1 and the deferred carryover 4.6 — module docstring plus Google-style docstrings on every function and both NamedTuples; `Args`/`Returns` match the implementations throughout, and the "why" is documented (single-read TOCTOU guarantee, escape-hatch semantics, the deliberate absence of a concurrency lock).
3. Comments: Issues limited to the deferred carryover 4.1 — comments explain "why" rather than "what" (the never-hardcoded guardrail default at 123-126, the `newline=""`/`utf-8-sig` rationale at 334-335, the shard-dedup rationale at 510-511, the empty-sets-message motivation at 583-584, the dry-run-only-flags rationale at 1404-1406).
4. Logging: Issues limited to the deferred carryovers 4.5/4.7 and the new 3.4 — `logconfig` with no `print()`, f-strings throughout, DEBUG/INFO/WARNING/ERROR levels used appropriately, every escape-hatch downgrade logged as a WARNING naming the table and the flag, and the `"=" * 60` run separators owned solely by `main` on every exit arm.
5. Exception Handling: Issues limited to findings 2.1 and the deferred carryovers 4.5/4.6 — no bare `except`; `from e`/`from None` chaining is correct at every wrap site (331, 342, 345, 538-541, 1188-1190); `load_tables` catches broadly only to guarantee rollback, logs a rollback failure without masking the root cause, and bare-`raise`s the original.
6. Executable Scripts: Issues limited to finding 2.1 — `main()` with an `if __name__ == "__main__"` guard (`# pragma: no cover`), required `--config` pointing at `code/load_ref_data/config/load_ref_data.toml`, logging deferred until after `parse_args`, config-existence and TOML-decode failures handled before dispatch, `--check`/`--dry-run` mutual exclusion enforced, and the three escape flags rejected outside `--dry-run`. The mode flags remain the same defensible CI/maintainer deviation from the single-`--config` rule accepted in the prior reviews.
7. Data Validation: N/A — this is a ref-data loader/validator, not a `data_val_` output-validation script, so the `data_val_` naming and `data_validation/` directory convention do not apply.
8. Unit Tests: N/A for this source file's content — the suite lives at `code/load_ref_data/unit_tests/` and is reviewed in `cr_20260813v02_test_load_ref_data.md`; a `pytest --cov=load_ref_data --cov-report=term-missing` run against this module reports 110 passing tests and 100% statement coverage (373/373).
9. SQL (best-practices): No issues found — all seven embedded statements are lowercase with explicit column lists and `inner join`, every value is parameterized via `%s`, `fetch_pk_columns` carries the complete three-way information_schema join predicate (constraint_name, table_schema, table_name — lines 422-426), ordering uses column names (`order by loaded_ts desc, audit_id desc`), and dynamic identifiers are composed with `sql.Identifier`/`sql.Placeholder` rather than string formatting (1096-1110). Statements are deliberately schema-unqualified because `pgconn.connection_kwargs` pins `search_path` to the configured schema. The `Level =` block-annotation convention targets standalone `.sql` files, not these single-statement embedded queries.
