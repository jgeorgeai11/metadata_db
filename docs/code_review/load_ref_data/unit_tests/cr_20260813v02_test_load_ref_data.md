---
name: cr_20260813v02_test_load_ref_data
goal: Re-review code/load_ref_data/unit_tests/test_load_ref_data.py against python-development skills (same-day follow-up to cr_20260813v01, whose three minors are applied and confirmed by a fresh pytest-cov run at 110 passing tests / 100% coverage of load_ref_data.py), focusing on behavior gaps that statement coverage cannot see.
created: 2026-08-13 11:46:49
updated: 2026-08-13 12:28:09
---

## Implementation Plan

1. [completed] Stale fixture comment - `code/load_ref_data/unit_tests/test_load_ref_data.py`
   - 1.1. [minor] Lines 21-22: the comment states that `_LIVE_COLUMNS` is the `clm_type_cd` shape "in ordinal order, matching code/apply_ddl/ddl_ref/0001_ref_initial.sql", but that migration now creates eleven columns (`code, description, pgm_family, stream, service_type, service_category, mdcr_code, pgm_type_cd, pgm_type_desc, ocs_file_setting, notes`) and `data_ref/codes/clm_type_cd.csv` carries the same eleven-column header, while the fixture lists five. The reduced fixture is a perfectly good synthetic shape — only the claim of correspondence is wrong, and it invites the next reader to "fix" the real DDL to match the test (comments guideline 3: keep comments current — an outdated comment is worse than none).
        - Current: `# The clm_type_cd shape used across tests: (name, data_type, is_nullable)`\n`# in ordinal order, matching code/apply_ddl/ddl_ref/0001_ref_initial.sql.`
        - Expected: `# A reduced clm_type_cd-shaped fixture: (name, data_type, is_nullable) in`\n`# ordinal order. Deliberately narrower than the real table (which has the`\n`# crosswalk columns too) — these tests exercise loader logic, not the`\n`# live schema, so the shape only needs to be representative.`
        - Resolution: Implemented as specified. Confirmed the stale claim first: `code/apply_ddl/ddl_ref/0001_ref_initial.sql` and the `data_ref/codes/clm_type_cd.csv` header both carry the eleven columns the finding lists, against the fixture's five. The fixture itself is unchanged — only the comment, which no longer asserts a correspondence to the real DDL.

2. [completed] Behavior gaps invisible to statement coverage - `code/load_ref_data/unit_tests/test_load_ref_data.py`
   - 2.1. [minor] Lines 572-577 (`test_list_csv_files_top_level_csv_raises`): the top-level branch is only driven with a lowercase `stray_tbl.csv`, so the `.lower()` in the source's `entry.suffix.lower() == CSV_SUFFIX` guard (load_ref_data.py line 254) is never exercised. Deleting that `.lower()` keeps every test green while turning a top-level `STRAY.CSV` from a loud error into a silently ignored file — precisely the silent-skip failure the guard exists to prevent, and the same class of gap as the in-folder case-variant already covered at lines 552-557 (unit-tests guideline 7.1: cover edge cases).
        - Current: `(csv_dir / "stray_tbl.csv").write_text("code\n", encoding="utf-8")`
        - Expected: add a sibling test writing `(csv_dir / "stray_tbl.CSV")` and asserting `pytest.raises(ValueError, match="directly under")`, so the case-variant guard is pinned at both tree levels
        - Resolution: Implemented as specified — added `test_list_csv_files_top_level_case_variant_csv_raises` beside the lowercase case, with a comment stating what deleting the `.lower()` would cost (a silently skipped stray). Verified it fails the guard-removed source and passes as written; the case is preserved on write, so the assertion holds on this case-insensitive filesystem too.
   - 2.2. [minor] Lines 761-1157 (the `validate_all` section): every test in the section supplies well-formed docs via `_write_ref_docs`, so no test proves that a malformed docs tree *propagates* out of `validate_all` as a `ValueError` rather than being accumulated into the returned issue list. `documented_columns`' raising behavior is well covered directly (lines 369-498), and `validate_all` calls it unguarded at source line 981, but the boundary between "accumulated issue" and "raised exception" — the distinction `main`'s error mapping depends on — is asserted nowhere. This is the test-side half of the undocumented `Raises` contract recorded as finding 1.1 in `cr_20260813v02_load_ref_data.md`.
        - Current: `_, issues = lrd.validate_all(MagicMock(), "reference", lrd.list_csv_files(csv_dir, {"codes"}), docs_dir, 1000)` — always against a valid docs tree
        - Expected: a test writing a malformed `codes/columns.yaml` (e.g. a row missing `data_type`) alongside a valid CSV and asserting `with pytest.raises(ValueError, match="Malformed ref docs row"): lrd.validate_all(...)`, documenting that broken docs abort the run instead of degrading into an issue
        - Resolution: Implemented as specified — `test_validate_all_malformed_docs_raise_not_accumulated` appends a `data_type`-less row to the docs the existing `_write_ref_docs` helper wrote (via `_append_doc_rows`) alongside a valid `clm_type_cd.csv`, and asserts `pytest.raises(ValueError, match="Malformed ref docs row")`. Placed directly after `test_validate_all_unreadable_csv_accumulated_not_raised` so the accumulate-vs-raise boundary reads as one pair, with a comment naming the contract (`main` maps the ValueError to exit 1). This is the test half of finding 1.1 in `cr_20260813v02_load_ref_data.md`, whose `Raises` section is now written.

3. [completed] Optional coverage and style enhancements - `code/load_ref_data/unit_tests/test_load_ref_data.py`
   - 3.1. [suggestion] Lines 623-630 (`test_read_csv_header_rows_and_hash`): the quoted-field case covers an embedded comma but not an embedded newline, so the documented rationale for `newline=""` in the source (load_ref_data.py lines 334-335: "quoted fields may embed newlines") is not locked by a test.
        - Current: `p.write_text('code,notes\n60,"a, quoted note"\n70,\n', encoding="utf-8")`
        - Expected: extend (or add) a case with `'code,notes\n60,"line one\nline two"\n'` asserting `rows == [["60", "line one\nline two"]]`
        - Resolution: Deferred — the behavior under test is the csv module's own documented handling rather than loader logic, and the existing quoted-comma case already proves the reader is used correctly rather than being hand-rolled on `str.split`.
   - 3.2. [suggestion] Lines 145, 227, 849, 1177, 1178, 1725: six lines exceed the 79-column width the rest of this file and all of `load_ref_data.py` (zero overruns across 1461 lines) hold to; line 227 is the widest at 86.
        - Current: `    issues = _shape(list(_HEADER), [_row("60"), _row("40", "Out")], dict(_DOCUMENTED))`
        - Expected: `    issues = _shape(\n        list(_HEADER), [_row("60"), _row("40", "Out")], dict(_DOCUMENTED)\n    )`
        - Resolution: Deferred — cosmetic wrapping with no behavioral effect; the repo enforces no line-length linter (no ruff/flake8 config in `pyproject.toml`) and comparable overruns exist in several `code/load_catalog_data/` modules, so this is a local tidiness preference rather than a violated standard.
   - 3.3. [suggestion] Lines 806-862 (and the `run --check` drift tests at 1539-1584): `missing_csv_issues` is a public function with no direct tests; its behavior is exercised only through `validate_all` and `run(check=True)`.
        - Current: `missing_csv_issues` is reached only via `lrd.validate_all(...)` and the `--check` drift tests.
        - Expected: optional direct tests calling `lrd.missing_csv_issues(ref_tables, documented, csv_tables, ...)` and asserting the issue list for each drift direction and the exemption path.
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 2.1: the function genuinely executes in these tests (the fresh coverage run shows its lines covered), and both drift directions, the undocumented-table `<schema>` placeholder, and the `--allow-dropped-table` downgrade are each asserted with specific message content, so direct tests would duplicate those assertions.
   - 3.4. [suggestion] Lines 793, 816, 835, 868, 908, 934, 964, 999, 1028, 1104, 1141, 1384, 1404, 1455, 1491, 1596, 1705: the happy-path CSV body `"code,description,pgm_family,stream,notes\n60,Inpatient,f,s,\n"` (and close variants) is repeated inline across many tests; a module-level constant beside `_HEADER` would remove the duplication.
        - Current: `csv_dir = _write_csv(tmp_path, "clm_type_cd.csv", "code,description,pgm_family,stream,notes\n60,Inpatient,f,s,\n")` repeated per test.
        - Expected: `_CSV_TEXT = "code,description,pgm_family,stream,notes\n60,Inpatient,f,s,\n"` defined once and passed to `_write_csv`.
        - Resolution: Deferred — unchanged decision from cr_20260813v01 item 2.2: the inline literal keeps each test's Arrange phase self-contained (the reader sees the exact bytes under test without a lookup), and several tests deliberately vary the body, so the constant would only cover some call sites.

## Skills with No Issues

1. Unit Tests skill: Issues limited to findings 2.1-2.2 and the deferred suggestions 3.1/3.3/3.4 — pytest throughout with no unittest-style classes; file and function naming follow `test_<module>.py` and `test_<function>_<scenario>_<expected>`; conftest fixtures (`fake_conn`/`fake_cursor`) and built-ins (`tmp_path`, `monkeypatch`, `caplog`) are used correctly; `@pytest.mark.parametrize` and `pytest.raises(..., match=...)` are applied where they fit; mocking sits at the psycopg2/env boundary or on `lrd`-module collaborators patched where they are used; tests are order-independent with no shared mutable state (`dict(_DOCUMENTED)`/`list(_HEADER)` copies at every call site); the cr_20260813v01 coverage minors are all applied, and a fresh `pytest --cov=load_ref_data --cov-report=term-missing` run gives 110 passed and 100% statement coverage (373/373).
2. Type Hints skill: No issues found — every test and helper annotates parameters and returns with modern syntax (`list[tuple[str, str, bool]]`, `dict[str, lrd.TableDocs] | None`, `tuple[str, ...]`, `-> None`); `Any` appears only where it mirrors the dynamic config/callback surfaces under test (`_base_config` overrides, `_capture`, `_fake_validate_all`, `load_calls`).
3. Docstrings skill: No issues found — the module docstring states scope and mocking strategy; every non-obvious private helper carries one (`_table_docs`, `_doc_row`, `_write_schema_yaml`, `_write_ref_docs`, `_expected_docs`, `_write_csv`, `_append_doc_rows`, `_UnreadableCsvPath`); the remaining underscore helpers and `test_*` functions are self-describing per test-file convention.
4. Comments skill: Issues limited to finding 1.1 — elsewhere the comments explain intent rather than restating code (the single-read TOCTOU note at 676-682, the shard-dedup rationale at 433-436, the per-table exemption scope at 951-957, the `<schema>` placeholder rationale at 894-898, and the closing `connection_kwargs` re-export note at 1781-1784, which accurately matches the source's `from pgconn import ENV_VARS, connection_kwargs`).
5. Logging skill: N/A — test module; the only logging interaction is asserting production WARNINGs through `caplog`, which is the intended usage.
6. Exception Handling skill: N/A — error paths are verified via `pytest.raises`, not implemented, in a test module.
7. Executable Scripts skill: N/A — not an entry-point script; `main()` is the unit under test.
8. Data Validation skill: N/A — validation behavior is the code under test, not implemented here.
